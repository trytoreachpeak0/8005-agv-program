# Cursor AI ignore list — syntax same as .gitignore
# Files listed here are excluded from indexing, semantic search, @ mentions, Tab/Agent context.
# See: https://cursor.com/docs/reference/ignore-file

# Legacy draft requirements — superseded by structured docs under requirement-documents/
requirement-documents/简易需求文档.md

# Legacy SQL/code reference from old program — reference only, not for AI indexing
mes/旧程序sql访问代码/

# Local schedule draft — do not read or use as project requirements/source of truth
develop_plan/软件开发计划(2026-07-08至2026-07-31).md
