# C2000-A2-KDDA0A0-AD6 寄存器表

来源：`C2000-A2-KDDA0A0-AD6 使用说明书` 第 5.1 节“寄存器列表”（第 21～24 页）。

> 权限说明：`R` = 只读，`RW` = 可读写。
>
> 地址范围列为根据“起始地址 + 个数”计算得到，便于开发时直接查看每组寄存器覆盖范围。

| 起始地址 | 地址范围 | 个数 | 寄存器内容 | 权限 | 数据范围 / 说明 | 适用功能码 |
|---:|---:|---:|---|:---:|---|---|
| 40100 | 40100～40102 | 3 | A2 模块 MAC 地址 | R | 示例：`00 09 f6 01 02 03`，发送序列为：`09 00 01 f6 03 02` | `0x03` |
| 40103 | 40103 | 1 | A2 设备型号 | R | 见设备型号表 | `0x03` |
| 40104 | 40104 | 1 | A2 模块版本号 | R | 例如 `5.2`，高字节为主版本，低字节为次版本 | `0x03` |
| 40105 | 40105～40106 | 2 | IP 地址 | RW | 例如：`10.1.1.11`；默认 IP：`10.1.1.10`。地址表示为 `0x0B01010A`：40105 表示 IP 地址低 2 字节 `0x010A`，40106 表示 IP 地址高 2 字节 `0x0B01` | `0x03`、`0x06`、`0x10` |
| 40107 | 40107 | 1 | Modbus TCP 端口 | RW | 默认 `502`，发送序列为 `01 F6` | `0x03`、`0x06`、`0x10` |
| 40108 | 40108 | 1 | 保留 | RW | 保留。读为 `0`，写无效 | `0x03`、`0x06`、`0x10` |
| 40109 | 40109～40110 | 2 | 子网掩码 | RW | 默认：`255.0.0.0`，格式同 IP 地址 | `0x03`、`0x06`、`0x10` |
| 40111 | 40111～40112 | 2 | 网关地址 | RW | 默认：`10.1.1.200`，格式同 IP 地址 | `0x03`、`0x06`、`0x10` |
| 40113 | 40113 | 1 | 自动或指定 IP | RW | `0`：指定 IP（默认）；`1`：自动获取 IP。发送序列同 Modbus TCP 端口 | `0x03`、`0x06`、`0x10` |
| 40114 | 40114 | 1 | 保留 | RW | 保留。读为 `0`，写无效 | `0x03`、`0x06`、`0x10` |
| 40115 | 40115～40146 | 32 | 服务器 IP 或域名 | RW | 默认 `10.1.2.132`，用于客户端模式，使用字符串形式保存 | `0x03`、`0x06`、`0x10` |
| 40147 | 40147 | 1 | 服务器端口 | RW | `1～65535`，默认 `502`，用于客户端模式 | `0x03`、`0x06`、`0x10` |
| 40148 | 40148 | 1 | 保留 | RW | 保留。读为 `0`，写无效 | `0x03`、`0x06`、`0x10` |
| 40149 | 40149～40150 | 2 | DNS 服务器 IP | RW | 默认 `8.8.8.8`，用于客户端模式解析服务器域名，格式同 IP 地址 | `0x03`、`0x06`、`0x10` |
| 40151 | 40151 | 1 | IO 模块工作模式 | RW | `0`：服务器模式（默认）；`1`：客户端模式。服务器模式时模块在 Modbus TCP 端口监听并等待其他设备连接；客户端模式时模块通过服务器 IP 或域名连接服务器 | `0x03`、`0x06`、`0x10` |
| 40152 | 40152 | 1 | 保留 | RW | 保留。读为 `0`，写无效 | `0x03`、`0x06`、`0x10` |
| 40153 | 40153 | 1 | 是否主动上传 | RW | `0`：被动采集（默认）；`1`：主动上传。主动上传 DI 状态；有 AI 功能的模块没有主动上传功能 | `0x03`、`0x06`、`0x10` |
| 40154 | 40154 | 1 | 设备 ID | RW | 用于区分不同设备 | `0x03`、`0x06`、`0x10` |
| 40155 | 40155～40164 | 10 | 设备名字 | RW | 不足 20 字符以 `0` 填充，高字节在前。例如名字为 `1234`，读 2 个寄存器返回 `0x32 0x31 0x34 0x33` | `0x03`、`0x06`、`0x10` |
| 40165 | 40165 | 1 | 识别 | RW | 识别 | `0x03`、`0x06`、`0x10` |
| 100 | 100～115 | 16 | DO1～DO16 的状态 | RW | `0x0000～0x0001`。保存 DO 当前状态；写入可改变 DO 当前状态，读取代表读取当前 DO 状态。**说明书原文此处写“保存 DO1～DO4 的当前状态”，与本组 16 个寄存器及 DO1～DO16 描述不一致，疑似原文笔误。** | `0x01`、`0x05`、`0x0F` |
| 116 | 116～131 | 16 | DO1～DO16 上电时的状态 | RW | `0x0000～0x0001`。设置 DO 上电状态，写入后，下次重启时 DO 状态为设定值 | `0x01`、`0x05`、`0x0F` |
| 10200 | 10200～10215 | 16 | DI1～DI16 的值 | R | `0x0000～0x0001`，表示 DI 当前电平信号 | `0x02` |
| 40300 | 40300～40315 | 16 | DI1～DI16 正脉冲有效状态 | RW | `0x0000～0x0001`。`0` 表示尚未产生有效正脉冲，`1` 表示产生了有效正脉冲；写 `0` 清零，其他值无效 | `0x03`、`0x06`、`0x10` |
| 40316 | 40316～40331 | 16 | DI1～DI16 负脉冲有效状态 | RW | `0x0000～0x0001`。`0` 表示尚未产生有效负脉冲，`1` 表示产生了有效负脉冲；写 `0` 清零，其他值无效 | `0x03`、`0x06`、`0x10` |
| 40332 | 40332～40347 | 16 | DI1～DI16 的正脉冲计数 | RW | `0x0000～0xFFFF`，可写入任意值；写入后从该值开始计数 | `0x03`、`0x06`、`0x10` |
| 40348 | 40348～40363 | 16 | DI1～DI16 的负脉冲计数 | RW | `0x0000～0xFFFF`，可写入任意值；写入后从该值开始计数 | `0x03`、`0x06`、`0x10` |
| 40364 | 40364～40379 | 16 | DI1～DI16 的电平变化计数 | RW | `0x0000～0xFFFF`，可写入任意值；写入后从该值开始计数 | `0x03`、`0x06`、`0x10` |
| 40380 | 40380～40395 | 16 | DI1～DI16 滤波器参数 | RW | 输入信号必须保持指定采样周期数后才被确认。默认值 `0x6`，即 6 个采样周期；范围 `0x1～0x14`，写入 `0` 返回失败 | `0x03`、`0x06`、`0x10` |
| 40396 | 40396～40411 | 16 | DI1～DI16 自动清零 | RW | 设置读取后自动清零，默认所有通道自动清零。该寄存器相应 Bit 位为 `1` 表示对应通道自动清 `0`，为 `0` 表示手动清 `0` | `0x03`、`0x06`、`0x10` |
| 40412 | 40412～40427 | 16 | DO1～DO16 的工作模式 | RW | `0` 表示电平模式，`1` 表示脉冲模式；其他模式暂时保留，写入其他值返回失败 | `0x01`、`0x05`、`0x0F` |
| 40428 | 40428～40443 | 16 | DO1～DO16 的脉冲宽度 | RW | 最小值 `50`，即输出 `50 ms` 脉冲；最大值 `65535 ms` | `0x03`、`0x06`、`0x10` |

## 功能码速查

| 功能码 | 含义 |
|---|---|
| `0x01` | 读线圈（DO）状态 |
| `0x02` | 读离散输入（DI）状态 |
| `0x03` | 读保持寄存器 |
| `0x05` | 写单个线圈 |
| `0x06` | 写单个保持寄存器 |
| `0x0F` | 写多个线圈 |
| `0x10` | 写多个保持寄存器 |
