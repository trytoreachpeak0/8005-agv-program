# Moved

The file and folder naming conventions now live one level up, in the workspace
repository, at `8005-workspace/file-naming-convention/`. They cover this
repository, `8005-agv-control-server`, and the workspace root — the projects
developed here — and deliberately do not govern the repositories owned by
other people.

Both language versions are there: `file-naming-convention.md` and
`文件命名规范.md`.
