# Ticket 02 test plan

Work vertically through the packaged operator seam:

1. RED/GREEN: entry-point contract and pre-connect rejection of unsafe targets.
2. RED/GREEN: deterministic profile manifest and owned-database lifecycle contract.
3. RED/GREEN: production Host endpoint workload and complete query evidence contract.
4. RED/GREEN: storage breakdown, replay identity, fail-closed gate, and package documentation.
5. Run focused tests, a real-SQL smoke with a deliberately tiny calibrated override, then Tier 1 once.

The full 7/30-day capacity runs are intentionally not executed during implementation; ticket 27 owns those costly gates. Ticket 02 must make those exact profiles reproducible and fail closed when evidence is incomplete.
