# OP_OPERATOR_IDENTITY 样本

当前仅建立治理占位，不含已晋升 latest。

- 默认只允许虚构工号和姓名；真实身份数据必须有批准、最小化和保留期限。
- 样本应覆盖命中、无结果、超时和失败类别，不暴露“账号是否存在”的生产明细。
- 只有 `import-run` 可晋升 latest。
- latest 使用 `latest.csv`、`latest.md` 和 `latest.meta.json`；meta 必须含 `sourceEvidence`，并声明个人信息处理状态。
