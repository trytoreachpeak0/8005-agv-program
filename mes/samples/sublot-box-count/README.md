# SUBLOT_BOX_COUNT 样本

当前仅建立治理占位，不含已晋升 latest。

- 样本必须使用虚构或经批准最小化的 SUBLOT，不得附带无关生产字段。
- 记录查询版本、输入参数是否脱敏、输出 `MAX_BOX_COUNT` 和错误类别。
- 只有 `import-run` 可晋升 latest。
- latest 使用 `latest.csv`、`latest.md` 和 `latest.meta.json`；meta 必须含 `sourceEvidence` 并可追溯到唯一 evidence run。
