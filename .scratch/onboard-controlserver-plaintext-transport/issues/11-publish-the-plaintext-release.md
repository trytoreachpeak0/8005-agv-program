# 发布明文传输版本并写入已知限制

Type: task
Mode: HITL
Status: resolved
Blocked by: 10

## Question

验收通过后，在 `8005-agv-control-server` 发布本轮候选，并把明文传输的代价原样登记。

票 08 已冻结的、本票直接使用的值：

- 版本号 **0.2.0**，tag `w2g-mvp-rc-0.2.0`（不是 `0.1.2`：新旧两端不可互通、旧站点配置会被显式拒绝
  启动，patch 位会被读成可就地替换）；
- tag 目标 commit **`19ce7db70893afea6c6361988c3bc612d77569d0`**（票 09 更新，见下）。证据提交
  `eaaa5b1` / `b6524ca` / `a0f1b3f` 都在候选之后，**不是**候选身份，不要错标到它们；
- 候选根 **`C:\Users\szy\Desktop\w2g-rc-20260901b-19ce7db`**（票 09 更新，见下）；

> **票 09 的更正**：票 08 冻结的候选 `w2g-rc-20260901-238b46e`（`56d4b1cc…`）已作废。它的
> `scripts/` 不含 `Update-ControlServerLocal.ps1`，而手册 §4.5 让站点用包内相对路径运行该脚本，
> 因此交付包里的升级路径跑不了。修发布脚本与手册都会改到被哈希覆盖的文件，候选遂在
> `19ce7db` 重建。两版之间 `src`／`tests`／构建输入零差异，两个 apphost 只差版本资源里那段
> commit 字符串，逐条证据见
> `evidence/g3/20260901-issue09-clean-install-acceptance/SUMMARY.md`。旧候选目录未删，留作对照。
- `0.1.0` / `0.1.1` 保留且不可变，**另需在 `0.1.1` 的 release 说明追加一条回指**：本轮起传输层改明文，
  0.1.1 与 0.2.0 服务端不可互通，指向本候选。只改说明正文，不动 tag 与资产；该编辑同样按下面的
  VERBATIM 回读判据取证。
- 写说明时不要把候选号与车载端程序集自身的 `0.2.0-safety-mock-travel+<commit>` 混为一谈，二者无关。

发布内容：

- annotated tag 绑票 08 候选对应的服务端 commit，显式使用 **40 位完整 SHA**（票 28 的教训：用
  `HEAD` 或分支名会在 owner 恰好推了新 commit 时错标目标）；
- 资产为完整候选上传，沿用 0.1.1 的三资产形态；
- 发布说明中的已知限制，**措辞不得淡化**：
  1. 车载凭据 `credentialProof` 以明文经网络传输，抓包即可永久冒充车载端下发仓门 IO 命令；
  2. `motionState` 等安全闸门输入经明文传输后可被篡改，票 14／本轮票 10 证过的「移动中拦、停稳
     放行」在不可信网络下不再成立；
  3. Kestrel 单一绑定使异机部署下 `/health/live` 与 `/version` 随投影端点一并暴露于厂内网（票 01
     第 3 节冻结的形态，本轮新增的暴露面）；
  4. 新旧两端不可混用，必须同版本升级；错配的失败形态见手册对照表（票 01 第 7 节）；
  5. 适用前提是受控的工厂内网；离开该前提不得使用本版本；
  6. 沿用 0.1.1 中仍然成立的其余已知限制（真实八仓 IO 与车载目标终端硬件资格等）。

约束：

- **只在服务端仓建 tag／release**。不得为凑齐三仓写入只读的车载端仓——车载端的版本引用归其 owner，
  agent 不得代建（票 28 的先例是由用户执行）；
- 协议仓不动，`protocol-v0.1.1` 保持原状；
- 上传前不得修改包内任何文件；如需更正，写在**包外**的 Release 说明里（票 12／15 的先例：包内手册
  在全量哈希内，改它要重建候选）。

判据必须是回读取证而非上传返回码：远程资产**下回来重算**哈希应全部 MATCH（红侧翻一字符应
MISMATCH）；tag 对象 peel 出的 commit 与候选 manifest 记录逐字一致；线上说明正文与本地副本在同一
归一化下 VERBATIM MATCH。注意 `gh --jq` 的输出在 PowerShell 里会被拆成数组而非字符串，逐字比对前
先落盘再 `Get-Content -Raw`，否则会假红（票 29 踩过）。

Mode 为 HITL：发布是对外动作，需用户明确授权后执行。

## Comments

### 2026-09-01 票 10 resolved，本票前置全部就绪

真车闭环已在 `19ce7db` 候选上走通（generation 8，`Stage=Completed`，证据
`ControlServer_MVP@17aec50`）。对本票的三处影响：

- 已知限制第 2 条现在有正面证据可引：明文形态下「移动中拦、停稳放行」在两条腿各成立一次
  （票 10 §2）。措辞仍不得淡化——证的是**受控内网下闸门按设计工作**，不是它在不可信网络下仍
  成立。
- 已知限制第 6 条继续成立且要点名：**`HW-REAL-IO` 未取得**。票 10 的 IO 输入是八仓模拟器
  （用户选定），真实 IO 模块、接线、锁与光幕仍无资格证据。
- **票 10 未采集 HMI 截图**（现场由用户操作，业务节点记录改由车载端日志承担）。若发布说明或
  验收附件需要票 29 式截图，须另行补采，不能引用票 10 的证据充数。

候选身份、tag 目标、版本号一律沿用上文票 08／09 冻结的值，未因票 10 改变。

## Answer

**已发布**：<https://github.com/trytoreachpeak0/8005-agv-control-server/releases/tag/w2g-mvp-rc-0.2.0>。
逐条证据在服务端仓 `evidence/rc/20260901-issue11-publish-0.2.0/`（提交 `ControlServer_MVP@ef669ea`），
取证脚本 `Verify-Publication.py` 可复跑，此处只留结论与偏差。

### 1. 判据全部是回读，**16 / 16 如期，红侧每个检测器都响**

没有一条结论取自上传返回码：

- **三个资产下回来重算**，逐字节等于上传前记录的值与大小（zip `abbff6e0…` / 123,991,314 B、
  manifest `23898539…` / 171,966 B、SHA256SUMS `5e3aeb3a…` / 99,003 B）。红侧三条：同一比对器喂
  翻一字符的期望值全部 MISMATCH。
- **tag 从远端重新 fetch 进临时 ref 再 peel**，不信本地那份。类型是 `tag`（annotated），peel 出
  `19ce7db70893afea6c6361988c3bc612d77569d0`，与下载回来的 manifest 的
  `components.controlServer.commit` 逐字一致。建 tag 时用的是 40 位全 SHA（票 28 的教训）。红侧同样响。
- **两份说明与本地副本 VERBATIM MATCH**（0.2.0 13,221 字符、0.1.1 10,482 字符），归一化只做换行，
  正文逐字符比。`gh --jq` 的输出先落盘再比（票 29 踩过的假红），脚本里写了原因。

### 2. 两条超出票要求的回读

票只要求资产哈希 MATCH。另加两条，因为发布说明第 2 节对它们有明确主张，主张就该回读：

- **GitHub 送回来的 zip 对着它自己的 manifest 自洽**：不解压直接从 zip 流里校验，870 条目、唯一顶层
  目录 `w2g-rc-20260901b-19ce7db/`、**869 / 869 哈希匹配**，0 失配 0 缺失；
- 两个单独上传的资产与 zip 内同名文件**逐字节相同**——这正是说明第 2 节声称的，此前只是断言。

### 3. 上传前包内零修改，0.1.1 资产零改动

zip 直接从候选根打包，869 条哈希在上传前（`OK=869 MISMATCH=0 MISSING=0`、
`unlistedFilesInReleaseRoot=0`、红侧翻红且产物未改动）与下载后各验一次，是同一批 869 条。
0.1.1 的回指**只改说明正文**：编辑后其三个资产摘要与编辑前逐字相同（`12a7ce56…`、`e607a9d9…`、
`727b3fbb…`）。

回指块放在正文**开头**而非追加到末尾：「新旧不可互通」是运维告警，埋在 204 行末尾等于没有。这是对
票里「追加」二字的一处有意偏离，据实记录。

### 4. `f0d6f1b` 有意不在本次发布内（用户 2026-09-01 决定）

发布过程中发现服务端仓有一个未推的提交 `f0d6f1b`「Stop offering an already-accepted demand back to
journey intake」，修的正是票 10 现场抓到、map 已判为 Out of scope 的受理重放冲突。

它改 `JourneyRuntimeEngine` 的候选扫描（新增 `DEMAND_ALREADY_ACCEPTED` 分类，**排在全部闸门之前**）
——正是票 10 闭环走的那条路径。并进候选的真实代价是：重建候选 ＋ tier 1 ＋ 重跑干净安装验收
（`DEMAND-DECISION-REACHED` 的判定分布会变）＋ **重跑真车闭环**（需现场安全 GO）＋ 重跑 G3；而
`f0d6f1b` 自身除两条回归测试外无任何现场证据。把未验证的改动换掉已验证的交付物，与本 map 一路的
证据规则相反。**用户选择 A：0.2.0 按现状发，修复走 0.2.1。**

处置：`f0d6f1b` 已推到 `ControlServer_MVP` 但**不被任何 tag 引用**；发布说明已知限制第 7 条点名该
commit、说明本版本不含它、并写明后续版本发布时需重跑干净安装验收与真车闭环。

### 5. 约束遵守情况

- **只在服务端仓建 tag／release**。车载端仓零写入，其 `238b46e` 的 tag 归 owner，说明第 8 节写明；
  协议仓与 `protocol-v0.1.1` 未动。
- 已知限制七条，前三条（凭据明文过网、安全闸门输入可篡改、健康与版本端点随投影暴露）**措辞未淡化**，
  「受控工厂内网」写为适用前提而非缓解措施。
- 说明里对三处**不利事实**据实写入而非略过：G3 三个 runner 跑在 `56d4b1c` 而非 `19ce7db`（附源码零
  差异的依据，并写明「不假装重跑过」）；`W2G-IS-00` 的覆盖形态因 TLS 代码路径消失而变化；
  `HW-REAL-IO` / `HW-ONBOARD-TARGET` 两项硬件资格与 `RecoveryRequired` 出口仍未取得。
- 票 10 未采集的票 29 式 HMI 截图**没有用票 10 的证据充数**，说明里也未引用截图。
