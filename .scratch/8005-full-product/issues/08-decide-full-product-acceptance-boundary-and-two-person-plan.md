# 决定完整产品验收边界、证据要求与双人分工排期

Type: grilling
Status: open
Blocked by: 07, 14

## Question

MVP 的验收是 P0～P7 一次受控试运行的顺序，只证明单次旅程：一台指定 AGV、一张 Map、真实 MesIngest、RIoT 和物料完成一次机台取货到关卡卸货。完整产品要另一套验收边界。同时决定与 Kun Wang 的分工与排期。

现场物理前提已具备，规模由票 11 定案（用户 2026-09-03 确认）：**3 台 AGV、3 个 8005 项目独占充电桩、n 个等待点，全部在唯一作业图 `mapId` 25 上**。跨 Map 运输已判为本图 Out of scope，验收不涉及多图。

### 票 02 传下来的一条已知验收难题

[票 02 决议](02-answer.md)判定 `REQ-0172`（充电失败车辆不取得独立优先级，与普通待充车辆
进同一排队集合按电量排序）与 `REQ-0173`（成功预占后不可抢占）**都是重构类条目，必须实现**。
但现场是 **3 台 AGV、3 个 8005 独占充电桩**（票 11 定案），桩数等于车数，**充电排队在现场
自然永远凑不出争用**。

票 02 明确了这一点的性质：3 车 3 桩削弱的是这两条的**现场可观测性**，不改变它们的归属，
也不减少实现义务。因此本票须为这两条判定证据形态，**不得默认「不触发即通过」**。可考虑的
形态包括受控双 Fake 联调、临时缩减名册使桩数小于车数的受控轮次、或以单元／集成测试覆盖
排队与预占不变量并在规格中如实记录「现场不产生争用证据」。选哪种由本票判。

同类问题还有 `REQ-0294` 的否定性约束（禁止 RIoT「一键停靠」与 `parkConfig` 调用）——
票 12 会给意见，最终证据形态归本票。

须回答：

1. **验收边界**：完整产品的「通过」是什么。单次旅程不够——多车并发要证明多车同时执行且不互相污染（现场是 3 台，须说明验收要求几台同时在跑），自动充电要证明一个完整充电周期含预占与释放，充电失败治理要证明清桩闭环。这些是一次综合试运行，还是若干次按批次分别验收？若分别验收，各批次的验收出口条件如何表述才不至于「每批都要清一次场」。

   **票 11 交下来的一道难题：3 车 3 桩使充电排队在现场自然不会触发。**`REQ-0172`（充电失败车辆不取得独立优先级，与普通待充统一按电量排序）与 `REQ-0173`（成功预占后不可抢占，在途期间也不得被更低电量车辆窃取）是排队与竞争语义，桩数等于车数时现场永远凑不出争用。这两条仍必须实现——它们是基线条目，不因当前配置凑巧不触发而免除——所以本票须判定它们的验收证据从哪来：临时用 `ChargingStationAllocationHold`（`REQ-0288`）暂停一两个桩人为造出争用、只用 Fake／集成测试证明、还是承认这一档没有现场证据并如实记录。**不得默认「不触发即通过」。**
2. **证据要求**：每类验收需要什么不可改写的证据，谁产生、存哪里。MVP 的做法是先过离线与假接口门禁再进现场，证据存 `evidence/{g2,g3,rc}`；完整产品的证据目录结构与保留策略是什么，尤其是多车与充电这类无法用单次日志证明的场景。
3. **安全边界**：MVP 有严格清场与永久红色证据的规则。多车并发在现场同时跑多台车（现场共 3 台），清场规则如何调整；自动充电涉及车辆自主前往充电桩，安全边界与人工介入点在哪。
4. **失败与回退**：某批次现场验收未通过时的处置——整批回退、部分保留、还是允许带已知缺陷投运。判断标准是什么。
5. **双人分工**：多车相关改动在车载端的量大于服务端，而 `8005-agv-onboard-hmi` 对本工作区只读、归 Kun Wang。逐批次给出服务端／车载端／协议三侧的工作归属，以及跨仓库反馈的通道（issue 与 PR 允许，带可复现门禁证据）。
6. **排期与批准节奏**：协议 v2 的正式 release 须两名产品负责人签名，AI 与 CI 不能批准。给出从本图结束到 v2 release 的批准路径：谁在什么节点签什么。同时明确记录——MVP 那张图留下的「车载端开发同事未评审、未批准」在本图同样成立，本图的范围与顺序由用户单人批准，Kun Wang 事后按 notify-after-change 通知，规格中须如实记录这一点，不得表述为已获双方同意。
7. **外部未知**：Kun Wang 的实际可投入时段是本工作区看不到的外部事实。本票须明确排期中哪些部分依赖该事实、以及在拿到之前排期以什么假设成立。

### 票 03 传下来的第二条验收难题

[票 03 决议](03-answer.md)把 B3 定为**档 2**，其中 `REQ-0196`（途中受控追加）、`REQ-0198`
（顺路取货延迟约束）与 `REQ-0197` 的换序半以 **fail-closed 形态**实施——门禁与代码路径建好，
但因 RIoT 给不出站到站的路径成本与时间估计而**恒走拒绝分支**。

这不是打折：`REQ-0198` 原文明文要求「任一增量无法可靠计算时**不得追加**」，fail-closed 正是
需求规定的行为。但它对验收的含义与 3 车 3 桩那条完全同类：

**上面这段的前提已于同日被推翻**（用户质疑后复核：`edges/{mapId}` 给全图边表含 `cost`，
且 RIoT 的规划算法就是跑最短路）。这三条现在有两种走向，**由票 14 依 Round 43 的实测定案**：
判为可用则按完整能力验收、本票无额外难题；判为不可用则回到「只能证明门禁正确拒绝」，
与 3 车 3 桩同类，**不得默认「不触发即通过」**。**本票在票 14 出结论前不要为这三条设计证据形态。**

另一条本票要知道的事实：票 03 已定多车推进模型为**单 worker 按车串行 + 每车超时预算**，
所以三车不会并发写库。验收时若要构造车辆间的竞争场景，竞争发生在**选车排序层**而不是
持久化层——`IX_VehicleDispatchLeases_VehicleKey` 那条过滤唯一索引在 B3 里被删除并下移到
`OrderIntents`，它不再是多车竞争的观测点。

票 13 定的六类任务执行范围也会给本票加验收对象：六类中若有几类在验收期现场没有真实物料可跑，
证据形态同样归本票判，票 13 会点名是哪几类。

### 已知边界

- 本票不执行任何验收，不跑试运行，不接触现场设备。
- 本票不代 Kun Wang 承诺工期，不代他评审或批准任何内容。
- 本票不决定批次划分本身，那是票 09；本票决定的是每个批次要满足什么才算完成。
- 本票不处理 MVP 的 P0～P7 试运行进度与 5 个未闭合缺陷，地图已将其列为 Out of scope。

## 来自票 13 的输入（2026-09-03）

**验收期只有 `WIRE_TO_GATE` 一类能在现场真实触发。**其余五类的固定端点（派工待送、烘箱、三光、
氮气柜）当前不在 `mapId` 25 的 206 个站里（206 = 205 个 AREA 命名机台 + 1 个关卡，
`evidence/g3/20260827-map25-dynamic-gates-refresh/result.json` 等三份证据同值）。现场这些区域物理
存在、MES 六类工序真实在跑，但 AGV 路网只测绘了焊线区到关卡这一段（用户 2026-09-03 确认）。

按 `REQ-0335`，缺绑定的任务类型会被正确地挡在投运之外——**这是设计行为，不是缺陷**。因此本票要为
`FP-C9a` 定的是**机制正确性的验收出口，不是「六类都跑通」**：

1. **fail-closed 的正确拒绝**：为一个缺绑定的 `TASK_TYPE` 提交 Demand，证明被 `REQ-0335` 挡住且
   原因精确。**这条能在现场取到证据。**
2. **绑定完备时的正确放行**：只能靠双 Fake 联调，或在现场补一个测试绑定取证。
3. **`REQ-0187` 唯一性跨类型生效**：需要构造同一 AREA 在两个 `TASK_TYPE` 下出现的观测，现场自然
   发生的概率取决于工序排布，可能需要造。

**两条硬约束：**

- **不得把 `FP-C9a` 归入 `证据受限实施`。**这里的证据不可得是**现场尚未测绘**造成的，补测绘后无需
  改代码即自动可取证，且门禁两侧都能验证（放行侧靠联调）。与票 14 清零掉的那个形态不是一回事。
- **`REQ-0202`（任务类型初始优先级带）在票 13 定的第一批里没有可观测行为**——最高初始带只有
  `STAGING_TO_WIRE`，而它被分到第二批，所以第一批的最高带是空的。这条的现场验收要等第二批。

另有一条与本票相关的缺口：**其余五类被丢弃的量当前不可观测**（它们的名字在控制服务端全仓含
`evidence/` 零命中，连日志都没有），因此无法用「放开前后的执行量差」验证放开是否符合预期。
若需要这个基线数，要在放开前先加一段观测——属实施图，但本票定验收证据形态时要知道它不存在。

详见 [票 13 决议](13-answer.md) 的 Q7。

## 来自票 12 的输入（2026-09-03）

**两条，第一条与票 13 的同类问题必须一致处置。**

**一、等待点在验收期无法现场真实触发，出口条件归本票。**

Round 43 实测：mapId 25 的 206 个站 `type` 恒为 1、`desc`／`param`／`user_define_properties`
全空，站名只有 `N#-#`（119）／`T##-##`（86）／`关卡`（1）三种形态，**没有一个是等待点或
充电桩**。票 12 的 Q1 据此定为「机制一次建齐，投运待现场测绘」，`n` 不写进规格。

**这与票 13 的「验收期只有 `WIRE_TO_GATE` 能在现场真实触发」是同一类问题，本票须一致处置**：
定的是机制正确性的验收出口，不是「空闲返回在现场跑通」。**两者都不得归入 `证据受限实施`**
——那里的证据不可得是现场尚未测绘造成的，补测绘后无需改代码。

**票 04 的三个 8005 独占充电桩是第三个同类实例**（`REQ-0171` 的桩名册同样是「地图 + 站点」
标识，那三个站点当前不在图上）。本票定验收边界时，这三簇应当共用同一条出口规则。

**二、`REQ-0294` 的否定性约束取架构测试，但它是横切工程不属 `FP-C4`。**

票 12 的 Q8 定为甲档：加一条架构测试，断言产品代码 `src/` 下 `.Raw` 命中数为 0，且对 Facade
的调用方法集合是获批清单的子集。**不叠加运行期 DelegatingHandler**（`Raw` 走同一个
HttpClient，路径拦截拦得住但那是运行时才发现；甲档在 CI 就挡住）。

**作用域远大于空闲返回**：它保护的是全部 RIoT 调用面，`REQ-0294` 只是唯一一条把它写进需求
文本的条目。票 09 排批次时把它作为一条**独立的横切工程**，本票定验收证据形态时同样不要把它
写成 `FP-C4` 的出口条件——否则 `FP-C4` 延后会连带把整个 RIoT 调用面的守卫一起延后。

**当前风险是量化的**：`OrderClient.Raw`／`TaskClient.Raw`／`MapClient.Raw` 三个属性在
vendored 包里**都是 public**，vendored `RIoT.Sdk.Generated.dll` 里 16 个 `Park*` 类型全部
在场，故 `riotSession.Tasks.Raw.Api.TaskNamespace.Vehicles.Park.PostAsync(...)` **能直接编译
通过**，编译期、运行期、测试期**三处都没有守卫**。现有测试只有正向断言（6 条实际调用的
路径）与供应链完整性（包哈希），**没有一条断言「不得调用其它端点」**。

详见 [票 12 决议](12-answer.md) 的 Q1、Q8 与第 1.8 节。

## 来自票 04 的输入（2026-09-04）

**三条硬输入，第一条会决定 `FP-C1` 能不能进带现场验收出口的批次。**

**一、`FP-C1` 的验收出口条件必须含「充电桩安装完成 + 名册录入」这个现场前置。**用户
2026-09-04 确认：三个 8005 独占充电桩**物理上还没安装**，map 25 当前没有充电桩站点。故投运
第一天名册必然为空，而票 04 已把「名册为空」定为退化到 `ManualChargingHold` 的人工充电路径。
**验收不能假定投运即可自动充电。**这与票 13 的「验收期只有 `WIRE_TO_GATE` 能现场真实触发」、
票 12 的「等待点尚未测绘」是同一类问题，**三者的处置方式应当一致**。

**二、`REQ-0174` 引的失败码 407802 只在测试环境验证过。**出处只有 Round 24
（`rcs/riot-behavior-lab/evidence/rounds/2026-07-21-round-24/runs/S1b-hang-detail.json:115-116`，
`"resultStr": "未知类型错误,导致订单挂起:错误编码为:407802"`；同文件 `:136` 的 `taskState`
`"7,78,1,0,..."` 把它绑到 `act 78 param1=1` 即开始充电），而那一轮的范围写着「仅测试车；
map30（api测试2）」，**不是 `RIOT-8005-RUNTIME`**。两个 SDK 仓库都没有错误码表，RIoT 自己也只回
「未知类型错误」。而 `REQ-0174` 要求的是「已经**目标 build/契约验证**的失败码」——
**目标 build 上没验过**。`ConfirmedUnableToCharge` 的严格系统事实因此缺一项目标环境证据，
本票要判它是验收前置、验收项，还是可带条件投运。

**三、两条「配置凑巧使规则不触发」的条目。**票 02 已指出 3 车 3 桩使 `REQ-0172`（充电失败车辆
不取得独立优先级）与 `REQ-0173`（成功预占后不可抢占）在现场自然不触发；票 04 另加一条同类——
**桩数 < 车数时，充满电的车占着桩会饿死低电车**（票 04 的 Q4 定「不主动腾桩」，因为 RIoT 的
离桩由下一张订单触发、`act(78,2,0)` 自动插入，8005 没有单独的离桩动作）。当前 3 桩 3 车不触发，
但桩数可配置。三条的验收证据怎么取归本票。

详见 [票 04 决议](04-answer.md) 的 Q3、Q4 与第六节。
## 来自票 05 的输入（2026-09-04）

**一个新的分工面：控制服务端的第一个人机界面。**票 05 定 `REQ-0268` 的看板本期实施，而控制服务端
**历史上没有任何界面**——`ControlServer.sln` 11 个工程里没有 WPF／Blazor／Razor，全部 HTTP 端点只有 5 个
（`/health/live`、`/health/ready`、`/version`、`/api/runtime/sessions`、`/api/onboard/v1/vehicle-safety`）。
车载端 WPF 归 Kun Wang，**这个界面归 Zhengyu Shao**，是本图新产生的一块工作量。技术形态（Web 还是 WPF）
留给实施图的原型票，本图不设计 UI——但它决定工程量，是排期的一个真实不确定源。

**验收边界有两处必须如实写明，不得含糊**：

1. **完整产品的验收证据里不会有任何人员认证项。**票 05 的 Q1 取甲档，`FP-C10`（8 条）与 `FP-C6`（7 条）
   全部延后。**不得表述为「权限已在批次 0 验收过」**——批次 0 的权限骨架在两端都零实现，唯一的认证是一个
   全场共用的环境变量，`administratorRole` 是客户端自称的字符串。
2. **`FP-C9b` 本期实施但 `REQ-0339` 只做一半**：验收出口只能验「明确确认影响预览」，不能验「使用当前
   密码新鲜二次认证」。票 05 明确排除了用共享密钥冒充二次认证。

**验收出口另有两条来自票 05 的约束**：`FP-C5` 只做最小闭环（`REQ-0310`／`REQ-0317`／`REQ-0318`／
`REQ-0320`），`REQ-0311` 退化实施为「恢复入口不在看板、不在车载端」，`REQ-0316` 延后；`FP-C8` 的看板
**只允许 fail-safe 方向的写入口**（暂停、收紧、阻断），激活、回滚、投运一律走受控预置配置，验收时
不要去验界面上不存在的放开动作。

详见 [票 05 决议](05-answer.md) 的第三节。

## 来自票 06 的输入（2026-09-04）

**本票的「两人分工」前提被交接改写，且多了一个 v2 打 tag 的硬前提。**

**一、`8005-agv-onboard-hmi` 与 `slots-simulator` 今后由我方负责开发**（用户 2026-09-04 告知）。
这是**将来**的开发归属变更，**不是现在的写权限变更**——工作区 `CLAUDE.md` 的写权限表照旧，
那两个仓对 agent 仍然只读。**交接的是那两个仓的开发，不是 `8005-agv-protocol` 的所有权**，
Kun Wang 仍是协议共同维护者。本票要判的是：分工与排期在交接前后各是什么形态，以及交接的
时间点是不是排期的一个约束。

**二、v2 打 tag 需要两名不同产品负责人，「第二人是谁」是本票必须回答的。**
`docs/release-governance.md` 要两名不同产品负责人签外部 attestation，
`g1-validate.mjs` 第 27 行实打实校验 `new Set(approvals.map(a=>a.ownerId)).size===2`，
**AI 与 CI 不能批准**。两端都归一边写之后这条不会自动失效——票 06 的判断是**交接完成前
Kun Wang 就是那个人**，且**双人签名的价值在完整产品阶段变大而不是变小**（它从此是唯一一个
外部评审点）。票 06 明确不改 `release-governance.md`。本票若认为要改，须单独论证。

**三、G2 证据的可信度有一个结构性缺口，本票的验收出口要处置。**票 06 查实：
两个实现仓的依赖清单里**没有任何 JSON Schema 校验库**；
`8005-agv-control-server/tools/ControlServer.Conformance/Program.cs` 共 55 行，做的全部事情是
算 manifest 哈希、比编译期常量、打印 `PREFLIGHT_PASS`，**不跑任何向量、不校验任何 schema**；
G2 证据（`artifacts/g2/*/gate-result.json`）记着 `vectorIds` 数组与 `testExitCode: 0`，
而那个 exit code 来自本仓自己的 xunit 套件——**向量的 `input.ndjson` 与 `expected.json`
从未被机械执行过**，绑定靠人的断言。

票 06 已补了一半：`productAssertions` 由 IS-01 的特例推广为全部向量必填，
使「某条向量在某端算通过」第一次有了可机械检查的定义。**另一半——谁来跑向量、用什么跑——
是本票的验收出口要回答的。**注意这与「双 Fake 联调」不是一回事：Fake 是对端的替身，
向量执行器是判据的执行者，目前后者根本不存在。

详见 [票 06 决议](06-answer.md) 的 1.3、第 6 问与第四节。

## 来自票 07 的输入（2026-09-04）

**票 07 已定切片家族，本票的三条前提被改写。**

**一、切片家族不覆盖完整产品的全部工作。**票 07 的 3.2 给出无切片清单：
`FP-C5` AGV 归档恢复（6 条）、`FP-C9b` 公共站点绑定运维治理（9 条）、
`FP-C8` 看板本体（`REQ-0268`／`REQ-0269`）、票 14 的 `RouteGraphSnapshot` 引擎——
四类都是**单端工程**（服务端↔RIoT／↔人／内部），照 MVP 的「双端能力」粒度立不了切片；
`FP-C6`／`FP-C10` 是票 05 判的延后。**这四类的验收出口形态由本票定。**

**二、多车并发（B2）在保形向量下结构上不可证明。**`runner-contract.schema.json` 的
`steps[]` 只有 `step`／`atMs`／`action`／`messageType`／`payloadRef` 五个字段，
`action` 十一个取值没有一个能指定「哪辆车」，`vectorId` 与 `integrationSliceId` 都是单值，
`input.ndjson` 是一条流。**保形向量表达的是一个会话的一条时间线。**这解释了票 06 为什么
给 B2 一条向量都没有。多车并发的证明只能落在服务端单端测试 ＋ 现场验收，**本票要定
那个现场验收出口长什么样**（3 台车、`mapId` 25、票 11 已确认的现场规模）。

**三、`runner/` 两个 schema 修还是删，是本票的决定。**票 07 的 1.5 查实：
`runner-contract.schema.json` **描述不了仓库里真实存在的向量**（契约要顶层对象带六项必填，
而 `input.ndjson` 是 step 对象的 NDJSON 流，step 带着契约 `additionalProperties: false`
禁止的 `adapter`／`result`／`virtualTimeOnly`，又缺必填的 `payloadRef`；
`initialPersistentFacts` 被锁成只能是空对象，任何需要前置状态的场景无法表达）；
`result.schema.json` `required` 16 项而真实 `gate-result.json` 只提供 5 项，
**缺的 11 项里 `onboardHmiCommit`／`fakePeerIdentities`／`firstDivergence`／
`evidencePointers` 正是「双端联合证据」的核心**。而 `g1-validate.mjs` 对 `runner/`
只做 `ajv.validateSchema` ＋ `ajv.compile`，**从不用它校验任何文档**。

**这是票 06 那条「G2 记着 `vectorId` 却只跑自家 xunit」的上游原因**——门禁结果本来就
没按契约产出。本票若定「要有真跑向量的 runner」，这两个文件要按真实格式重写；
若定「不要」，应当删除而不是留作虚假文档。

**四、门禁名称不受两仓归属交接影响。**`CONTROL_SERVER_G2`／`ONBOARD_HMI_G2` 命名的是
**哪一端**不是**哪个人**，切片的 `gates` 数组一字不改。本票要回答的仍是票 06 交来的
「v2 打 tag 的第二名产品负责人是谁」。

详见 [票 07 决议](07-answer.md) 的第 4 问、3.2 与第四节。
