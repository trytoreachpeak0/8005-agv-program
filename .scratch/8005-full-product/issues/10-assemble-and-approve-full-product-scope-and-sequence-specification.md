# 汇编《8005 完整产品实施范围与顺序规格》并最终批准

Type: grilling
Status: open
Blocked by: 08, 09

## Question

把票 01～09 的全部决定汇编成一份可直接交给实施图的《8005 完整产品实施范围与顺序规格》，并请用户最终批准。批准即本图 Destination 达成，地图结束。

规格至少须包含：

1. **身份绑定**：v1.0.0 基线路径与 SHA-256 `5e409953dc24d3acbe399e1babf761fbf12c005662f59a0eac5fd240038d6fba`、content commit `b9f321228b534a3b316d3ac1abede05176ed6a70`、tag `requirements-baseline-v1.0.0`；MVP 剖面 TSV 与其 SHA-256；票 09 的 348 行定稿与其 SHA-256。规格自身也记录哈希。
2. **348 行全量剖面**：逐条给出批次归属，零遗漏，与票 09 定稿一致。
3. **批次与依赖图**：N 个批次的主题、内容、批次间依赖与理由，批次 1 的选定说明。
4. **重构与增量的分类结论**及两类排序规则（票 02）。
5. **各业务面的范围决定**：多车并发与 Worklist 执行模型（票 03）、自动充电与充电失败治理（票 04）、治理面增量（票 05）——每一面写清做什么、不做什么、不做的是延后还是范围外。
6. **协议 v2 冻结面**（票 06）：消息面、错误码、交付类别、向量范围、与 v1 的版本关系、冻结强度与实施图还需补什么。
7. **切片家族**（票 07）：编号体系、与 `W2G-IS-00`～`07` 的关系、门禁模型、与协议 v2 的绑定。
8. **验收边界与证据要求、双人分工与批准路径**（票 08）。
9. **明账**：MesIngest 100 条为何在范围外及其指针；MVP 作为软前置的假定；车载端开发同事未评审未批准这一事实；协议 v2 正式 release 的双人签名门禁不变，AI 与 CI 不能批准；本图不产代码，批次实施归随后的实施图。

批准前须交叉核验：规格中每一处结论都能追溯到某张已解决票据；票 01～09 的答案与规格无矛盾；348 行计数闭合。

### 已知边界

- 本票不产代码，不改任何仓库的实现，不改 v1.0.0 基线，不切 protocol release。
- 批准人是用户一人。本票不代 Kun Wang 批准任何内容，不得把单人批准表述为双方同意。
- 规格落在 `.scratch/8005-full-product/`；是否提升到 `docs/` 由用户在批准时一并决定。
- 本票结束后，若涉及 `8005-agv-protocol` 的推送，按 notify-after-change 规则在同一任务内开 issue @`SocialKKKK`。

## 来自票 13 的输入（2026-09-03）

**一、剖面结构变了。**新增第 11 列 `Batch0FormNote`；新增簇编号 `FP-C9a`、`FP-C9b`；批次 0 由 186
降至 167，待排期条目由 62 增至 81。348 行逐条一行的零遗漏口径不变。汇编时新列的内容要进最终规格
——它记的是「MVP 剖面当时的判定没错，是形态变了」，**这是本图已踩到四次的坑，规格里应当留痕**，
而不是只留一个改后的批次号。

**二、`REQ-0157` 的身份绑定断裂，与 tag 缺失同类，一并处置。**该条绑定的 commit
`420c96c2f961aaffa42a5443fa42e6585b1c993f` 在 `8005-mes-ingest` 对象库中**不存在**（2026-09-02
拆仓时历史被重写），且基线记的路径 `mes/queries/mes-task-union/` 与实际路径
`queries/mes-task-union/` 不一致（该仓 README 里还有第三个路径 `service/queries/mes-task-union/`）。
**query.sql 的 SHA-256 仍完全吻合**（`54a140ad…39ae`），所以内容身份未断，断的是 commit 指针与路径。
这与本票已悬的 `requirements-baseline-v1.0.0` tag 缺失是同一类问题：**基线里的身份绑定引用了会被
历史重写抹掉的东西**。汇编前须一并有结论。

**三、可能出现第三种「不做」形态。**map Notes 现在只认「延后」与「本图范围外」两种。票 13 撞见的
`REQ-0298` 与票 14 `RouteGraphSnapshot` 的正面冲突（已开票 15）**可能产生第三种：需要需求变更**。
若票 15 判甲档或丁档，本票的表述层要接住它，且不得把它表述为需求作废或废弃。

详见 [票 13 决议](13-answer.md) 与 [票 15](15-resolve-req-0298-conflict-with-route-graph-snapshot.md)。
