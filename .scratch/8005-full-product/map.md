# 8005 完整产品实施范围与顺序路线图

Label: wayfinder:map

## Destination

形成一份经用户批准的《8005 完整产品实施范围与顺序规格》，覆盖 `requirements/baselines/current-requirements-v1.0.0.md` 全部 348 条需求，逐条给出实施批次归属与批次间依赖，并固定全量切片家族编号、协议 v2 的冻结面与完整产品的验收边界与证据要求。本图只产决策，不产代码。

## Notes

- **只做决策，不产代码。**批次 1 及其后的实施属于随后的实施图，不属于本图。本图不写代码、不改 v1.0.0 基线、不动 `requirements/current-baseline.md` 指针、不替 Kun Wang 决定车载端实现、不代签任何 protocol release。
- **批准人是用户一人。**范围与顺序、切片家族、协议 v2 冻结面与验收边界全部由用户单独批准。协议 v2 相关票据落定并推送后按 `8005-agv-protocol` 的 notify-after-change 规则开 issue @`SocialKKKK` 通知，通知不是前置门禁，本图不因等待对方评审而挂起。MVP 那张图留下的「车载端开发同事未评审、未批准」在本图同样成立，须在规格中如实记录，不得表述为已获双方同意。
- **零遗漏口径是全部 348 条。**最终规格必须对 348 条逐条给出一行，MVP 已覆盖的 186 条（直接必须 85 + 交互／安全依赖 101）标注为批次 0 并指向 MVP 剖面证据，真正新排期的是 162 条。不允许「只排非 MVP 部分」的口径。
- **MVP 是软前置。**本图假定 WIRE_TO_GATE MVP 会通过现场验收，批次 0 直接引用已批准的 MVP 规格；MVP 的收尾、P0～P7 试运行进度与 `8005-agv-control-server/docs/defects/` 当前 5 个未闭合缺陷不进入本图票据，也不构成本图票据的阻塞。
- **MesIngest 的 100 条不在本图。**见 Out of scope。本图只在最终规格中保留一行指针，不核对、不重排、不为其分配批次。
- **人员认证与账号本期完全不建（2026-09-04 用户定案，票 05 的 Q1，取「甲档」）。**此前的 standing preference 已由票 05 落定为决议，但**它当初的事实前提是错的，纠正记录在此**：原先写的是「权限**骨架已在批次 0**，砍掉可调条目等于账号与密码继续靠部署时预置配置管理」。**票 05 查实：没有账号也没有密码可以「继续」。**控制服务端 `src/` 里 `SystemAdministrator`／`MaintenanceAdministrator`／`AdministratorAccount`／`PasswordHash`／`LoginSession` 全部零命中，28 张持久化表既无账号表也无管理员审计表；唯一的管理员身份是消息 payload 里的 `administratorRole` 字符串（`OnboardRecoveryCoordinator.cs:251` 只校验它属两值枚举），唯一的认证是同文件 `:1006` 的 `RecoveryProofAccepted` 对一个环境变量做定时安全比较——**全场共用一个密钥、不识别人、也不区分那两个角色**，持有密钥者可以自称 `SYSTEM_ADMINISTRATOR`。车载端同样零命中 `Login`／`Password`／`Account`／`工号`，`operatorId` 来自环境变量、`AdministratorRole` 硬编码为 `MAINTENANCE_ADMINISTRATOR`。**这是本图第八次「能力藏在批次 0」。**曾被列为「骨架已在批次 0、本图不得重开」的那批条目（`REQ-0250` 三种访问身份、`REQ-0251` 两级管理员、`REQ-0252` 一人一号单会话、`REQ-0253` 异常处置权限随角色授予、`REQ-0255` 管理员操作不可改写审计、`REQ-0276` 基础密码格式、`REQ-0278`／`REQ-0279` 会话不超时）**共 8 条已由票 05 移出批次 0，改判进新簇 `FP-C10 人员认证与账号`，整簇延后**。**另纠正一处陈旧**：本条此前把 `REQ-0336` 也列进这份骨架清单，但票 13 早已把它改判进 `FP-C9b`，票 05 判它延后。`REQ-0226`／`REQ-0235` 不在这 8 条内，但 `MaintenanceDoorAccessMode` 两端全零命中，票 05 已把它作为可能的第九次线索交票 09。此前列的 10 条可调条目——`REQ-0236`、`REQ-0254`、`REQ-0311`、`REQ-0256`、`REQ-0273`、`REQ-0274`、`REQ-0275`、`REQ-0277`、`REQ-0280`、`REQ-0339`——票 05 的处置不是一刀切：`REQ-0236`／`REQ-0256`／`REQ-0273`／`REQ-0274`／`REQ-0275`／`REQ-0277`／`REQ-0280` 七条随 `FP-C10` 延后；`REQ-0311` 退化实施（恢复入口不在看板、不在车载端，只在受控运维流程里）；**`REQ-0254` 与 `REQ-0339` 判「部分实施」而不是砍**。**票 05 用事实解掉了三条跨簇耦合中的两条，第三条降级。**原先记的是：`REQ-0311`（恢复权限只属系统管理员）是 AGV 归档恢复的安全前提，`REQ-0339`（敏感生效动作要求新鲜二次认证）是配置激活与回滚（`REQ-0266`／`REQ-0346`）的安全前提——留其一砍其二会造成有审计无二次确认。**票 04 查出第三条跨簇耦合，方向与前两条不同**：`REQ-0254`（票据 62 的专门权限并入管理员角色）是「充不上现场确认」「人工清桩确认」「充电桩恢复确认」三个 `FP-C1` 动作的**唯一有效身份模型载体**——`REQ-0176`／`REQ-0179` 写的 R-11／R-13 已被 `REQ-0254` 明文退役，`REQ-0270`（批次 0）另外明文「不恢复 R-12/R-13 为系统授权角色」，批次 0 的三身份骨架里没有 R-11，而协议 v1 的 `administratorRole` 枚举已实装 `REQ-0254` 的模型。**砍掉它的后果不对称**：`REQ-0176` 写的是「R-11 **或等效维护权限人员**」有退路，`REQ-0179` 写的是「初始授予 R-11 与 R-13」**没有退路**。前两条是「另一簇能力的安全前提」，这一条是「另一簇能力的唯一身份模型」。**票 05 的处置**：`REQ-0254` **不需要砍——它本来就在**，协议 v1 的 `administratorRole` 两值枚举已冻结、服务端已在校验，判部分实施（枚举模型已实装，身份自报那一半随 `FP-C10` 延后），`FP-C1` 三个人工动作不会失去身份模型载体。`REQ-0339` 拆两半：「明确确认影响预览」本期实施，「使用当前密码新鲜二次认证」随 `FP-C10` 延后；**明确排除用共享密钥冒充二次认证**。`FP-C9b` 因此保留在本期（8 条实施、`REQ-0336` 延后），`REQ-0311` 随 `FP-C5` 退化实施。逐条见 [票 05 决议](issues/05-answer.md) 的 Q1～Q5。
- **无认证前提下，人机界面上只允许 fail-safe 方向的动作（2026-09-04，票 05 的 Q6）。**暂停、收紧、阻断可以长在界面上；恢复、激活、回滚、投运一律走受控预置配置 + 版本化审计。理由是前者最坏后果是运力下降，后者最坏后果是绕过安全门禁——而甲档下没有任何东西能证明操作的是谁。与之配套的判断标准是**「变更时机能不能等停机窗口」，不是「频率高不高」**：变更时机由现场事实触发、不能等停机窗口的才需要交互入口，其余全部受控预置。**本期唯一的人机界面是 `REQ-0268` 的看板**（只读 + 少数 fail-safe 写入口），它也是控制服务端历史上第一个界面——现存全部 HTTP 端点只有 5 个，`ControlServer.sln` 11 个工程里没有任何 WPF／Blazor／Razor。这条与票 04 给充电桩名册定的形态同构（名册走预置配置、运行期的暂停与恢复分配不是名册变更），是本图第二个实例。
- **「不做」在本图只有两种合法形态：延后，或本图范围外。**v1.0.0 的 348 条 Lifecycle 全部 `active`，本图不改基线，因此任何范围收窄都不得表述为需求作废或废弃。票 09 负责区分二者，票 10 负责在规格中如实表述。**票 03 又引入了一个必须与「不做」严格区分的形态：`证据受限实施`**——门禁与代码路径完整实现、条目要排批次要写代码要有验收证据，只是判据依赖的外部证据当前不可得而恒走需求自身规定的保守分支。**它是「已实施」不是「不做」**，票 09 与票 10 不得把它归入延后或范围外。定义见 `CONTEXT.md`。**注意该形态目前在本图没有任何实例。**它最初的三个实例（`REQ-0196`、`REQ-0198` 与 `REQ-0197` 换序半）是票 03 依「RIoT 查不到站→站成本」判的，该前提已被推翻，票 14 依 Round 43 实测把三条改判为**完整实现**，`证据受限实施` 在本簇**清零**。词条保留是因为后续票据仍可能产生新实例；**但票 09 与票 10 不得再把这三条按该形态排期或表述**。
- **顺序粒度到批次与切片编号，不到逐切片证据清单。**本图产出 N 个实施批次、批次间依赖，以及全量切片家族的编号体系与它同 `W2G-IS-00`～`07` 的关系（沿用／替换／并存）。逐切片的验收证据清单属于实施图。
- **重构与增量的判据、划分与顺序已由票 02 定案，后续票据在其内工作，不各自重做一次判断。**判据是**推翻有代码或契约位置的现行架构不变量之一**（I1 车辆占用必由 Demand 引起／I2 单车单 journey／I3 单 Demand 两段固定行程／I4 站点无独占／I5 车载对 Demand 只读），对应判据 B1～B5。62 条判为 **16 条重构、46 条增量**。**票 13 补充识别了第六条不变量 I6：站点任务类型准入必在装货腿检查并冻结**（`WireToGateStore.cs:795-800` 明文 `"Only LOAD may carry a complete station/task admission identity."`，而准入表键在 AREA 机台站上）——`STAGING_TO_WIRE` 方向反转后 AREA 站是卸货端，故推翻 I6，是重构类。`CONTEXT.md` 的措辞是「已识别五条」，不是穷举封闭，故这是补充识别而非推翻票 02；**这是本图第二次发现该判据集不完整**（第一次是票 02 自己查出 B2 无需求条目载体）。**票 05 补充识别了第七条不变量 I7：车载持有并声明自身仓位配置，服务端只记录不裁决**——`[车载] Configuration.cs:270-272` 两个硬编码默认值 `SlotModelVersion="eight-slot-v1"` 与 `ActiveSlotConfigurationVersion="eight-slot-modbus-v1"`，由 `WireToGateSessionClient.cs:402-403` 自报给服务端，而服务端 `SlotTemplate`／`ActiveSlotConfiguration`／`SlotConfigurationVerification`／`VehicleConfigurationMaintenance` 等八个标识符全部零命中。`REQ-0258`（服务端是唯一权威维护入口）与现状**方向相反**，故推翻 I7，是重构类。**这使 `FP-C7` 不再是纯增量簇**，票 05 票据正文那句「本票 21 条全部无不变量依赖，可以从第一天并行」在 8 条地基改判进来后不再成立。**票 13 另使待排期条目由 62 条增至 81 条**（19 条移出批次 0），**票 12 再增至 82 条**（`REQ-0290` 移出批次 0），**票 04 再增至 84 条**（`REQ-0281`／`REQ-0282` 移出批次 0），**票 05 再增至 102 条**（18 条移出批次 0：8 条权限骨架、2 条看板显示规则、8 条仓位配置权威地基）。**票 12 补上了 B1 缺失的需求载体**：票 02 判 B1 时用的 `REQ-0292`／`REQ-0294` 只说「空闲返回要取得占有」，没说占有槽本身是什么、有几种用途、谁压过谁——那三条约束由 `REQ-0290` 承载。判定结论不变，**这是本图第三次发现票 02 的判据集或其载体不完整**。**全新能力不等于重构**——充电 17 条里 13 条是建在新原语之上的增量。排序不用「重构整体先于增量」，用「每条增量不早于它所依赖的那条不变量的推翻」；重构内部顺序固定为 **`B2 → (B1+B4 一起) → B3 → B5`**。两条纠正：**充电依赖的是等待点而非多车调度**（`REQ-0178`），**348 条里没有任何一条要求「系统支持多辆车」**，B2 无需求条目载体、票 09 须单列。逐条归属表见 [票 02 决议](issues/02-answer.md)。
- **完整产品执行全部六类 MES 运输任务，不止 `WIRE_TO_GATE` 一类（2026-09-03 用户决定，票 03 的 Q9）。**基线四条（`REQ-0003`／`REQ-0150`／`REQ-0181`／`REQ-0184`）都在说六类，`TransportDemandKey` 即 `TASK_TYPE + SUBLOT`；但它们在剖面里全标批次 0，`ControlServerImpact` 却写着「执行 **WIRE_TO_GATE** 字段、站点和容量准入」——**只覆盖六分之一**，而 62 条里没有任何一簇承载其余五类。**票 13 已定案**：六类真名查实为 `DIE_TO_WIRE_STAGING`、`DIE_TO_OVEN`、`WIRE_TO_GATE`、`WIRE_TO_OPTICAL`、`STAGING_TO_WIRE`、`WIRE_TO_NITROGEN`（工厂 IT SQL 六个 `UNION ALL` 分支的硬编码字面量）。**纠正本节此前的一处措辞**：`WIRE_TO_GATE` 不是「MVP 阶段的命名」，它就是 MES 的真实 `TASK_TYPE` 之一；基线只是没写它的名字（只点了方向反的 `STAGING_TO_WIRE`）。自造的是 `profileId` 的 `WIRE_TO_GATE_MVP`。**摄取侧无事可做**——MesIngest 已全量摄取六类、无任何 `TASK_TYPE` 过滤，本簇是「只加执行」。承载执行的条目**不在 62 条里**，而是 348 行里被误判批次 0 的 17 条公共站点绑定条目，票 13 已改判。
- **「能力藏在批次 0 条目里、实现却是退化形态」已出现八次，后续票据须假定还有第九次。**前五次分别是：多车（票 02，「348 条里无一条要求多辆车」）、`REQ-0190`／`REQ-0194` 的车辆准入（票 03，标识符全仓零命中）、六类任务（票 03 的 Q9）、**票 13 查出的公共站点绑定治理整簇 17 条**，以及**票 12 查出的 `REQ-0290`**（`VehiclePurposeClaim` 原子争用车辆，B1 的定义条款）。共同点是 MVP 剖面的判定**在单一形态下没错**，但完整产品语境下那个形态是退化的。**第四次比前三次都严重**：前三次至少有一个单一形态的实现，这次是整簇零实现——`PublicStationFunction`／`FixedTaskStation`／`PublicStationBindingSetVersion`／`TaskTypePublicStationRuleVersion`／`MapPublicStationRequirementSet`／`PublicStationBindingHold` 六个标识符在控制服务端 `src/` **全部零命中**，被一个 `gateStationId` 标量在事实上顶替。**逐条复核，不要抽查**；票 09 尤其要注意。**第五次由票 12 查出，且它不在票 13 预告的线索里**：`REQ-0290` 明文规定「所有用途通过唯一 `VehiclePurposeClaim` 原子争用车辆」，规定了搬运／充电／清桩维护／空闲返回四种用途共用一个占有槽与三级优先级；MVP 剖面只读到它后半句的 `MandatoryChargeEntryThreshold`，`ControlServerImpact` 写成「以电量门禁阻断新任务」。单一用途下它退化为「一个 Demand 一个租约」，`VehiclePurposeClaim` 在 `src/` 零命中。**`REQ-0290` 与 `REQ-0147` 在本图 01～15 号票据中此前从未被提及过一次**——票 02 判 B1 用的是**使用**该原语的 `REQ-0292`／`REQ-0294`，定义它的那条一直在批次 0 里。**票 13 预告的线索仍未验证**：`TransportDemandSuppression`、`LoadPreparationAlert`、`UnassignedDemandBacklog`、`AreaEqpUniquenessMonitor`、`DispatchZoneAreaAssignment`、`EnRoutePickupDeliveryDelay` 六个标识符同样零命中，全部指向批次 0 条目引用的配置与告警面（见 [票 13 决议](issues/13-answer.md) 第六节）；第五次出现在别处**不构成放松逐条复核的理由**。**第六、七次由票 04 查出，而它给出了这一形态的第一个系统性检出手段**：按 MVP 剖面的 `ControlServerImpact` 分组——串为「以电量门禁阻断新任务并提示人工充电。」的条目**恰好 3 条**（`REQ-0281`、`REQ-0282`、`REQ-0290`，三条的车载与协议影响列也逐字相同），票 12 已改判其一，票 04 把另两条一并改判进 `FP-C1`。`REQ-0281`（三个电量阈值与硬关系）的批次 0 判定只在「单一固定行程 + 无充电周期」下成立，代码里只有一个标量 `JourneyRuntime:minimumBatteryPercent`；`REQ-0282`（`ChargingPolicyVersion` 受批准按周期冻结）只在「一个参数一台车、部署配置即已批准版本」下成立。**手段可复用：同一个影响串下的条目其批次 0 判定成立于同一形态，一条被推翻则同串全部要重判**，票 09 逐条复核 164 条时直接用。**另注两条候选改判本票未动**：`REQ-0148`（命令面零实现，属 `FP-C2` 之外的跨簇面）与 `REQ-0208`（比当前电量而非任务后余量，属 `FP-C2`），证据在 [票 04 决议](issues/04-answer.md) 第三节，由票 09 判。**第八次由票 05 查出，是迄今规模最大的一次，而且它是第一次「主动用手段搜、一次搜出三簇」**：按同一手段查两个影响串——「提供身份、权限、审计和版本化仓位配置权威。」18 条与「提供当前状态、权限裁决和审计留存。」13 条，合计 31 条里 20 条标批次 0，**票 05 改判 18 条**。三簇各自的退化形态是：其一，权限骨架 8 条成立于「单机、单共享密钥、身份由客户端自报」（两端均无账号、密码、登录会话，见上面「人员认证与账号本期完全不建」一条）；其二，`REQ-0269`／`REQ-0270` 两条看板显示规则成立于「看板存在」，而看板与其数据源均零实现、协议里连 `Alarm`／`Alert` 都零命中；其三，仓位配置权威 8 条（`REQ-0257`／`REQ-0258`／`REQ-0259`／`REQ-0262`／`REQ-0263`／`REQ-0264`／`REQ-0265`／`REQ-0267`）成立于「车载持有并声明自身仓位配置」，即新识别的不变量 I7。**手段的可复用性再次得到验证，而且这次是三串同时命中**，票 09 逐条复核时继续按影响串分组。**票 05 另交一条未查线索**：`MaintenanceDoorAccessMode`（`REQ-0226`～`REQ-0231`）在服务端与车载端两端全零命中，它在影响串「掌握任务、仓位集和装卸业务承诺。」下，该串 16 条里 13 条标 `FP-B0`，可能是第九次。
- **事实核查要查到能力的来源，不能只查一个接口的入参就下断言（2026-09-03 教训）。**票 03 曾据「`getRouteCostsBy` 入参没有起点字段」判定「RIoT 查不到站到站成本」，并据此把三条需求判为恒 fail-closed；用户一句质疑即推翻——`GET /api/imap/v1/mapInfo/edges/{mapId}` 早就给全图边表含 `Edge{snode, enode, cost}`。同一次复核还查出：原判「全量 schema 扫 `Eta` 零命中」只扫了 `task.json`，`order.json` 有 `eta`／`arriveTime`。**后续票据下「某能力不存在」这类断言前，须穷举该能力可能的来源面**（本例是 imap 路网、order 订单侧、task 路由三处），而不是只看最像的那一个接口。子 agent 的报告同样适用这条——它给的是它查过的范围内的事实，不是全域的。**票 04 撞了第二次，形态不同**：它查了 vendored Facade 层的完整符号表（39 个 `*Async` 方法，`Charg*` 零命中）就断言「RIoT 没有充电接口」，而能力在 Generated 层与 OpenAPI spec 里（`POST /api/task/v1/order/charge/{vehicleKey}` 与 6 条 `chargeConfig`）。**教训因此收窄为一句：穷举「来源面」时，「面」包括抽象层次，不只是命名空间。**
- **协议 v2 已一次冻结（2026-09-04，票 06）。**身份三元组定为 **`ProtocolVersion` 2 ＋ `profileId` `AGV_FULL_PRODUCT` ＋ release `1.0.0`**；消息面 **54 条沿用 ＋ 5 条改 payload ＋ 9 条新增 = 63 条**，denylist 11 条不动；错误码 **43 → 45**（`appendOnly` 不破）；向量 **20 → 31**，命名沿用 `CV-<场景名>` 不带编号，`productAssertions` 由 W2G-IS-01 的特例推广为全量必填；**不引入版本协商**，沿用精确 `ProtocolVersion` ＋ 精确 `ProtocolReleaseIdentity`，两端同一个 CD 包同窗口替换、窗口内旧版车被带期望身份地拒绝。**`ProtocolVersion` 2 ＋ release major 不是选择**——`docs/release-governance.md` 第 9 条明写这十一类变更强制两者同升。**本票产出「设计面已冻结、可据以实施」，不产出「可发布」**；实施图还欠七件事才能切 `ProtocolRelease`，其中最大单项是**重生成约 1500 个 invalid 样例而仓库里没有生成器**。逐条见 [票 06 决议](issues/06-answer.md)。
- **完整产品切片家族已定（2026-09-04，票 07）：替换旧家族，取 `FP-IS-NN`，16 个切片。**`FP-IS-00`～`07` 与 `W2G-IS-00`～`07` **一一对应**，是 v2 下的重证而**不是可沿用的通过结论**；`FP-IS-08`～`15` 是新能力；票 06 的 31 条向量全部绑定、无一落空。**旧家族的证据靠 `ProtocolReleaseIdentity` 引用，不靠编号**——`gate-result.json` 记着 `protocolTag` ＋ `protocolRepositoryCommit` ＋ 三个哈希。门禁 `G1 / CONTROL_SERVER_G2 / ONBOARD_HMI_G2 / G3` **不增不减，现场验收不进门禁模型**（`gates` 是每切片相同的 `const`，而现场前置逐簇不同——桩没装、等待点没测绘）。**批次与业务簇都不进 id**：批次是循环依赖（批次由票 09 定而票 09 被票 07 阻塞），业务簇是稳定性（本图已改判簇归属 5 次，而 id 活在 161 个测试 trait 与不可变证据目录里）；业务面由 `definition.scope` 承担。**切片只覆盖控制服务端↔车载端的双端能力**——单端工程（`FP-C5`、`FP-C9b`、看板本体、`RouteGraphSnapshot` 引擎）与**多车并发**都不立切片，后者是因为保形向量是单会话格式、`steps[]` 无车辆维度，**结构上不可表达**。`definition` 由可选改全部必填，`demandRepresentation` 三个 `const` 重写为 `authorityModel`（`onboardMode` 单值改数组，四个取值）。逐条见 [票 07 决议](issues/07-answer.md)。
- **票 06 判为「v2 落地最大单项」的样例生成器实际存在（2026-09-04，票 07 查实）。**票 06 的 1.6 写「约 1500 个 invalid 样例……仓库里没有生成器」，它查的是 `8005-agv-protocol`；生成器在**本仓** `.scratch/wire-to-gate-ai-implementation-kit/tools/generate-protocol-candidate.mjs`，**595 行**，写出整棵树（schema、全部 valid/invalid 样例、向量、`index.json`、runner 两个 schema、manifest、docs，**连 `g1-validate.mjs` 本身**），顶层四个常量 `BASE_ID`／`candidateVersion`／`profileId`／`protocolVersion` **正是票 06 的 3.1 要改的那四项**，切片家族在里面是一张 10 行的表。**用户 2026-09-04 决定复活并升级它**，工作量重估交票 09——真实提前量不是 1500 个文件，而是把 0.1.1 手工补的东西（`schemas/governance/` 三个文件、attestation 外置逻辑、`x-sorted` 语义规则、`definition` 块、`productAssertions`）补回生成器。**注意它未被实际执行过**（Bash 工具里没有 `node`），票 09 重估前应先在一次性目录试跑。
- **现场物理前提已具备，规模由票 11 定案（2026-09-03 用户确认）：3 台 AGV、3 个 8005 项目独占充电桩、n 个等待点，全部在唯一作业图 `mapId` 25（`老厂前线new`）上。**因此多车调度与自动充电两簇可以排进带现场验收出口条件的批次，不必降级为「仅双 Fake 联调」。**用户明确要求车数、桩数、等待点数均可配置**——核对基线，三者已分别由 `REQ-0259`／`REQ-0190`／`REQ-0194`、`REQ-0171`、`REQ-0289` 覆盖，**不派生新需求**。**但「需求已覆盖」不等于「已实施」**（票 03 查实）：`VehicleTaskTypeAdmission`、`DispatchZoneVehicleAdmission`、`DispatchZoneVehiclePreference` 三个标识符在 `8005-agv-control-server` **全仓零命中**，实际存在的只有 `StationTaskTypeAdmission`（**站点维度、无车辆维度**，`taskType` 在 `JourneyRuntimeEngine.cs:75` 硬编码）；车辆侧只有 `appsettings.json` 里那台车的两个标量白名单，且 `REQ-0194` 的方向是反的（配置是车→区，需求要区→车集合）。`REQ-0190`／`REQ-0194` 标着批次 0 是对的（单车下语义确实被满足），但**多车化是真实工程量**，票 09 不得因此跳过排期。N 车配置形态已由票 03 定为 `vehicles` 数组。注意 `remote-ops/onboard-hmi/config/` 目前只有 `agv01` 的现场配置，新增车辆的现场值填写属实施图。**另注意 3 车 3 桩使充电排队在现场自然不会触发**，`REQ-0172`／`REQ-0173` 的验收证据怎么取由票 08 判。**票 04 从用户处取得两条改变形态的现场事实（2026-09-04）**：其一，**三个充电桩物理上还没安装**，map 25 当前没有充电桩站点，故「名册为空」是投运第一天的真实状态而非边角情况，`FP-C1` 的验收出口必须含「桩安装完成 + 名册录入」这个现场前置；其二，**8005 自建充电机制下充电桩是普通站点**——用 RIoT 本体充电机制才需要把站点设成充电桩 `type`，自建机制（普通订单 + 移动与充电动作）用普通站点即可，**因此装完后哪些 id 是充电桩只能问用户，不能查 `type` 找到**。这比「现场尚未测绘」更强：充电桩身份**永远**不可从 RIoT 观测推导，`ProjectExclusiveChargingStationRegistry` 是唯一来源且不可交叉校验。同一条也已确认**不会有其它项目的车占用 8005 的充电桩**，故站点占位判据只需覆盖本项目车辆。
- **日历约束：用户要求一周内完成「规格 + 批次 1 落地」。**本图目标在 2～3 天内走完全部票据，把余下日历留给实施图。顺序按依赖与风险排，不按日历倒推；宁可单票更重，不要更多轮次。
- 跨端决策须遵守 `8005-agv-program/CONTEXT.md` 与 `docs/adr/cross/` 中已接受的权威边界，特别是 NDJSON ProtocolEnvelope、精确 ProtocolVersion、消息交付类别、RecoveryHandshake、车载 IO 独占权威、服务端任务/仓位集决策权与车载物理执行权，以及 ADR-0056 的 .NET 工具链基线。
- `8005-agv-onboard-hmi` 与 `slots-simulator` 对 agent 只读：可读、可开 issue 与 PR，不得改内容。本图不受此限，因为它不产代码。
- 需要产品判断或最终批准的票据使用 `grilling` 与 `domain-modeling`；任何新领域词一旦定案立即写回 `8005-agv-program/CONTEXT.md`。
- **设计细节不再交给用户逐条确认（2026-09-04 用户指示）。**用户原话是「我觉得现在 wayfinder 让我确认过多的设计细节了，我不想关心设计细节」。**该问的**：范围收窄或放开、工程代价量级、不可逆的方向选择、跨仓库或跨人的约束。**不该问、由本会话定完写进决议的**：命名、请求/应答配对形态、enum 取值、dedup 键、字段放哪条消息、快照还是事件流、命名规则要不要带编号。判据是一句自检：**这个问题选错了，代价是「改一次 schema」还是「返工一个簇／多花一周／得罪协作方」**——前者自己定，后者才问。自定的决策必须在决议里**显式标注为「本票自行定案、未经用户逐条确认」**并写明理由，票 06 的第七节是第一个范例。
- **`8005-agv-onboard-hmi` 与 `slots-simulator` 今后由我方负责开发（2026-09-04 用户告知）。****这是将来的开发归属变更，不是现在的写权限变更**——工作区 `CLAUDE.md` 的写权限表在用户明确改它之前照旧，那两个仓对 agent 仍然只读，本图不得据此往那两个仓写任何东西。**交接的是那两个仓的开发，不是 `8005-agv-protocol` 的所有权**，Kun Wang 仍是协议共同维护者。两条下游后果：其一，`docs/release-governance.md` 要两名不同产品负责人签 attestation、G1 第 27 行实打实校验 `new Set(approvals.map(a=>a.ownerId)).size===2`、AI 与 CI 不能批准，**两端都归一边写之后「第二人是谁」成了 v2 打 tag 的硬前提**，票 06 已交票 08；其二，`CONTROL_SERVER_G2`／`ONBOARD_HMI_G2` 的切片分工机制不变，但它不再是两个人的分界，票 08 原本的「两人分工」前提要重写。**票 06 的判断：交接完成前 Kun Wang 就是那第二名产品负责人**，本图不改 `release-governance.md`——改它等于自我豁免 Notes 里「不代签任何 protocol release」那条。
- **本图的全部工作在 worktree 内进行（2026-09-03 用户指示）。**分支 `wayfinder/8005-full-product`，路径 `8005-agv-program/.claude/worktrees/full-product-map`，基于 `main` 的 `10ac49b4`。后续会话先切进去再动手，不在 `main` 的工作树上改地图产物或证据。该目录已进 `8005-agv-program/.gitignore`，`main` 侧看不到它。
- **本图新增第三种「不做」之外的合法形态：`需求变更待批`（2026-09-04，票 15）。**此前只有「延后」与「本图范围外」两种（另有票 03 的 `证据受限实施`，那是「已实施」的子形态）。第三种的定义是：**条目要实施，而且要按与基线文本不同的形态实施**；本图输出变更提案，提案在用户批准前不改基线，实施图据修订后的文本执行。它既不是延后（工作照做）、也不是范围外（在 Destination 之内）、更不是 `证据受限实施`（后者恒走需求自身规定的保守分支，本形态则是**偏离**需求文本）。**目前有两个实例，同属提案 `CP-0001`：`REQ-0298`（重写）与 `REQ-0146`（增列四个 `imap` 端点）。**票 09 排批次时按正常条目排期并标记它挂哪个提案，票 10 汇编时必须同时呈现基线原文、提案修订文、以及「提案未批准前实施图不得据修订文开工」这条约束。
- **RIoT 调用白名单已扩四个 `imap` 只读端点，且产品代码必须走具名 Facade（2026-09-04 用户批准，票 15）。**`GET /api/imap/v1/mapInfo/edges/{mapId}`、`.../mapInfo/stations/{mapId}`、`GET /api/imap/v1/mapResource/removedEdge/{mapId}`、`GET /api/imap/v1/mapEdgeGroup/all` 是票 14 的 `RouteGraphSnapshot` 引擎的四个数据源，**票 37 的获批清单里一个 `imap` 端点都没有**（9 个具名 endpoint 全在 `/api/version/` 与 `/api/task/` 下），vendored `RIoT.Sdk.Facade` 的 38 个 `*Async` 方法里也没有任何边表方法。票 37 明写「白名单到此封闭……其它未具名调用全部不授权」「生产业务代码只可经具名 Facade 调用，**默认拒绝 RawEscape 和任意 URL**」「扩大白名单……必须再次取得用户明确批准」，故这是一次正式扩权。**落地形态是在 `riot-sdk` 里新增具名 Facade 方法**（自定义反序列化落在 SDK 内部，snake_case、`s_node`／`e_node`、站点侧键名字面带点），产品代码路径**不得**使用 `.Raw`。**这给引擎添了一段票 14 未计的真实提前量：`riot-sdk` 要发一版、控制服务端要升 vendored 包**，票 09 计入。用户批准时的描述是「边表与站点位姿读取」，四个端点是本票的精确化，非用户逐个点名。
- **建单前置 `RouteCost` 门禁保留，票 14 的「一律退出」被票 15 收窄。**票 14 定 `getRouteCostsBy` 与 `queryNearEnd` 一律退出产品代码路径，**但它全文未提及五条明文要求建单前置 RouteCost 的基线条目**：`REQ-0147`（这一条还是票 37 白名单给建单授权时附的前置条件）、`REQ-0293`、`REQ-0302`、`REQ-0305`、`REQ-0345`。收窄后的分工不是折衷，是**两个 API 各自能力的自然归属**：选车阶段问「站 A 到站 B」，`getRouteCostsBy` 起点恒为车当前位置、答不了，用自建图；建单前置问「这台车此刻能不能到那个站」，**那正好就是 `getRouteCostsBy` 唯一能答的问题**。`queryNearEnd`／`queryNearestStart` 的全面退出不变。保留的净新增工程量接近零（该门禁在 `src/` 里 `RouteCost` **零命中**，无论证据源是谁都要新写），删除的代价是五条基线同时被违反。**票 14 担心的量纲问题不会回来**——建单前置只用可达性布尔判定（`costs:-1` + `"vehicle route to station unreachable"`），不用数值。两个证据源分歧时**阻断建单并告警**（反方向结构上不会发生：图判不可达的车进不了候选）。
- **需求正文冲突的复核取「定向扫描」，口径是 A∪B 面 120 条而不是 348 条（2026-09-04 用户定，票 15）。**手段不是从需求侧逐条读，而是**从本图已定决议侧提取受约束的名词与动作，扫 348 条正文，只精读命中条目**。本票实测两轮（路网/可达性面 19 命中、白名单/Facade 面 15 命中）**从一条已知冲突查出三组**。交票 09 的口径：**A 面**禁止性措辞 107 条（`REQ-0298` 那一类「需求禁止了我们要做的事」）、**B 面**边界沿用措辞 25 条（「需求要求继续用我们已经不用的东西」，**票 15 新查出的第二组冲突整个来自这个面，而此前全部票据都只在讨论 A 面**）、**C 面**唯一性与封闭措辞 67 条按需、**D 面**「留给后续设计」**全基线只有 2 条**（`REQ-0199`、`REQ-0309`）——**任何「这一层留给后续设计所以不冲突」的论证都必须落到这两条之一，落不到就走不通**。348 条正文合计 51,793 字符。**「剖面判定错误」那一面（票 12 的 `REQ-0290` 类）不进本手段**，它已由影响串分组 + 第 11 列 `Batch0FormNote` 覆盖，两个面的检出手段不同源。
- **实施批次已定（2026-09-04，票 09）：九个批次 0～8 ＋ 一个延后集，348 行 `Batch` 列定稿。**批次 0 **117**、批次 2～8 共 **109**、延后 **22**、Out of scope **100**。**批次 1 在 348 行里不产生任何一行**——协议冻结面是票 06 的产物不是需求条目，与票 02 的 B2 同理，故本票单列**十项无条目载体的工程**（协议 v2 候选生成、三条实现缺陷＋两条架构测试、v2 两端实现、161 个 trait 重打标、B2 多车、`RouteGraphSnapshot` 引擎＋`riot-sdk` 发版、RIoT 订单命令面、白名单架构测试、版本化配置快照与审计、`STAGING_TO_WIRE` 启用）。**批次间依赖是 DAG 不是链**，11 条边每条都归约到一条已推翻的不变量、一条协议前置、一条切片 `prerequisites` 或一条现场前置；严格串行的只有 `2轨B → 5 → 6 → 7` 这条不变量链。**批次 1 = 协议 v2 候选生成与 G1 通过**（用户定），它**不含**两端实现与八切片重证——那三项在批次 2 轨 A，不在一周承诺内。逐条见 [票 09 决议](issues/09-answer.md)。
- **A 面「零命中」有两种相反的含义，判之前先读它要求的是「做」还是「不做」（2026-09-04，票 09）。**`REQ-0165`（「不能仅凭停留时间推定订单已经失效」）是**纯禁止性条目**——不做某事的实现方式就是不写代码，`QueueingStall` 零命中恰恰是**遵守的证据**；而 `REQ-0234`（「自动调用 `OrderHold`」）要求做某事，零命中才是缺口。**把合规当缺陷与把缺陷当合规，是同一个错误的两个方向。**另附两个会造成假阳性的口径坑：`grep -r` 必须带 `--include="*.cs" --exclude-dir=bin --exclude-dir=obj`（否则 vendored DLL 的符号会被当成源码，`OrderHold` 假报 2 次、`DispatchDisable` 假报 19 次）；`grep -rc` 的输出在 Windows 绝对路径下不能用 `awk -F: '{print $2}'` 取计数，盘符里的冒号会把字段切错。
- **样例生成器实测通过，票 06 的「v2 落地最大单项」被推翻（2026-09-04，票 09）。**控制端**没有 node**（不只 Bash 工具没有，`pwsh` 里 `where.exe node` 同样找不到——票 07 把原因记窄了），改在 `vm01`（node v24.20.0）的一次性目录里跑：一次 `node` 调用产出 **1504 个文件**。与协议仓 v0.1.1 做 1516 个文件的全量逐字节比对：**逐字节相同 1493 个**，含 `examples/` 全部 **1395**（54 valid ＋ **1341 invalid**）、全部 57 个 schema、19 个向量的 38 个文件、`errors/error-codes.json` 与 `runner/` 两个 schema；同名不同内容 10、仅仓库有 13、仅生成器有 1。**结论三层**：它能跑；它就是 v0.1.1 examples 树的来源；**零漂移**——0.1.1 手工补的东西一个字节都没落在 `examples/` 或任何一个 schema 上。因此那 1341 个 invalid 样例**不是工作量**，真实提前量是改那个 66 KB `.mjs` 的 **11 项输出点**。`06-answer.md` 不改写（本图不回溯改旧的），但**票 10 汇编时不得引用那个估算**。
- **白名单再扩一个端点，`CP-0001` 修订项二由四个改为五个（2026-09-04 用户批准，票 09）。**新增 `GET /api/imap/v1/mapResource/removedStation/{mapId}`。交接文档记的「运行态被移除的站点数据源未查实」是错的——Round 43 当天就调过（`runs/007-removed-station-map-25.json`，HTTP 200、`result: []`、14 ms），同轮还调了 `removedEdgeDetail/25`。`CONTEXT.md:1765` 的 `RouteGraphSnapshot` 词条本来就写着运行态是「当前被移除的边**与站点**」，**词条一直是对的，缺的是白名单里那个具体端点**。`removedEdgeDetail` **不批**，引擎用不上。另查实 `mapEdgeGroup/all` 是 **20,013 字节、30 ms**，票 14 的 10 分钟 TTL 不必重估；但它返回的三个边组全在 mapId 14 与 9 上，**map 25 一个边组都没有**，故票 14 的「`mapEdgeGroup` 指纹变化即进陈旧态」这个触发器**在当前现场恒不触发**——机制要建、现场证明不了它工作，与票 12 的等待点、票 13 的 `FP-C9a` 同构，**同样不得归入 `证据受限实施`**。
- **`FP-C12 维护开门模式` 整簇延后（2026-09-04 用户定，票 09），这是第三个整簇延后的簇。**`REQ-0225`～`REQ-0230` 六条，`MaintenanceDoor` 与 `SlotStateMismatch` 两端源码全零，是本图**第九次**「能力藏在批次 0」（票 05 交来的线索，确认）。三条理由：`REQ-0226` 要求在车载端**即时重新认证**而甲档下没有认证模型；它的业务目的（光幕漏检导致产品锁在仓内、需要人开仓取出）**已有一条路**——`ExceptionRecoverySession`（160 命中）加强制机械取出（`ForcedMechanical` 13 命中），两者在服务端都真实实现了；做它需要新协议消息，**等于当场推翻票 06 刚做的一次冻结**并把已定为批次 1 的 v2 撑大。**延后不是作废**，Lifecycle 仍 `active`。
- **本图第十、十一次「能力藏在批次 0」，第十一次含安全能力（2026-09-04，票 09）。****第十次是 `TransportDemandSuppression`**——票 13 预告的六个线索标识符里第一个被证实的。`REQ-0155` 明文要求按 `TransportDemandKey` 保存抑制，实际实现是 `OnboardRecoveryCoordinator.cs:828` 把 `CANCELLED_BY_OPERATOR` 写进 `JourneyRuntimeRow.BlockReasonCode`，**挂在 journey 行上**；单车单 journey 下等价，而完整产品下 `REQ-0154` 规定 GONE 后同键再现产生**新 `DemandId`**，新 journey 行**命不中旧的 `BlockReasonCode`**——**被操作员取消过的运输需求会重新派车**。**第十一次是 RIoT 订单命令面**：`OrderHold`／`OrderContinue`／`HangContinue`／`CancelOrder`／`triggerEmergency`／`cancelEmergency`／`EmergencyStop` **两端源码全部零命中**，`VehicleFaultIsolation`／`FaultedVehicle` 同样零。**退化形态是：MVP 的阻断只作用于 8005 自己的派车与仓门，不作用于 RIoT 订单与车辆急停**——进入阻断只会停止建新单、按不住已发出的订单，无法证明停车时不会自动触发急停。**一个同名不同物的陷阱**：服务端 5 处 `DispatchDisable` 全是 `CreateDispatchDisabled`（8005 自己的建单开关），与 `REQ-0167` 说的 RIoT 侧车辆 `DispatchDisable` 是两回事。**用户 2026-09-04 决定就此在 `8005-agv-control-server` 开缺陷单**，交 MVP 侧判断是否影响当前 P0～P7 试运行；本图票据不因此挂起。已开 [`8005-agv-control-server#1`](https://github.com/trytoreachpeak0/8005-agv-control-server/issues/1)，走 issue 而不是直接写 `docs/defects/`——**「这算不算 MVP 的缺陷」该由 MVP 侧判断，不由本图代答**，本图也不往那个分支的工作树写任何东西。
- **`REQ-0309` 里还有第二条 Edge 禁止，但论证不冲突、不进 `CP-0001`（2026-09-04，票 09）。**它写着「缺失能力**不得**以手工录入、**Edge 数据**或未授权接口补洞」，而票 15 只处置了 `REQ-0298`。**论证**：该句主语是「目录」，禁的是目录同步能力有缺失时拿 Edge 顶替目录；`RouteGraphSnapshot` 不顶替目录——目录仍由 `MapStationCatalogSnapshot` 承担，引擎是独立的可达性与成本证据面。**反向收益**：`REQ-0309` 同一句还写着「只能经已批准的 RIoT Map/Station 只读调用面和**具名 Facade** 获取目录」——**这是票 15「产品代码必须走具名 Facade」的第二个需求载体，而且比票 37 那份位置不稳的 scratch 票据稳**。
- **`REQ-0259` 的 IO 完整性门禁一旦启用，三台现有车会同时失去业务就绪（2026-09-04，票 09 新查）。**该条要求「已发布模型中的**全部**仓位必须具有完整并经实际核对的 `SlotIoBinding`，整车才达到 `SlotConfigurationReadiness`，在此之前**整车不得取得业务就绪**，管理员也不能强制放行」；而 I7 的现状是车载自报配置，服务端八个相关标识符全部零命中。**排期上必须把「三台车的 IO 录入与逐仓现场核对」与门禁启用放在同一批次内**（批次 3），且核对是现场动作——`REQ-0263` 要求逐仓确认开锁输出作用于正确物理仓门，**不允许抽样**。

## Decisions so far

<!-- 已解决票据才在此保留一行摘要；详细答案只存在票据中。 -->

- [建立全量 348 条实施剖面底稿](issues/01-build-full-product-implementation-profile-draft.md) — 348 行零遗漏底稿已落 `evidence/full-product-implementation-profile-draft.tsv`（SHA-256 `15892bb4…4d9c`），按来源票据分 13 簇并四项交叉核验通过；**跨地图在 348 条里无任何需求支撑、且 `REQ-0193` 明文禁止跨 Map 运输**，`REQ-0287`／`REQ-0288`／`REQ-0326`／`REQ-0339` 四条的既有归簇被纠正。答案见 [票 01 决议](issues/01-answer.md)。
- [决定跨 Map 运输的去留，以及它对 v1.0.0 基线的影响](issues/11-decide-cross-map-transport-scope-and-baseline-implication.md) — **跨 Map 运输判为本图 Out of scope，Destination 不变，基线不动**；现场经用户确认为单图作业（`mapId` 25，3 车 3 桩 n 等待点全在该图），`REQ-0193` 是对现实的准确描述而非限制，**「单 Map→跨地图」作为第三大重构的前提正式作废，重构只剩两项**；`REQ-0191`／`REQ-0192`／`REQ-0193` 在 348 行里写「批次 0 已实施，本图未放宽」。查出跨图约束只覆盖运输、不覆盖充电与等待点（当前不构成风险），并厘清「跨地图」一词裹着的三个不同概念。答案见 [票 11 决议](issues/11-answer.md)，现场事实证据见 [`evidence/map-topology-observed-facts.md`](evidence/map-topology-observed-facts.md)。
- [判定重构类与增量类归属，并定两类的排序规则](issues/02-classify-rearchitecture-versus-incremental-and-ordering-rule.md) — **判据定为「推翻五条有代码位置的现行架构不变量之一」**（B1～B5），62 条判为 **16 重构 / 46 增量**，逐条无遗漏；排序改用「每条增量不早于它所依赖的那条不变量的推翻」而非整体先后（**23 条治理面增量可从第一天并行**），重构内部顺序 **`B2 → (B1+B4) → B3 → B5`**；查明**充电依赖等待点而非多车调度**（`REQ-0178`）、**348 条里无一条要求多车**（B2 无需求载体）、**多车本身不进协议 v2**（54 个消息信封均必填 `agvId`）。`FP-C4` 8 条据此从票 03 拆出为票 12。答案见 [票 02 决议](issues/02-answer.md)。

- [决定多车并发、车辆占用与 Worklist 执行模型的重做范围](issues/03-decide-multi-vehicle-concurrency-and-worklist-execution-scope.md) — **同日复核推翻了一条事实前提**：「RIoT 查不到站到站成本」不成立，`edges/{mapId}` 给全图边表含 `Edge{snode, enode, cost}`，且**用户确认 RIoT 的规划算法就是跑最短路**，故自算不属 `RouteCostEvidenceBoundary` 所禁的「弱替代值」。据此补充 Q12（先实测再定，**Round 43 已于当日执行完毕，判定「边表可用」**——自建有向图 23/23 复现 RIoT 的择站选择，新立契约 BC-MAP-003 与 BC-ROUTE-002）与 Q13（`EnRoutePickupDeliveryDelay` 用**代价单位**而非时间），`REQ-0196`／`REQ-0198`／`REQ-0197` 换序半的形态**改由票 14 依实测定案**；Q1 档 2 与 Q2～Q11 不受影响。定为 **B2 全开 + B3 档 2 + B5 幅度 1**，16 条全部有归属（9 重构／7 增量），无一条延后到本图之外。**档位边界由 RIoT API 面决定而非主观选择**：`getRouteCostsBy` 起点恒为车当前位置、**查不到站→站成本**、**无任何时间估计**，而 `REQ-0198` 自带「增量无法可靠计算时不得追加」，故 `REQ-0196`／`REQ-0198`／`REQ-0197` 换序半**以 fail-closed 形态实施**（代码要写、批次要排，运行时恒拒）；反之首次组多 Demand 计划与选车成本比较**都算得出来**（终点固定为关卡，车间比较可精确归约为「各车→取货站」单段批量查询）。另定：选车走档 β（真实路径成本 + 零容差）、防饥饿机制实施而阈值留空、`REQ-0185` 复用 `AdmissionPolicy` 版本化、N 车用 `vehicles` 数组、B5 只推翻 select（与 `SublotSubmitted` 同构）、多车用单 worker 按车串行 + 每车超时预算、**唯一性从 lease 表下移到 `OrderIntents`**（`DispatchUniquenessGuard` 因此一字不改仍成立）、`REQ-0328` 取集合 B 且仅未取货、`REQ-0219` 取五字段回显、`OperationSession` 每 Demand 一个。**纠正票 02 两处**：`legType` 与 `stopRole` 的 enum 不因 B3 而改（那是票 12 与票 04 的要求）。答案见 [票 03 决议](issues/03-answer.md)。

- [依 Round 43 实测定途中追加与换序的形态](issues/14-decide-en-route-append-and-resequencing-form-from-round-43.md) — 三条**全部按完整能力实施**，票 03 的 fail-closed 形态**全部撤销**、`证据受限实施` 在本簇清零；新增工程大件 **`RouteGraphSnapshot` 路网成本引擎**。四条定案：其一，边际成本用**计划锚**（自插入位的前一站起算）——车当前位置到计划下一站那一段在插入前后完全相同、做差必然抵消（`PlannedStopMutationBoundary` 禁改当前下一站），故计划锚是**精确值不是近似**，连带使 `getRouteCostsBy` 与 `queryNearEnd` **一律退出产品代码路径**、量纲一致性降为无关项、绕 Facade 走 `Raw` 查成本的约束消失；其二，**可达性权威迁到自建图**，并据实测（可达中位 **185/206**、站 170 完全孤立）把「图上不可达」（常态、静默退出、不告警）与「成本证据缺失」（异常）拆成两个词条；其三，快照**双周期刷新 + fail-closed**（设计态按 `gmtUpdate` + 10 分钟 TTL 兜底，运行态移除集独立 10 秒周期），`mapEdgeGroup` 指纹变化或动态代价由空变非空即进陈旧态；其四，`REQ-0197` 两半**合并成一行**。**票 03 的 Q2 升级为全自算**，且升级幅度大于票 14 正文预设——但正文担心的「引擎不可用则选车失效」**不成立**（只失去成本排序层，退到 `DeterministicDispatchTieBreak`，与档 β 下 `getRouteCostsBy` 不可用后果相同）；**真正加重的是可达性那一半——引擎成为派车链路的硬依赖，快照陈旧即本轮不派车**。排期上引擎是**可与 B2 并行的旁路大件**（真实提前量，票 09 别漏），但必须先于任何多车派车能力上线。答案见 [票 14 决议](issues/14-answer.md)。

- [决定六类 MES 运输任务的执行范围，及其对 348 行剖面的影响](issues/13-decide-six-task-type-execution-scope-and-profile-impact.md) — **六类执行能力一次建齐，投运范围由现场绑定逐类 fail-closed 决定**；承载它的不是新需求，而是 348 行里被误判批次 0 的 **17 条公共站点绑定条目**（整簇零实现，本图第四次也是最严重一次「能力藏在批次 0」）。六类真名与五个 `PublicStationFunction` 完全闭环，**摄取侧无事可做**（MesIngest 已全量摄取六类、零过滤，本簇是「只加执行」）。六条定案：其一，六类走**同一套配置驱动机制**而非六份代码分支——`REQ-0343` 的 `TaskTypePublicStationRuleVersion` 已规定每类用哪个功能、该功能是起点还是终点，方向是规则里的字段不是 `if/else`；其二，公共站点绑定治理取**乙档**（数据模型 + fail-closed 校验落地为 `FP-C9a` 7 条，版本化／暂停／回滚／审计延后为 `FP-C9b` 9 条，与票 05 的 `FP-C7` 同批判档），并给票 05 一条硬约束——**`REQ-0339` 是 `FP-C9b` 的安全前提，砍它会同时挖空公共站点绑定治理**；其三，**`STAGING_TO_WIRE` 单列后一批**，因其方向反转推翻新识别的 **I6**，且 `BuildRouteEvidenceId` 的起终点位置互换**编译期完全静默**却会让幂等重放失配（风险隔离，不是省事）；其四，协议四处 breaking 的取值——`workType` 用 **MES 原始字面值**、`stopRole`／`legType` **拆开**取货卸货与站点功能两个正交概念、`profileId` 改名，清单给票 06；其五，剖面**新增第 11 列 `Batch0FormNote`** 记录「批次 0 判定在什么形态下成立」，批次 0 由 186 降至 **167**，`REQ-0204` 并入 `FP-C4`（票 12 的 B4 必须覆盖公共业务点独占）、`REQ-0205` 并入 `FP-C2`；其六，**验收期只有 `WIRE_TO_GATE` 能在现场真实触发**（其余四个公共功能不在 `mapId` 25 的 206 个站里），票 08 要定的是机制正确性的验收出口而非「六类都跑通」，且**不得把 `FP-C9a` 归入 `证据受限实施`**——那里的证据不可得是现场尚未测绘造成的，补测绘后无需改代码。答案见 [票 13 决议](issues/13-answer.md)。

- [决定空闲返回与等待点的独占语义与范围](issues/12-decide-idle-return-and-waiting-point-exclusivity-scope.md) — **B1 取得独立的 `VehiclePurposeClaims` 新表（主键 `VehicleKey`）取代 lease 表，B4 取 `(MapId, StationId)` 复合键、一行两状态、离点证据释放**；空闲返回作为一个停靠进入计划，为此给停靠加**第三个正交维度 `StopPurposeCategory`**（`BUSINESS`／`WAITING_POINT`／`CHARGER`），`CHARGER` 值由本票定、票 04 不再提第四个值。**最大发现：`REQ-0290`（`VehiclePurposeClaim` 原子争用车辆）是 B1 的定义条款而它一直在批次 0**——本图第五次「能力藏在批次 0」，且它与 `REQ-0147` 在本图此前从未被任何票据提及；改判后批次 0 由 167 降至 **166**、待排期由 81 增至 **82**、`FP-C4` 由 9 增至 **10**（5 重构／5 增量）。**票 03 删 lease 表唯一索引后留下的用途层空窗，正好只有它能补**：用途跨越 2～9 个订单，车在相邻订单之间 `OrderIntents` 上没有活跃行。另定：`Purpose` 四值枚举一次定全（`TRANSPORT`／`CHARGING`／`CLEARING_MAINTENANCE`／`IDLE_RETURN`，是本票与票 04 的接口）、不加隔离级别改为捕获唯一约束冲突、`REQ-0291` 全量重评全量实现（代价是一个独立于 journey tick 的重评入口）、`REQ-0294` 取架构测试（**但它是横切工程不属 `FP-C4`**）。**现场事实：Round 43 实测 mapId 25 的 206 个站 `type` 恒为 1、`desc`／`param`／`udp` 全空、站名只有 `N#-#`／`T##-##`／`关卡` 三形态，没有一个是等待点或充电桩**，故 `n` 不写进规格、机制建齐待测绘、验收期无法现场触发（与票 13 的 `FP-C9a` 同构，**不得归入 `证据受限实施`**），**票 04 的充电桩会撞上同一堵墙**。答案见 [票 12 决议](issues/12-answer.md)。

- [决定自动充电桩调度与充电失败治理的范围](issues/04-decide-automatic-charging-station-and-charging-failure-scope.md) — **自动充电全链路自动化、清桩闭环一次做完、备用桩改派进本期**，17 条一条不延后；`FP-C1` 整簇在控制服务端零实现（20 个标识符全零命中，`src/` 里含 `Charg` 的只有 3 行）。**用户 2026-09-04 给出两条改变形态的现场事实**：三个充电桩**物理上还没安装**，且 **8005 自建充电机制下充电桩是普通站点**——用 RIoT 本体机制才需要 `type=2`，自建机制不需要，故装完后哪些 id 是充电桩**只能问用户**，`ProjectExclusiveChargingStationRegistry` 是唯一身份来源且不可与 RIoT 交叉校验；另确认**不会有其它项目的车占用 8005 的桩**，占位判据只需覆盖本项目车辆（`BC-VEH-006` 已证「轮询全部车辆」永远证不出「空闲且无人正前往」，故那条路单独不成立）。**名册为空取「退化到 `ManualChargingHold`」而非静默**——与票 12 的等待点不对称是因为后果不同：车原地不动无害，车退出服务不能无声；这也顺带避开了 `manualChargingHold` 的含义变更。**最大工程发现：充电订单不是单段 move**，而是 `move(目标桩) + act(78, param1=1)`（Round 24 实测，离桩的 `act(78,2,0)` 由 RIoT 自动插入），而 vendored facade 的 `CreateMoveOrderAsync` 写死单元素 `move`**建不出来**，故 `RoutineOrderCreationCall` 扩为两种获准形态。**第二个工程大件：RIoT 订单命令面**（`CANCEL` 等四个命令 SDK 全在场、8005 一条没接、全图 15 张票据无人认领），跨 `FP-C1`／`FP-C2`／`FP-C4`，不计入任一簇。另定：分配核心共用但排序键不合并（先判用途再选资源，两个键作用在不相交子集上）、充满不主动腾桩（RIoT 机制如此）、四个人工动作按「对象里有没有车」分两端（协议增量压到两对消息）、名册走受控预置配置不做 UI。**查出本图第六、七次「能力藏在批次 0」并给出第一个系统性检出手段**：按 MVP 影响串分组，`REQ-0281`／`REQ-0282`／`REQ-0290` 恰好三条同串；改判后批次 0 由 166 降至 **164**、待排期由 82 增至 **84**、`FP-C1` 由 17 增至 **19**（4 重构／15 增量）。答案见 [票 04 决议](issues/04-answer.md)。

- [决定治理面增量范围（账号权限、配置审计、AGV 生命周期、看板）](issues/05-decide-governance-incremental-scope.md) — **人员认证与账号本期完全不建（用户 2026-09-04 取甲档）**，其余治理面按实施价值分判：`FP-C5` 做最小闭环、`FP-C7` 连地基一起做、`FP-C8` 看板做、`FP-C9b` 保留在本期。**最大发现是本图第八次也是规模最大一次「能力藏在批次 0」**——批次 0 的权限骨架在**两端都零实现**（`SystemAdministrator`／`PasswordHash`／`LoginSession` 全零，唯一的认证是一个全场共用的环境变量，`administratorRole` 是客户端自称的字符串），这**推翻了 map Notes 里「砍掉可调条目等于账号与密码继续靠部署时预置配置管理」那句话的前提**。用票 04 的影响串分组手段一次搜出三簇、改判 18 条出批次 0，批次 0 由 164 降至 **146**、待排期由 84 增至 **102**，新增簇 `FP-C10 人员认证与账号`（8 条，整簇延后）、`FP-C7` 由 6 增至 **14**、`FP-C8` 由 1 增至 **3**。**两条跨簇耦合被事实解掉而非被判断压过**：`REQ-0254` 不需要砍（协议 v1 已实装、服务端已校验，判部分实施），`REQ-0339` 拆两半（影响预览确认实施、密码二次认证延后，**明确排除用共享密钥冒充**），故 `FP-C9b` 保留在本期。**另立第七条不变量 I7**（车载持有并声明自身仓位配置，服务端只记录不裁决），`REQ-0258` 推翻它、判重构，`FP-C7` 因此不再是纯增量簇。另定：唯一人机界面是看板且界面上只允许 fail-safe 方向动作、配置审计取版本级不可变完整快照（字段级差异按需计算不另存）、回滚影响边界统一为「各消费者在自己的冻结点固化版本」、AGV 生命周期取两个正交维度且业务可用性是 fail-closed 谓词链的**派生量**不是可写状态、票 03 交来的两份名单**不做运行期在线修改**（`AdmissionPolicy` 机制已经在跑，本票唯一一处「现状即结论」）。给票 06 的协议输入是**三项 breaking**：`CapabilitySnapshot` 缺指纹字段（54 个 schema 里 `fingerprint` 零命中）、配置激活的一对新消息、告警上报消息。答案见 [票 05 决议](issues/05-answer.md)。

- [一次冻结协议 v2 消息面、错误码与向量范围](issues/06-freeze-protocol-v2-message-surface-error-codes-and-vectors.md) — **v2 = `ProtocolVersion` 2 ＋ `profileId` `AGV_FULL_PRODUCT` ＋ release `1.0.0`，消息面 63 条、错误码 45 条、向量 31 条，不引入协商。****最大发现是错误码注册表的两端不对称**：`ErrorCode` 是 43 值封闭 enum 且不只用于报错——`BlockingFact.reasonCode` 与 `SlotState.reasonCodes` 也用它，车载端 43 条**全部命中**，控制服务端只发 5 个码而**其中 3 个不在 enum 里**（`PROTOCOL_RELEASE_MISMATCH`／`RECOVERY_AUTHENTICATION_REQUIRED`／`RECOVERY_SESSION_ALREADY_OPEN`）。**它们一直没被抓到，是因为两端都不做运行期 schema 校验**——两个实现仓的依赖清单里没有任何 JSON Schema 库，`tools/ControlServer.Conformance` 只有 55 行、只算 manifest 哈希、**不跑任何向量不校验任何 schema**，G2 证据记着 `vectorId` 而实际跑的是本仓 xunit 套件。判为实现缺陷不为它们开口，并要求两端各加一条**「reasonCode 字面量 ∈ 注册表」架构测试**——那是「协议仓的 enum」与「实现真正发出的字节」之间目前**唯一缺失的一条校验**。另查出四条推翻票据预设的事实：**`profileId` 改名影响 1403 文件／1509 处**（票据引的 56／58 只是 `schemas/`，`examples/` 有 1345 个）；**`g1-validate.mjs` 硬编码了 v1 的形状**，第 25 行的 `worklistItems.maxItems===1` 与「顶层 `demandId` 必须支持 null」两条**与票 03 及本票的定案直接冲突**，不同步改则 G1 必红；**版本错配是一条带完整期望身份的说明性错误**，不是票据预设的「连接被拒而无解释」；**`REQ-0264`／`REQ-0266` 本期就要指纹**，票 05 把它框成「为已延后的 `REQ-0316` 预留」框窄了——不加则 `FP-C7` 本期做不完。另定：九条新增消息统一取「单条 `Result` 带 `outcome` enum」范式（省 4 条 schema 与约 100 个样例）、**配置激活那对必须走 RELIABLE 而非 REQUEST/RESPONSE**（`REQ-0264` 的「不能猜测成功」正是 `PENDING_RESULT_REPLAY` 的理由）、**告警走快照而非事件流**（`REQ-0269` 禁「已过期」展示状态，事件流断线重连那段就是它）、**告警 code 不复用 `ErrorCode`**（开放集合塞进封闭 enum 等于每个故障码发一次 breaking）、两个快照的 dedup 键统一改用 revision（`items[].demandId` 在 `maxItems` 1→8 之后变成多值键、语义无定义，这一条票 03 未提）、**票 04 与票 12 关于 `legType` 的推诿判给票 12**（不加 `TO_CHARGING_STATION`，充电桩由正交的 `StopPurposeCategory` 承载）、`slotStates` 保持定长 8（该定长出现在**两条**消息里）、`appsettings.json:9` 的死配置判删。**`WireToGateMvpProtocolProfile` 词条的两个限定词被完整产品全部推翻**，新立 `AgvFullProductProtocolProfile` 并改 `ProtocolRelease` 词条正文——这是本图**第一次发现既有词条的定义被推翻而不是缺词条**。答案见 [票 06 决议](issues/06-answer.md)。

- [决定全量切片家族编号与 W2G-IS-00～07 的关系](issues/07-decide-full-product-slice-family-and-relation-to-w2g-is.md) — **替换为 `FP-IS-NN` 家族（`^FP-IS-[0-9]{2}$`），16 个切片，31 条向量全部绑定；门禁模型不增不减；批次与业务簇都不进 id。****本票据自身的预设被推翻四条**：其一，`^W2G-IS-0[0-7]$` 不是「两份副本分属两个仓库」而是 **9 处硬编码 ＋ 161 个测试 trait**——票 06 漏了 `integration-slice-index.schema.json` 的两处、`result.schema.json` 的两处、`test-wire-to-gate.ps1` 的 `[ValidatePattern]` 与它那张 `$sliceVectors` 表（**`index.json` 的第二份副本，两份之间无人对账**），以及 `[Trait("IntegrationSlice", ...)]` 这个 `dotnet test --filter` 的分区键；其二，票据陈述的三种代价**两种不成立一种做不到**——「替换作废证据链」不成立（**证据由 `ProtocolReleaseIdentity` 换代作废，与编号无关**，三选项间是常量）、「沿用难区分证据」不成立（`gate-result.json` 有六项发布身份，真实代价是永久名实不符 ＋ trait 歧义）、「并存」结构上做不到（schema 锁 `minItems/maxItems: 8`，且 v1 的 `index.json` 活在不可变 tag 里，一个仓一个路径——**并存就是替换换个说法**）；其三，「命名一眼看出属哪个批次」是**循环依赖**；其四，`definition` 的 `demandRepresentation` **三个字段全是 `const`**，充电／激活／告警切片一个都填不进去，推广必然连带重写。**另查出两条票 06 与交接文档都记反了的事实**：`runner/` 两个 schema 是**无生产者、无消费者、且与真实向量和真实门禁结果都不兼容的孤儿**（契约要顶层对象带六项必填而 `input.ndjson` 是 step 的 NDJSON 流，`initialPersistentFacts` 被锁成只能是空对象；`result.schema.json` `required` 16 项而 `gate-result.json` 只给 5 项，**缺的正是 `onboardHmiCommit`／`fakePeerIdentities`／`firstDivergence`／`evidencePointers` 四项双端联合证据核心**；而 G1 对 `runner/` 只做 `validateSchema` ＋ `compile`，**从不用它校验任何文档**）——**这是票 06 那条「G2 记着 `vectorId` 却只跑自家 xunit」的上游原因**，修还是删交票 08；以及**样例生成器实际存在**（见 Notes）。**本图第二次发现既有词条的定义被推翻**：`CONTEXT.md:231` 的 `IntegrationSlice` 写着「一段 **WIRE_TO_GATE** 跨端能力」，已改写并新立 `IntegrationSliceFamily`。答案见 [票 07 决议](issues/07-answer.md)。

- [处置 `REQ-0298` 与票 14 `RouteGraphSnapshot` 的正面冲突](issues/15-resolve-req-0298-conflict-with-route-graph-snapshot.md) — **`REQ-0298` 走需求变更提案 `CP-0001`（本图输出、不执行），理由不是「我们要违反它」，而是它的来源票据白纸黑字写着的两条理由今天都已失效。****最大发现是冲突不止一条而是三组，后两组票据完全未提。**其一，`REQ-0298` 是**我方 2026-08-24 自批的边界决定**（来源票 77，`Source Authority: 用户本人`、`AI Involvement: none`、`Derivation: verbatim-list-item`），票 77 第一轮取证逐字写着排除 Edge 的两条理由——「**现有材料仍未证明**目标运行环境的 Edge 返回权限、完整性、变化语义与跨版本稳定性」与「当前需求基线**不必为了既有派车规则先**承诺本地完整路网图」——**前三项已由 Round 43 填补，第二条的语气本就是「不必先承诺」这句范围声明**；规范化逐字抄了以「当前产品」开头的 Answer 首条，把时点限定丢掉后剩下一句读起来绝对的禁止（**本图第二次撞见规范化改变了原决定的力度**）。其二，**五条基线条目明文要求建单前置 `RouteCost` 门禁而票 14 把它一并赶走了**（`REQ-0147`／`REQ-0293`／`REQ-0302`／`REQ-0305`／`REQ-0345`，票 14 全文一条未提，而 `src/` 里 `RouteCost` **零命中**——这道门禁从没实现过），已收窄票 14 的定案、门禁保留。其三，**引擎要调的四个 `imap` 端点全部不在获批白名单里**（票 37 的 9 个具名 endpoint 全在 `/api/version/` 与 `/api/task/` 下，vendored Facade 的 38 个 `*Async` 方法无任何边表方法），用户已批准扩项且要求走具名 Facade。另查出 **`CONTEXT.md` 自身已因票 14 改写不完整而矛盾**（`:533` 可达性归 `StationReachability`，`:835` 仍写「并通过 RouteCost」），四处已修正。**否掉一条诱人的捷径**：不能拿 `REQ-0309` 的「API 与数据结构留给后续设计」覆盖 `REQ-0298`——全基线只有 `REQ-0199` 与 `REQ-0309` 两条自我豁免，二者都是所属票据的**末条范围分离声明**，而 `REQ-0298` 是**首条事实范围声明**，且 `REQ-0309` 豁免的是「怎么实现」不是「哪些事实进入产品」。**v1.0.0 至今零次需求变更**（348 条全是 `Change Proposal: none`，Modified 段是 None），`CP-0001` 会是第一份；格式在基线头部已定死，**但执行流程尚不存在**。答案见 [票 15 决议](issues/15-answer.md)。

- [决定实施批次划分与批次间依赖，348 行剖面定稿](issues/09-decide-implementation-batches-and-inter-batch-dependencies.md) — **九个批次 0～8 ＋ 一个延后集，348 行 `Batch` 列定稿**（新 SHA-256 `aa5b1117…ac34`，349 LF、0 CRLF、220,819 字节）；批次 0 **117**、待排期 **131**、延后 22、Out of scope 100。**本票最大的产出不是批次表，是逐条复核查出的 29 条改判**——这是本图迄今规模最大的一次（票 05 那次 18 条），并一次查出**第九、十、十一次**「能力藏在批次 0」，**第十一次含安全能力**：`OrderHold`／`triggerEmergency`／`cancelEmergency` 两端全零，MVP 的阻断按不住已发出的订单、也不会自动触发急停（已按用户决定在 `8005-agv-control-server` 开缺陷单）。**批次 1 = 协议 v2 候选生成与 G1 通过**，它的可行性由本票实测支撑——**样例生成器在 `vm01` 上一次跑出 1504 个文件，与协议仓 v0.1.1 逐字节完全相同**，票 06 判的「最大单项 = 手写约 1500 个 invalid 样例」整段不成立，真实提前量是改那个 `.mjs` 的 11 项输出点。另定：**批次 1 在 348 行里不产生任何一行**（与 B2 同理，故单列十项无载体工程）、依赖是 DAG 且 11 条边各有归约、切片到批次一对多且顺序与 `prerequisites` 全部一致、**没有切片的工作由七类增至九类**、`REQ-0207` 单独排进批次 2（可达性权威是多车派车硬前置）、白名单再扩一个端点（`removedStation`，用户批准）、`REQ-0309` 的第二条 Edge 禁止**论证不冲突不进 `CP-0001`**、`FP-C12` 整簇延后（用户定）、`REQ-0259` 的 IO 门禁须与三台车 IO 录入同批。答案见 [票 09 决议](issues/09-answer.md)。

## Not yet specified

- 充电桩与等待点的现场安装与测绘时间。票 04 从用户处确认三个 8005 独占充电桩**物理上还没安装**，票 12 确认等待点尚未测绘进 RIoT 地图；两者装好并录入名册之前，`FP-C1` 与 `FP-C4` 在现场都无法真实触发。这不阻塞本图任何票据（机制一次建齐、投运待现场，两簇的形态已定），但它使**这两簇的现场验收窗口不可预测**，而 map Notes 的日历约束是「一周内完成规格 + 批次 1 落地」。安装完成后现场值只能由用户给出（充电桩身份不可从 RIoT 观测推导），届时排期与验收出口是否要重排、以什么方式重排，现在还看不清。与下一条 Kun Wang 可投入时段同属「本工作区看不到的外部事实」。

- **`8005-agv-onboard-hmi` 与 `slots-simulator` 归属交接的时间点与形态。**用户 2026-09-04 告知这两个仓今后由我方负责开发（见 Notes），但**什么时候交、怎么交、交接期间两边各做什么，全部未知**，而这三件事同时决定：票 08 的分工与排期形态、v2 打 tag 需要的第二名产品负责人是谁（`release-governance.md` 要两名不同的人、G1 实打实校验、AI 与 CI 不能批准）、以及 `ONBOARD_HMI_G2` 由谁跑。**这块迷雾取代了原先那条「Kun Wang 的实际可投入时段」**——那条问的是他有多少时间，现在要问的是他还做不做这两个仓；两个问题的排期后果完全不同。票 06 已把能定的部分定了（交接完成前 Kun Wang 就是那第二名产品负责人，本图不改 `release-governance.md`），剩下的仍是本工作区看不到的外部事实，票 08 之后大概率还需要一次外部确认。**票 07 缩小了这块迷雾的一角**：`CONTROL_SERVER_G2`／`ONBOARD_HMI_G2` 命名的是**哪一端**不是**哪个人**，切片的 `gates` 数组一字不改，所以交接**不影响门禁模型与切片分工机制**，只影响「第二名产品负责人是谁」与「谁来跑 `ONBOARD_HMI_G2`」。与上一条充电桩测绘同属「外部事实」类迷雾。
- 完整产品是否需要新的需求基线版本。本图不改 v1.0.0；若批次划分过程中发现某些条目在完整产品语境下的语义与基线文本不符，是走需求变更流程还是在规格中加注，届时再判。**第一个具体实例（跨 Map 运输需要修改 `REQ-0193`）已由票 11 处置完毕——判 Out of scope，不走需求变更。****第二个实例已由票 13 撞见并另开票 15**：票 14 的 `RouteGraphSnapshot` 与 `REQ-0298`（「Edge、几何路网、本地最短路与动态交通状态均不进入本产品事实」）**逐字冲突，四项全中**，而 `14-answer.md` 全文未提及该条。它**不能照票 11 的办法化解**——票 14 已把引擎定为完整产品的硬依赖，它在 Destination 之内，判不了 Out of scope；`REQ-0298` 也不是「对现实的准确描述」而是一条禁止性设计约束。这条迷雾保留，因为其余条目是否还有同类冲突仍未逐条查过（两个实例都是复核别的事情时偶然撞见的，不是穷举结果），且**两个实例的解法未必同源**——票 11 证明这类冲突的解法未必是改基线、先要问的是现场事实，而 `REQ-0298` 没有对应的现场事实可问。是否要对 348 条做一次专门的冲突复核，由票 15 判。**票 12 又添了第三个实例，而它的形态与前两个都不同**：`REQ-0290` 标着批次 0 却是 B1 的定义条款、完整产品语境下完全未实现——**需求文本本身没问题，是剖面判定错误**，故解法是改判归簇，既不动基线也不判 Out of scope。**这使这条迷雾分成了两个互不覆盖的检出面**：前两个实例要逐条读基线文本找禁止性约束，第三个要逐条查代码验证批次 0 判定在什么形态下成立（票 13 加的第 11 列 `Batch0FormNote` 记的正是后者）。票 09 的逐条复核覆盖后者，**前者目前没有任何票据系统性覆盖**。票 15 判「要不要专门复核」时须说清复核的是哪一种。**票 04 给这一形态添了第二、三个实例（`REQ-0281`、`REQ-0282`），并把它从「偶然撞见」变成了可系统检出**：三个实例共享同一个 `ControlServerImpact` 串，说明按影响串分组是有效手段（详见 Notes 中「能力藏在批次 0」一条）。**票 05 又给这一形态添了 18 个实例，并把它从「有手段可用」推进到「手段已被验证可批量使用」**：一次查两个影响串共 31 条、改判 18 条出批次 0，三簇分属权限骨架、看板显示规则与仓位配置权威，全部是主动搜出来的（详见 Notes 中「能力藏在批次 0」一条）。这一面现在有两层覆盖：票 09 的逐条复核 + 影响串分组。**票 06 第一次给另一个检出面添了数据点，而且是反向的**：`REQ-0264`／`REQ-0266` 的原文里各有一个名词（「指纹」）**要求了一个协议上不存在的字段**（54 个 schema 里 `fingerprint` 零命中），这不是禁止性约束而是**遗漏性约束**——方向与 `REQ-0298` 相反，但检出手段相同，都是逐条读基线文本。这说明那一面的复核收益比票 15 目前估计的更高：它不只能查出「需求禁止了我们要做的事」，还能查出「需求要求了我们没有的东西」，而后者的后果是一个簇本期做不完（票 06 查实不加指纹则 `FP-C7` 无法实施）。**票 15 已把这条迷雾收敛掉大半，并给它装上了手段。**其一，**该面第一次有了系统性覆盖**：取「定向扫描」（从已定决议侧提关键词扫 348 条正文），口径是 **A∪B 面 120 条**，本票完成第一轮、剩余交票 09（见 Notes）。其二，**这条迷雾问的「走需求变更流程还是在规格中加注」现在有了第一个真要走变更流程的答案**：`REQ-0298` 判甲档，提案 `CP-0001`，并为此在 map Notes 里新增第三种合法形态 `需求变更待批`。其三，**遗漏性约束这个方向的收益被实测抬高了**——票 15 从 B 面（边界沿用措辞，25 条）查出五条建单前置 `RouteCost` 门禁被票 14 一并赶走，**而此前全部票据讨论的都是 A 面**。**剩下的不确定只有两点**：A∪B 面另外那约 100 条还没扫，以及**需求变更的执行流程本身尚不存在**（v1.0.0 至今零次变更，格式在基线头部已定死但没有任何文档规定谁在什么时候把提案变成 v1.1.0、要不要重算 Baseline SHA-256 与 tag）——后者与下一条的 tag 问题同源，票 10 汇编前须一并有结论。**票 09 已把这条迷雾的前一半彻底关掉：A∪B 面 120 条全部扫完，剔除 `OOS-MI-*` 的 13 条后逐条精读 107 条。**净结果只有一条新候选——`REQ-0309` 的第二条 Edge 禁止——**且判定不冲突**（论证见票 09 决议 1.2，主语是「目录」，引擎不顶替目录）。它还反向送回一条收益：`REQ-0309` 是「必须走具名 Facade」的第二个需求载体，比票 37 那份位置不稳的 scratch 票据稳。**同时票 09 给这个手段补了一条使用纪律**：A 面的零命中有两种相反含义，纯禁止性条目（如 `REQ-0165`）的零命中是遵守的证据而不是缺口（见 Notes）。**这条迷雾现在只剩后一半——需求变更的执行流程本身尚不存在**，而 `CP-0001` 仍是两条修订项，其中第二条已由四个 `imap` 端点扩到**五个**（票 09）。提案还在长，而没有流程这件事一天没解决，它就一天落不了地。
- 基线 tag `requirements-baseline-v1.0.0` 本地与远端都不存在（票 01 发现），content commit `b9f3212` 仍可解析。补打 tag 还是从身份绑定中删除这一项，需用户决定；票 10 汇编前必须有结论。同类问题：MVP 剖面 TSV 没有 `.gitattributes` 保护，它的哈希稳定性靠当前 git 行为而非规则，是否要一并加固还没判。**票 12 查出第三个同类**：`REQ-0294` 正文引用的「现有白名单」实际是 `.scratch/current-requirements-baseline/issues/37-decide-riot-api-allowlist-and-call-safety-boundary.md`——**另一张已完成地图的工作票据，不是产品文档**。那份 scratch 若被清理，RIoT 调用白名单就没有权威副本了，而它是唯一一份逐条列举获批调用的东西（`REQ-0147` 只覆盖建单那一类）。三者共性是**需求文本引用了一个位置不稳的外部身份**，票 10 汇编前须一并有结论。**票 04 加重了白名单那一条**：获准的 RIoT 建单调用**从一种形态变成两种**——普通搬运与空闲返回是单段 move，前往充电桩是 `move(目标桩) + act(78, param1=1)`（vendored facade 的 `CreateMoveOrderAsync` 写死单元素 `move`，建不出来）。那份纯文档白名单因此不仅位置不稳，内容也已过时。

- `riot-sdk` 仓库 HEAD 与控制服务端链接的 vendored `RIoT.Sdk.Facade 0.1.0-controlserver.2` **不是同一个 API 面**（票 03 查实）：`ListStationsStrictAsync`、`GetVehicleCardAsync`、`FindOrderByUpperIdAsync`、`ListOrdersByStatesAsync`、`GetVehicleExecutionFactsAsync` 在仓库源码里不存在，来源 commit `e708f874` 因历史重写已消失。**票 14 已大幅降低这条的权重**：Q1 定案后 `getRouteCostsBy` 退出产品代码路径，档 β 不再需要它；而路网成本引擎**必须自定义反序列化**（现场是 snake_case 且是 `s_node`／`e_node`，Kiota 生成层本就不可用），所以它需要的只是一条能发 HTTP GET 的通道，**不依赖生成层的类型**。**票 12 已把残余风险彻底消掉**：`OrderClient.Raw`／`TaskClient.Raw`／`MapClient.Raw` 三个属性在 vendored 包里**都是 public**，控制服务端当前 `.Raw` 零命中是**没用不是不能用**。这条迷雾到此结束，**不必再为它保留重开票 03 Q2 的余地**。反过来它暴露了一个新事实，已由票 12 的 Q8 处置：`Raw` 是 public 逃逸舱且 vendored 的 `Generated.dll` 里 16 个 `Park*` 类型全部在场，故 `REQ-0294` 明文禁止的一键停靠**编译期、运行期、测试期三处都没有守卫**，白名单是一份纯文档。

## Out of scope

- **MesIngest 与 MesIngestWatch 的 100 条**（Inspector E 30、AreaFilterProfile 26、MesIngest 服务与目录 18、GONE／存储 26）。它们标「本场景不适用」的含义是与 WIRE_TO_GATE 旅程无关而非未实现，归 `8005-mes-ingest`，该仓已有 `new-mes-ingest`、`mes-ingest-bounded-storage-low-memory`、`demand-series-inspector-e`、`mes-ingest-watch-area-live-sync` 四张地图、1100 个测试与已落地的 CI/CD。本图不重排一个健康仓库的既有工作，最终规格只留一行指针。
- WIRE_TO_GATE MVP 的收尾、P0～P7 现场试运行执行，以及 `8005-agv-control-server/docs/defects/` 当前 5 个未闭合缺陷的处置。它们有自己的图与证据链。
- 批次 1 及其后任何批次的代码实施、部署与发布。归随后的实施图。
- **跨 Map 运输**（票 11 判定）。v1.0.0 的 348 条无一条要求它，`REQ-0191`／`REQ-0192`／`REQ-0193` 明文将运输两端限定在同一 Map，三条均已在批次 0 实施。现场当前为单图作业（`mapId` 25 `老厂前线new`，3 台 AGV 与全部机台、关卡、独占充电桩、等待点均在该图）。若将来现场扩为多图作业，放开跨 Map 运输须先经需求变更流程处置 `REQ-0193`，属独立的后续工作，不在本规格的批次序列内。**这段措辞是最终规格 Out of scope 段的原文，票 03 第 6 点原样引用**；它只描述要走哪条流程，不预测将来会不会放宽，因此不构成在规格里预告一次未经批准的需求变更。见 [票 11 决议](issues/11-answer.md)。
- 修改、废弃或重发 v1.0.0 需求基线，以及把本图判定的延后条目表述为永久取消。
- 代替 Kun Wang 评审或批准车载端实现，代签 protocol release，或以 AI／CI 充当 `docs/release-governance.md` 要求的两名产品负责人之一。
