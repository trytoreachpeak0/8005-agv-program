# 决定故障车辆隔离、货物处置与人工越权边界

Type: grilling
Status: resolved
Blocked by: 63

## Question

AGV 故障、离线或导航失败达到什么事实与时限后进入不可用，复用“禁用”还是保持独立业务状态；已装载货物如何现场取出、改派、终止或形成补救记录，哪些角色可以确认、是否需要二次认证，以及事实未知时能否人工强制标记或解除？

## Evidence rows

`R01-A1899`、`R03-A1587`、`R03-A1595`、`R03-A1597`、`R03-A1601`、`R03-A1616`、`R03-A1618`、`R03-A1628`。

## Comments

- 2026-08-05：用户批准把“管理员禁用（AdministrativeVehicleDisable）”与“车辆故障隔离（VehicleFaultIsolation）”建模为两个独立且可同时存在的业务状态。故障隔离不复用“已禁用”；底层可以使用 DispatchDisable 形成调度阻断，但控制动作不等同于故障业务状态、故障确认或故障解除。
- 2026-08-05：用户批准采用两级触发：明确且新鲜的不可恢复故障事实或授权现场确认可直接进入车辆故障隔离；离线、通信中断、导航失败或单个订单 FAILED 等模糊症状先进入疑似车辆故障阻断，立即禁止新派车并告警，但不直接断言车辆故障，后续须凭独立证据或现场确认升级。
- 2026-08-05：用户进一步批准持续时间永远不能单独把疑似车辆故障阻断升级为车辆故障隔离；超时只提高告警等级和人工响应紧迫度。升级必须取得新鲜、版本绑定的车辆级不可恢复故障硬证据，或授权人员对具体车辆的现场确认。
- 2026-08-05：用户批准严格的车辆故障硬证据白名单。当前只有新鲜的 `emergencyState=CAN_NOT_RECOVER` 可自动触发车辆故障隔离；未绑定目标版本的 `sysState=ERROR/HARDWARE_ERROR`、无批准码值语义的 `lastErrorCode`、`hardwareErrorCode`、`faultCodesList`，以及离线、导航失败和订单 `FAILED` 均只触发疑似阻断。未来扩展必须绑定环境、build、车型/固件、严重度和清除条件并再次批准。
- 2026-08-05：用户批准人工进入隔离的初始权限授予 R-11 设备/电气维护人员与 R-13 AGV 运维/调度管理员；任一具备权限者可用个人账号单独确认具体车辆故障并进入隔离，不需要二次认证或双人审批。R-12、R-15 可提供诊断证据但不能仅凭软件或网络异常确认单车故障，普通生产操作员只能报告或请求处置。确认须审计确认人、角色、车辆、时间、现场事实与关联异常证据，且不授权解除隔离、终止任务或处置货物。
- 2026-08-05：用户批准车辆隔离后，只有原 RIoT 订单已确认终结、没有已提交 LoadBatch、没有活动或未知结果的仓位操作，且服务端可证明车上没有该任务产品时，才解除原车辆绑定并让原 DemandId 重新进入正常派车。该处理不是业务取消，不写 TransportDemandSuppression，也不创建新需求实例；任一装货、订单或仓位事实未知时，任务继续绑定原故障车并阻断。
- 2026-08-05：用户批准已有装货事实或货物状态未知的任务进入“故障车载货保全”，保留原 DemandId、车辆、仓位、产品和订单绑定，不得直接改派。只允许车辆修复并完成恢复核验后继续原任务，或经授权的现场取货流程完整取出货物后终止原任务；等待时长只提升告警和响应紧迫度，不自动选择路径或取消。
- 2026-08-05：用户批准由 R-09 班组长或具备等效生产管理权限者，经即时二次认证决定载货任务不再等待车辆修复、切换为现场取货并终止路径。R-11、R-13 可报告、申请或执行各自获授权的设备/车辆处置，但不能仅凭维护或调度权限替生产方终止载货任务；普通生产操作员可协助取货和人工搬运但不能作出该决定。
- 2026-08-05：用户批准故障载货取出使用独立受控模式，不复用禁止开启业务绑定仓位的现有维护开门模式。进入前须有有效取出决定，并证明车辆不会移动：优先确认当前订单已 HELD/终结、车辆停稳且继续移动受阻；RIoT 结果未知时必须由 R-11 完成断电、抱闸或等效现场物理隔离。VehicleFaultIsolation 与 DispatchDisable 本身均不证明停稳。只有 R-11 或等效设备维护权限者可授权并执行故障开仓，R-13 可协调但不能仅凭调度权限开仓，普通操作员只能在安全条件建立后协助取货，且只能开启决定绑定的目标仓位。
- 2026-08-05：用户批准不建立“人工完成运输”任务，而形成不可变的故障载货补救记录。全部目标仓位须达到 `EMPTY + 锁闭 + 开锁输出复位`，并记录取出人与具名接管人、产品/Sublot、车辆、仓位、现场位置、时间、原 DemandId 和取出决定；完整取出交接后，原任务以 `TERMINATED_BY_FAULT_CARGO_HANDOFF` 终结，并与对应 TransportDemandKey 的永久抑制原子提交。8005 不标记成功送达、不创建人工运输任务、不等待或轮询 MES 扫码；任一目标仓位、产品身份或交接事实未知时继续保持故障车载货保全，不得部分终止。
- 2026-08-05：用户批准解除车辆故障隔离采用两个独立动作：R-11 或等效维护权限者提交车辆故障恢复确认，R-13 或等效调度权限者经即时二次认证执行恢复业务使用授权；同一人兼具两项权限时仍须分别留痕。系统必须独立核验位置、订单、运动、急停、仓门、开锁输出、货物绑定和未结操作，任一未知或冲突均继续隔离且不得人工强解。解除只恢复后续资格评估，不自动继续订单或立即派发任务。
- 2026-08-05：用户批准载货任务修复续行始终保留原 DemandId 和原车辆。原 RIoT 订单仍确认为 HELD 且身份、目标与货物绑定一致时走既有 OrderContinue 授权和回查；原订单已确认终结、相关阻断已收敛且 8005 任务未终止时，可在重新通过正常派车和安全门禁后为同一 DemandId 建立关联旧订单的新 RIoT 移动单。结果未知、身份冲突、位置或路线不可证明时继续载货保全，不得换号绕过或改派；已完成故障载货补救终止的任务不可复活。
- 2026-08-05：用户批准疑似车辆故障阻断可以凭正面恢复证据自动解除：车辆与 RIoT 通信及状态须满足统一的新鲜度/连续稳定门槛，不存在硬故障证据，并完成订单、位置、运动、急停、仓门、货物绑定和未结调用的无冲突对账；任一事实未知时不得人工强解。解除只恢复资格评估，不自动继续订单或派发任务；期间出现硬故障证据则升级正式隔离。具体轮询次数和时间窗口由[决定监控新鲜度、告警升级、重试与日志留存规则](73-decide-monitoring-freshness-alert-retry-and-log-retention.md)统一确定。
- 2026-08-05：用户批准进入疑似车辆故障阻断或正式车辆故障隔离时立即禁止新派车，并对本项目当前执行的 RIoT 订单自动调用 OrderHold、回查确认 HELD；不得用 Cancel 代替停车。OrderHold 不可用、结果未知或车辆未及时停下时保持全部阻断并形成高优先级升级事件，EmergencyStop 的具体条件交由[决定安全联锁失败升级与紧急停止边界](70-decide-safety-interlock-failure-escalation-and-emergency-stop.md)统一决定；取得实际停车证明前不得显示为已安全停稳。
- 2026-08-05：用户否决此前逐步形成的审批链，指出存放错误和车辆本体故障的处置过于繁琐，并批准统一简化为 ExceptionRecoverySession：具备统一异常处置权限的人员验证一次个人身份后，可在绑定具体事件、车辆、任务和目标仓位的会话中完成隔离、选择修复续行或取货终止、处理存放错误、交接和车辆恢复，不再要求 R-09 取货审批、R-11/R-13 双动作恢复、双人复核或逐动作二次认证。系统自动留痕；停车/物理隔离、目标范围、仓位 `EMPTY + 锁闭 + 开锁输出复位` 及恢复前完整对账仍是不可绕过门禁。未知事实不能靠身份强制恢复，但完成断电、抱闸等物理隔离后可执行必要救援，车辆继续隔离至事实可验证。本条取代本票前述与其冲突的角色审批和重复认证安排。
- 2026-08-05：用户批准 ExceptionRecoveryPermission 初始授予 R-09、R-11 与 R-13，并允许按个人显式扩展给经过现场培训的生产人员，而不要求改变岗位角色；任何持权人验证身份后均可独立完成整套异常处置。无权限人员可报告、协助取货或接管货物但不能操作异常处置界面。断电、抱闸等动作仍由具备相应现场作业资质者实际执行并记录，作业资质不是审批步骤。
- 2026-08-05：用户批准一次身份验证建立的 ExceptionRecoverySession 只绑定一个处置人员、一个异常事件、一台车辆及该事件涉及的任务和目标仓位集合；会话内连续完成操作，不逐步重新验证。人员交接、切换车辆、扩大仓位范围、主动退出或事件关闭时会话结束；短暂断线后同一人员、同一事件且会话仍有效时，完成最新状态对账即可继续，断线期间不能扩围。空闲超时时长由后续统一登录权限规则配置。
- 2026-08-05：用户批准以统一 PlacementCorrection 取代分散的存放错误处置。原任务进行中时，在异常处置会话中冻结受影响范围、取出或换回正确产品并按实时物理状态更新原任务，不创建新任务；原任务已完成时不重开或改写旧终态，只完成必要取出、现场交接并自动追加纠错记录，不生成补运任务或修改 MES。两种情况都以目标仓位状态可证明、仓门锁闭和开锁输出复位为完成门禁；未知或冲突只隔离受影响仓位，无法限定范围时才隔离整车，且不再申请、审批或逐仓重新认证。
- 2026-08-05：用户随即纠正上述 PlacementCorrection 扩张：8005 是一对一输送，普通放错只允许在车辆离开起点前通过既有 LoadCorrection 调整；离站后将错就错完成输送，不再纠正、换仓、重绑、重开任务或生成补运，最多记录差错事实。ExceptionRecoverySession 不处理普通存放错误，只处理仓内传感器/锁故障、车辆本体故障及实际被困货物；本条完全取代上一条 PlacementCorrection 决定。
- 2026-08-05：用户批准极端故障使用 ForcedMechanicalCargoRecovery：锁无法经 DO 弹开，或锁反馈、光幕/占用检测等状态失真时，先完成断电、抱闸或等效机械隔离，再由具备现场作业资质者强制机械开锁或拆卸；系统停止重复 DO，将可证明的受影响仓位/模块标为物理状态未知，范围不明时隔离整车。人工交接证据可以关闭身份明确货物的取出、交接、任务终止与抑制，但不能证明设备安全或恢复；身份未知时保持待盘点。强制处置建立新的恢复代次，使旧命令、快照、检查点和迟到结果只能作历史证据；车辆须修复锁、DO、反馈、光幕及 IO，完成空仓/有物、锁闭、输出复位测试和全量对账后才能恢复。
- 2026-08-05：用户要求在把本票收敛为最终 Answer、关闭票据并更新地图之前，先把拟写入文档的完整内容发给用户确认；在取得该确认前保持本票 claimed。
- 2026-08-05：用户审阅并确认最终收敛稿，授权按该版本写入 Answer、关闭本票并更新地图。

## Answer

用户于 2026-08-05 逐项批准并最终确认以下结论；本 Answer 取代 Comments 中被后续纠正或简化的中间方案：

1. **管理员禁用与车辆故障隔离独立。** `AdministrativeVehicleDisable` 是主动阻止后续调度的业务状态，`VehicleFaultIsolation` 是故障处置状态；两者可同时存在，均不等同于 RIoT `DispatchDisable` 控制动作，也不因底层动作成功而自动证明当前订单已停止或故障已解除。
2. **故障采用“疑似阻断—确认隔离”两级事实模型。** 离线、通信中断、导航失败、单个订单 `FAILED` 和持续时间均不足以证明单车故障，只形成 `SuspectedVehicleFaultBlock`，立即阻止新派车并告警。时间只提高告警和响应紧迫度，永远不能单独升级故障事实。
3. **自动确认故障使用严格硬证据白名单。** 当前唯一可自动进入 `VehicleFaultIsolation` 的事实是新鲜的 `emergencyState=CAN_NOT_RECOVER`。`sysState`、`lastErrorCode`、`hardwareErrorCode`、`faultCodesList` 及其它候选须先绑定环境、build、车型/固件、语义、严重度和清除条件并再次批准。具备 `ExceptionRecoveryPermission` 的人员也可在异常处置会话中依据具体现场事实确认隔离；软件或网络异常本身仍不能证明单车故障。
4. **疑似阻断凭正面证据自动解除。** 车辆与 RIoT 通信及状态须满足统一的新鲜度和连续稳定门槛，不存在硬故障证据，并完成订单、位置、运动、急停、仓门、货物绑定和未结调用的无冲突对账；任一事实未知时不能人工强解。具体轮询次数和时间窗口由[决定监控新鲜度、告警升级、重试与日志留存规则](73-decide-monitoring-freshness-alert-retry-and-log-retention.md)统一确定。
5. **进入阻断先保护当前订单。** 进入疑似阻断或正式隔离时立即禁止新派车，并对本项目当前执行订单自动调用 `OrderHold`、回查 `HELD`；不得以 Cancel 代替停车。OrderHold 不可用、结果未知或车辆未及时停下时保持全部阻断并形成高优先级事件，具体 EmergencyStop 升级交由[决定安全联锁失败升级与紧急停止边界](70-decide-safety-interlock-failure-escalation-and-emergency-stop.md)统一决定；实际停车得到证明前不得显示为已安全停稳。
6. **所有故障与被困货物处理统一为一次身份验证的异常处置会话。** `ExceptionRecoverySession` 绑定一个处置人员、一个事件、一台车辆及固定任务/仓位范围；会话内可以连续选择修复续行、受控取货、强制机械取出、实物交接和车辆恢复，不使用审批工单、双人复核或逐动作二次认证。人员交接、切换车辆、扩大仓位范围、主动退出或事件关闭时会话结束；短暂断线后仅在同一人员、同一事件且会话仍有效时完成最新对账后继续，断线期间不得扩围。空闲超时由统一登录权限规则配置。
7. **异常处置权限按个人授予。** 初始授予 R-09 班组长、R-11 设备/电气维护人员和 R-13 AGV 运维/调度管理员，也可显式授予经过现场培训的生产人员而不改变岗位角色。任何持权人验证个人身份后均可独立完成系统内整套异常处置；无权限人员可以报告、协助或接管货物但不能操作异常处置界面。断电、抱闸和强制机械开锁仍由具备相应现场作业资质者实际执行并记录，作业资质不是系统审批步骤。
8. **普通放错只在离站前纠正。** 8005 是一对一输送；车辆离开起点前保留既有 `LoadCorrection` 调整机会，离站后将错就错完成输送，不再换仓、重绑、重开任务、生成补运或修改 MES，最多记录差错事实。`ExceptionRecoverySession` 不处理普通存放错误，只处理仓内传感器/锁故障、车辆本体故障及实际被困货物。
9. **未装货任务保留原需求身份重新派车。** 只有原 RIoT 订单已明确终结、没有已提交 `LoadBatch`、没有活动或结果未知的仓位操作，且服务端可证明车上没有该任务产品时，才解除原车辆绑定并让原 `DemandId` 重新进入正常派车；该处理不是业务取消，不写 `TransportDemandSuppression`，也不创建新需求实例。任一事实未知时继续绑定原故障车并阻断。
10. **已装货任务保持原绑定并采用双路径。** 已有装货事实或货物状态未知时进入 `FaultedVehicleCargoHold`，保留原车辆、DemandId、仓位、产品和订单绑定，只允许修复后由原车继续，或在异常处置会话中取出、交接并终止；超时只告警，不自动选择路径或取消。路径选择不是审批：货物仍完整保留于原仓位并重新安全闭环时可改走修复续行；一旦产品已经从原绑定中取出并交接，对应 DemandId 必须完成取货终止闭环。
11. **修复续行保持同一业务任务与载货车辆。** 原 RIoT 订单仍确认为 HELD 且身份、目标和货物绑定一致时走既有 `OrderContinue` 授权及回查；原订单已明确终结、相关阻断收敛且 8005 任务未终止时，可重新通过正常派车和安全门禁后为同一 DemandId 建立关联旧订单的新 RIoT 移动单。订单结果未知、身份/位置/路线冲突时继续载货保全，不得换号绕过或改派；已经形成补救终止记录的任务不得复活。
12. **正常受控取货以电子物理闭环和具名交接终止业务。** 车辆须已停稳且当前订单已 HELD/终结；结果未知时先完成断电、抱闸或等效机械隔离。全部目标仓位达到 `EMPTY + 锁闭 + 开锁输出复位` 且产品由具名人员接管后，系统自动形成 `FaultCargoRecoveryRecord`，与原任务 `TERMINATED_BY_FAULT_CARGO_HANDOFF` 终态及对应 TransportDemandKey 的永久抑制原子提交。记录只证明取出和责任交接，不表示 8005 或 MES 已完成运输；8005 不创建人工运输任务、不等待 MES 扫码。
13. **极端锁和传感器故障使用强制机械取出。** 锁无法经 DO 弹开，或锁反馈、光幕、占用检测等事实已经失真时，先完成断电、抱闸或等效机械隔离，再由具备现场作业资质者以应急机械方式开锁或拆卸；系统停止重复发送开锁 DO，将可证明的受影响仓位或 IO 模块标为物理状态未知，影响范围无法确定时隔离整车。
14. **货物业务闭环与设备恢复闭环分离。** 强制取出后，产品身份明确且完成具名交接时，`ForcedCargoHandoffRecord` 可以证明货物已经救出和责任交接，允许终止对应任务并写入抑制，但不能证明电子空仓、仓门安全或车辆恢复。产品身份不明时只记录实物，相关任务保持待盘点，不猜测绑定。
15. **强制处置建立新的恢复代次。** `ForcedRecoveryGeneration` 使此前全部仓位命令、传感器快照、执行检查点和迟到结果失去更新当前状态的资格，只保留为历史证据。车辆恢复前必须修复锁、DO、锁反馈、光幕和相关 IO，完成空仓/有物、锁闭、输出复位测试，并在新代次内重新核验位置、订单、运动、急停、仓门、货物绑定和未结操作；全部无未知或冲突后系统才解除隔离。恢复不需要第二角色批准或再次认证，只恢复资格评估，不自动继续订单或派发任务。

以上规则以身份验证和系统安全门禁取代审批链：人工身份能够及时发起和连续完成处置，但不能覆盖仍然未知或冲突的安全事实；物理隔离允许必要救援，却不等同于设备已经恢复。
