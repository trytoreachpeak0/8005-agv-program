# RIoT 环境与接口快照用户确认（2026-08-03）

## 证据身份

- 来源：本次 Codex 对话中的用户直接答复。
- 日期：2026-08-03。
- 确认者：用户本人；依据“确定当前基线的最终批准人”，其为本次当前需求基线默认且唯一的最终批准人。
- 原始答复保存在本次对话记录；本文件是结构化上下文指针，不声称存在另一个现场原始文件。
- 本确认只绑定环境、快照来源与适用范围，不批准任一 API 的调用权限、鉴权方式或运行时语义。

## 已明确确认的 8005 环境

- `172.19.206.222` 是 8005 项目真实运行时需要访问的 RIoT 实例。
- 所有者角色为软件人员和实施人员；本次答复没有提供具名个人。
- 有效期为永久，但只可从现场网络环境访问。
- RIoT 版本为 `v2.2.0.14`。

## 已明确确认的另一项目测试环境

- 这是另一个项目的 RIoT 实例，但曾被用作 8005 的 RIoT API 测试环境。
- 有效期为永久；访问需要接入对应网络环境，例如 VPN 或 5G 模块。
- 它与 8005 现场实例是不同现场的不同实例。
- 准确地址为 `172.10.1.72`。
- 准确 build 为 `2.2.0.30`。
- 所有者角色为软件人员和实施人员；本次答复没有提供具名个人。

## 已关闭的记录歧义

用户初次答复把测试环境地址写为 `172.19.1.72`、build 写为 `v2.2.014`；仓库既有 SDK、行为实验和网络配置材料反复记录 `172.10.1.72`，Round 1 又把实验平台版本事后补记为 `v2.2.0.30`。

用户随后在同次对话中明确修正：准确地址为 `172.10.1.72`，准确 build 为 `2.2.0.30`。本记录保留初次答复及修正链，不再把 `172.19.1.72` 或 `v2.2.014` 作为环境事实。

## 已确认的 OpenAPI 快照来源

- 现存 `rcs/riot_swagger/` 下 14 份原始 Swagger/OpenAPI 文件由用户本人于 2026 年 7 月初手动导出。
- 导出源为 `172.19.206.222`，即 8005 真实运行时 RIoT 实例。
- 该快照适用于 8005 项目。
- 用户在“决定 RIoT 项目 API 白名单与调用安全边界”的同日对话中最终澄清：这批 OpenAPI 的源 build 为 `v2.2.0.14`；同一套 OpenAPI 客户端后来用于 `2.2.0.30` 环境的 riot-lab 行为测试，测试环境 build 不改变快照来源。
- 精确导出日、逐接口导出 URL/操作步骤和当时使用的网络入口未被记录；这些缺失必须显式保留，不由文件时间或 Git 历史代填。

## OpenAPI 源 build 修正链

用户在同日梳理 API 白名单时一度把“OpenAPI 来源”和“riot-lab 测试所用 RIoT build”混写为 `2.2.0.30`，随后明确纠正：OpenAPI 是 `v2.2.0.14`，只是被放到 `2.2.0.30` 环境下测试。本记录保留这次误解与最终澄清链，最终事实以本段为准。

## 秘密边界

本确认不记录也不批准任何用户名、密码、token、CallApiKey、cookie 或私钥。现场网络可达性、VPN/5G 接入条件和实例地址不构成应用调用授权；后续仍须独立批准 API 白名单及调用安全边界。
