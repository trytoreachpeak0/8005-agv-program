# 批次 2 实施票据

范围来自 [`8005-full-product/full-product-scope-and-sequence-specification.md`](../8005-full-product/full-product-scope-and-sequence-specification.md)
的批次 2：**14 条需求**（`FP-C11` 10 ＋ `FP-C13` 3 ＋ `REQ-0207` 1）＋ **四个无条目载体的
大件**（B2 多车、`RouteGraphSnapshot` 引擎、RIoT 订单命令面、白名单架构测试）＋ **轨 A
全部**（v2 两端实现、trait 重打标、`FP-IS-00`～`07` 重证）＋ **三项待办**（跨仓桌面锁、
假 RIoT 扩能力面、白名单提升为产品文档）。

规格本身不产代码；这里的 19 张票是它的实施图（后来又加了票 20～24，批次归属未定；**2026-09-14 用户裁定归批次 2**）。

---

## ✅ 批次 2 已完成（2026-09-09，用户宣告）

两轨出口都达成：**轨 B**（票 18）2026-09-08 `resolved`，八条验收全过；
**轨 A**（票 17）2026-09-09 `done`，九条验收全勾。
执行记录与它同规格 8.3 表格原文的差异，写在
[规格 8.3 的「执行记录」小节](../8005-full-product/full-product-scope-and-sequence-specification.md)。

🔴 **完成不等于下面这些事被解决了，读结论请连着看：**

1. **`G3` 不是八片。** `FP-IS-00`／`04`／`05`／`06` 有 G3 面且 `formalSlicePass=true`；
   **`FP-IS-01`／`02`／`03`／`07` 本批次没有 G3 面**，用户裁定如实记录、不补场景，不发证据。
   规格 8.3 表格原文写的是「八片全 PASS」，宣告完成即接受这处差异。
2. **`FP-IS-04`／`05` 的通过依赖一条已知豁免**（现场库历史那一项，票 24 已拆成如实记录，
   `matchesBoundProtocolCommit` 照实为 `false`）。**真正的解是重采一次 v2 现场运行**，未做。
3. **W1 的空载急停演练未做**（票 19，仍 `ready-for-agent`）。不阻塞——票 19 逐字写着不要因它
   挂起批次 2，且 W1 门槛是批次 3 代码就绪。**但它仍是一件欠账，随 W1 窗口补齐。**
4. **八份 `ONBOARD_HMI_G2` 的 `g1Status` 仍是 `SKIPPED`**（用户裁定不重出）。
5. **不含 RC。** 三份 G3 `run-result.json` 里 `fullG3` 与 `releaseCandidate` 仍是
   `INCONCLUSIVE`；打 RC 要两名产品负责人的 attestation，`protocol-v1.0.0` 至今没打 tag。
6. **票 20／21／22／23／24 的批次归属仍未定**——它们不在规格的批次 2 范围里。
7. **四个仓截至宣告时全部未推。**

**2026-09-13 补记：在已发布的 `protocol-v1.0.0` 上复核过，两轨出口仍然成立。**逐项证据见
[规格 8.3 执行记录下的「补记」小节](../8005-full-product/full-product-scope-and-sequence-specification.md)。
上面七条的现状随之变成：

- 第 1、2、6 条不变。第 2 条在 v1.0.0 这一轮仍如实记 `matchesBoundProtocolCommit=false`。
- 第 3 条：W1 窗口 2026-09-13 开过，但做在生产的 v0.3.0 库上，急停代码只在 v2 线，演练仍未做。
  用户同日定：**票 19 改挂到生产切到 v2 线的那次现场窗口**，与 `8005-agv-control-server#44` 同批。
- 第 4 条**已解决**：v1.0.0 这一轮车载端 G2 八片的 `g1Status` 都是 `PASS`。
- 第 5 条仍成立：`protocol-v1.0.0` 已于 2026-09-12 发布，但 `fullG3` 与 `releaseCandidate` 仍是
  `INCONCLUSIVE`，批次 2 仍不含 RC。
- 第 7 条：服务端、车载端、协议的分支都已推送；轨 B 在 v1.0.0 上的 CI 证据 2026-09-13 入库
  （control-server `65bffc0c`）。本仓的批次 2 记录（`fp/b1-close`）与票 02、票 04 的交付
  （`fp/batch-2`：白名单文档、需求基线 `v1.1.0`）同日推送并开 PR 进 `main`。

**2026-09-14 补记：批次 2 收尾，轨 A 的 `G3` 补齐八片，本批次关闭。**用户 2026-09-13 要求补完四片 G3 面、勾完规格 8.3 表后关闭批次 2。
逐项证据见[规格 8.3 执行记录下 2026-09-14 的补记](../8005-full-product/full-product-scope-and-sequence-specification.md)。上面七条的现状：

- 第 1 条**已解决**：`FP-IS-01`／`02`／`03`／`07` 在新的 journey runner 上 `formalSlicePass=true`，八片都有 G3 面，
  `slicesWithoutSurfaceThisBatch` 为空。两道 G2 十片也在新提交上重出（服务端 `052759bc`，车载端 `8d19fee`），全部 `PASS`。
- 第 2 条不变。
- 第 3 条：急停演练仍未做。用户 2026-09-14 确认**维持挂到生产切 v2 的现场窗口**，它是批次 2 唯一的欠账。
- 第 4 条已解决（不变）。第 5、6 条不变。
- 第 7 条：服务端 `fp/b2-close`、车载端 `w2g/b3-on-v2` 的收尾提交与证据都已推送，并分别合入两端 v2 长线
  （服务端 `fp/v2-impl` 快进到 `7327ef3a`；车载端 `w2g/fp-v2-impl` 经 PR #59 合并为 `8dee1c3f`）。
  轨 B 在新代码上重跑了 CI：L2 26 次全 `PASS`，三个出口场景与急停单发各 3/3，逐场景证据已入库；单元测试 722 passed。见规格那节补记的轨 B 两行。

**2026-09-14 补记二：悬着的裁定逐条了结，零散遗留转成 issue。**批次关闭后用户要求把票里挂着的「待用户裁定」清干净。

用户当日裁定：

- **票 20～24 归批次 2。**它们是票 17 的前置，在批次 2 期间完成。上面第 6 条随之了结。
- **票 16／20／22 的测试形态偏离认可现状。**验收里的「同一条测试」按「同一个架构测试类」理解；两端落成同一类里查缺、查伪两个 `[Fact]`，票 22 的两个方向合成一条断言，都不是偏离，代码不动。
- **`8005-agv-control-server#36`（恢复会话开着时重启车载端，重连后恢复入口还在不在）在 v2 线补一条真车载端 L2。**
  场景 `real-onboard-restart-with-open-recovery-session` 首跑 4/8 红：服务端重放开着的会话这一半是好的，但车载端「申请恢复」被拒后
  复用被拒动作的 messageId，「补偿清空」被服务端判内容冲突断开，入口永久失效（缺陷单 `docs/defects/20260914-onboard-reuses-rejected-recovery-action-id.md`）。
  MVP 线 `ab346ed` 早已修过；按用户裁定移植为车载端 `w2g/b3-on-v2@372186f`（十片 G2 全过，证据 `19a740c`），
  随后在已提交的服务端 `8bf65925` 与车载端 `19a740c` 上 **8/8 PASS**，红、绿两份证据都在服务端 `evidence/l2/20260914-real-onboard-restart-with-open-recovery-session-00{1,4}/`。

已被后来的进展覆盖、不再待裁定的（逐条核过）：

| 原待裁定项 | 出处 | 现状 |
| --- | --- | --- |
| 票 20 是否阻塞票 17 | 15-answer 第十五节 | 票 17 已于 2026-09-09 done，不再成立 |
| `8005-agv-program` 要不要开 PR | 15-answer 第十五节 | 批次 2 记录已经 PR #57／#58／#59／#60 合进 `main` |
| `CLAUDE.md` 第 19 行说车载端仓对 agent 只读 | 15-answer 第十五节第 4 条、票 20 第 10 条验收 | `main` 上第 19 行已写明 `8005-agv-onboard-hmi` 与 `slots-simulator` 在 `w2g/*` 分支可写 |
| 需求线路 G3 那条身份断言怎么办 | 17-answer 第七节第 2 条 | 票 24 拆开，第 7 项改为如实记录 |
| 八份 `ONBOARD_HMI_G2` 要不要重出 | 17-answer 第七节第 3 条 | 2026-09-13、09-14 两轮都已重出，`g1Status` 为 `PASS` |
| 票 17 第三条验收认到哪一级 | 23-answer 第七节第 4 条 | 用户 2026-09-09 定分级口径；2026-09-14 八片均 `formalSlicePass=true` |

转出清单（每条写明了来源与 2026-09-14 实测现状）：

| 遗留 | 出处 | issue |
| --- | --- | --- |
| 接收环里 9 个 `O_TO_C` 方向的 `case` 标签 | 15-answer 第五节 | `8005-agv-onboard-hmi#60` |
| 入站校验比 v2 schema 严的三处业务收窄（批次 4 的六类任务首先会打到） | 15-answer 第九节 | `8005-agv-onboard-hmi#61` |
| `OPERATOR_TIMEOUT`：`protocol-v0.3.0` 有、`protocol-v1.0.0` 没有 | 15-answer 第十四节末 | `8005-agv-protocol#10` |
| G2 的 `gate-result.json` 要不要补 `assuranceLevel` | 23-answer 第七节第 2 条 | `8005-agv-control-server#58` |
| 车载端 `run-staged-g3-recovery-ack-drop.ps1` 仍写字面切片常量 | 23-answer 第七节第 3 条 | `8005-agv-onboard-hmi#62` |
| 服务端 `dotnet format` 空白问题（4 个文件 8 处涨到 11 个文件 37 处） | 17-answer 第七节第 4 条 | `8005-agv-control-server#59` |
| G3 断言归属表待切片家族负责人复核 | 23-answer 第三节、第七节第 1 条 | `8005-agv-control-server#60` |

**同日查出一个比上面七条都大的缺口，另开总票 `8005-agv-program#61`**：MVP 线与 v2 线 2026-09-04 分叉后，MVP 线真车与现场窗口跑出来的修复几乎都没带进 v2 线
（两端 68 个改动 `src/` 的 MVP 独有提交，已逐条分类：车载端需移植 13、服务端需移植 8，另有 25 条等用户回答 Q1～Q6）。
它是在补 #36 那条场景时暴露的（见下），用户裁定逐条核对后再按组移植，**作为生产切到 v2 线的前置**，与票 19、`8005-agv-control-server#44` 同属切换前后的待办。
其中 Q1 直接决定切换会不会让生产退回一趟一单、无自动充电：这两项 MVP 已在生产使用，在全产品规格里排在批次 6～8。

**2026-09-14 补记三：票 19 改做缩减版演练，用备用车 agv02，不再等切 v2；工具已就绪，演练搁置。**

- 原演练要 RIoT 把一张在途单报成 FAILED 来触发 v2 的自动急停。用户确认 FAILED 只会因地图或系统内部配置问题出现，管理员也造不出来，
  所以原卡点无解。这条触发要不要保留另开 `8005-agv-program#63` 待定。
- 用户同日定长期规则：v2 的完整测试与实车运行用 agv02／agv03 隔离做，不干扰 agv01 上的 MVP。演练因此不必等生产切 v2。
- 缩减版由服务端 `tools/ControlServer.EmergencyDrill`（`fp/b2-close@194a27bc`）对空载、行驶中的 agv02 直接发一次急停，
  量 RIoT 停车与闩锁，然后先取消演练单、再解除急停。「只发一次」与「组合证据判停稳」仍以 L2 为准。
  白名单 1.5 节的破例用户已批准，只限这次、只限 agv02，条件写在票 19。
- 工具对假 RIoT 自测 47/47，尚未对真车发写调用。只读摸底显示 agv02 未联网、在 `xinji` 地图、闩锁 `CAN_NOT_RECOVER`，
  用户当晚不在现场，**演练搁置**，等 agv02 恢复后再逐条授权执行。上面第 3 条的欠账状态不变。

## 依赖图

```
P 波（零前置，六件事同时开工）
  01 桌面锁     02 白名单文档     03 假 RIoT 扩能力面
  04 CP-0001 批准（用户）        05 决策链 prefactor    06 持久化地基
                    │                    │                  │
        ┌───────────┼──────────┐         └────────┬─────────┘
        │           │          │                  │
Q 波   07 SDK      08 白名单   │        ┌─────────┼──────────┬─────────┐
      imap Facade  架构测试    │        │         │          │         │
        │  ←04     ←02         │       09 B2    10 命令面   13 FP-C13  │
        │                      │      多车        │                    │
        └──────────┬───────────┘                 11 故障隔离           │
                   │                                                   │
                  12 引擎（含 REQ-0207）  ←04 ←06 ←07 ←05 ──────────────┘
                   │
S 波（与 Q 波全程并行，前置是批次 1）
  14 服务端 v2 ＋ trait 重打标 ✅   15 车载端 v2（长期分支）✅   16 vectorId 守卫 ✅
                   └────────────────┬──────────────────┘
                                   17 FP-IS-00～07 重证   ← 轨 A 出口 ✅ **2026-09-09 done**
                                      **2026-09-09 第二轮：两道 G2 各八片全 PASS 且都已入库，
                                      G1 实测通过，G3 三个 runner 两过一败。**
                                      八片仍未通过——**第三条验收用现有 G3 runner
                                      结构上做不出来**：三个 runner 把切片结论写成
                                      字面常量（只名四片、全 INCONCLUSIVE），
                                      且都只发 run-result.json。见 17-answer.md 第六节。
                                      **2026-09-09 第三轮：第三条验收显式阻塞在票 23。**
                                      **第四轮：票 23 已 done，工程卡点解除；第三条按分级口径
                                      如实改写但仍不勾。用户随后裁定 FP-IS-04／05 那条断言
                                      为已知豁免（范围窄到第 7 项合取），口径问题解决。**
                                      **用户随后定「先拆再跑」，授权改门禁断言 ＋ 出证：
                                      票 24 拆断言，三个 runner 真跑全过，六份 gate-result.json
                                      入库（1f25f6e）。第三条勾上，九条验收全满，本票 done。**
                                      🔴 **出口带四条限定**：四片本批次无 G3 面（如实记在案，
                                      不发证据，所以出口不是「八片都过了 G3」）；FP-IS-04／05
                                      的通过以那条现场库历史豁免为前提；八份 ONBOARD_HMI_G2 的
                                      g1Status 仍是 SKIPPED；fullG3 与 releaseCandidate 仍
                                      INCONCLUSIVE，出口不含 RC。见 17-answer.md 第七节
                                      与 24-answer.md。

  20 车载端 vectorId 守卫（长期分支）  ←15 ✅
     票 16 的车载端另一半。**是否阻塞票 17 未定**——它不在规格的批次 2 范围里，
     由用户决定归批次 2 还是批次 3。画在这里只表示它的前置是票 15。**2026-09-14 用户裁定归批次 2。**

  21 车载端 FP-IS-07 两个向量缺口的关闭  ←20 ✅
     票 20 的守卫把 CV-FAULT-CARGO-HANDOFF 与 CV-FORCED-MECHANICAL-RECOVERY 报成欠账。
     **它是票 17 的输入**：不关掉就不能声称 FP-IS-07 重证通过。用户 2026-09-09 定单开一张
     票并补产品代码。与票 20 同样不在规格的批次 2 范围里，归属未定。**2026-09-14 用户裁定归批次 2。**

  22 车载端 IntegrationSlice trait 与 run-w2g-g2.ps1 的 -Slice  ←21
     **同样是票 17 的输入**，交接文档里的「障碍 2」：ONBOARD_HMI_G2 至今不按切片过滤，
     八片共享同一个结论，票 17 的「八份 gate-result.json」勾不上。车载端 IntegrationSlice
     trait 为 0 处，而 58 处 ProtocolVector 已覆盖八片全部 20 条向量——是投影，不是补测试。
     用户 2026-09-09 定单开一张票。归属与票 20／21 一样未定。**2026-09-14 用户裁定归批次 2。**

  23 G3 的按片出证与分级状态  ←17（是它的阻塞项，不是它的下游）✅
     **票 17 第三条验收的唯一出路。**三个 G3 runner 把切片结论写成字面常量、且都只发
     run-result.json；全仓只有 test-wire-to-gate.ps1 发 gate-result.json，而它第 4 行是
     [ValidateSet('G2')]。用户 2026-09-09 定单开一张票，并**引入分级状态**——把
     status（断言过没过）与 assuranceLevel（实际动了什么）拆成两个字段，
     formalSlicePass 由此算出而不再是字面 $false。
     ⚠️ **另查出 FP-IS-01／02／03／07 在四个 G3 runner 里一次都不出现**，
     八份证据不是改个出证路径就有的。归属与票 20／21／22 一样未定。**2026-09-14 用户裁定归批次 2。**
     **2026-09-09 完成**（控制端 a46101e）：三个 runner 真跑，六份 gate-result.json；
     FP-IS-00／06 通过，FP-IS-04／05 失败在一条关于现场库历史的已知断言上。
     🔴 **票 17 第三条验收随之从工程问题变成口径问题**，见 17-answer.md 第七节第 1 条。
     **第四轮已把该条按分级口径改写、保持未勾**；本票到此为止，不再是任何东西的阻塞项。

  24 demand-bearing G3 那条身份断言的拆分  ←23（票 17 第三条的收口路径）✅
     `protocolAndBuildIdentityBoundToTheSharedBinding` 是**七项合取**，只有第 7 项问的是
     被恢复的现场库的历史（2026-08-29 那次现场运行绑 protocol-v0.1.1，任何 v2 身份的运行
     对它都过不了）。用户 2026-09-09 裁定该项为已知豁免，并授权「先拆再跑」——**这是本线
     第一次获准改门禁断言**。前六项仍是断言，第 7 项改为 fieldStoreProvenance 如实记录，
     matchesBoundProtocolCommit 照实写 false。断言名不增不减，所以票 23 那张归属表不动。
     **2026-09-09 完成**（控制端 b46b072）：三个 runner 真跑全过（19／19、20／20、20／20），
     六份 gate-result.json 全 PASS＋formalSlicePass=true，入库 1f25f6e。
     🔴 **最要紧的一条：这不是把 FAIL 改绿。** 拆开之后前六项作为断言全过、第 7 项量出来
     仍是 false，两件事同时成立——**当初「挂的只是现场库那一项」是推断，现在才是实测**。

  票 15／20 的交付形态在 2026-09-09 由用户改定为**推长期分支，不开 PR**：
  `OnboardHmi_MVP` 已钉到已发布的 `protocol-v0.3.0`，PR 进去等于覆盖已签发布。
  做法对齐控制端的 `origin/fp/v2-impl`。见 issues/15-answer.md 第十四节。

T 波
  18 轨 B 出口证据（3 车 L2 三连绿）  ←08 ←09 ←11 ←12 ←13   ← 轨 B 出口
                   │
                  19 W1 空载急停演练  ←18 ←11 ＋ W1 窗口开启
                     2026-09-14 改做缩减版：用备用车 agv02，不再等窗口；卡在 agv02 离线，见开头补记三。
```

**关键路径**：`04 → 07 → 12 → 18 → 19`。票 04 是用户批准动作，越早越好。

**严格串行的只有三段**：`06 → 09/13`、`10 → 11`、`07 → 12`。其余全可并行。

## 并行波次

| 波 | 可同时开工 | 说明 |
| --- | --- | --- |
| P | 01 02 03 04 05 06 | 六件事零冲突，04 是用户动作 |
| Q | 07 08 09 10 13（＋批次 1 到位后 14 15 16） | 最宽处 5～8 条并行 |
| Q′ | 11 12 | 11 等 10，12 等 07 |
| T | 18 → 19 | 出口，串行 |

## 冲突边界（并行前提，不可违反）

| 文件／区域 | 独占者 | 其余票的约束 |
| --- | --- | --- |
| 派车主循环（候选发现与准入段） | **票 05** | 只加判据文件 ＋ 注册表一行，不重构 |
| `ControlServerDbContext`、Migrations | **票 06** | 零 migration |
| `Ports.cs` | **票 06** | 票 05 的新接口放独立新文件 |
| `tools/` 合成装置 | **票 03** | 不动 `src/`、`tests/` 产品代码 |
| `riot-sdk` | **票 07** | 服务端只消费，不改 SDK |

**两个 prefactor（票 05、票 06）存在的唯一理由是消除并行冲突。** 实测热点：EF 模型快照是
单文件，两条能力轨各加一个 migration 必然冲突；派车引擎的候选发现与准入段被 B2、引擎、
`FP-C13`、`REQ-0207` 四方争抢。不集中处理就没有并行。

## 两条排期提示

- **轨 A 的重证（票 17）建议放在一个连续窗口内跑完**，窗口内轨 B 暂缓合并——G2 与 G3 证据
  绑定精确 commit，边跑边合会让证据的 commit 绑定不断变化。或等票 09～13 合完再跑。
- **票 19 不由批次 2 自己决定何时开工**——W1 窗口的门槛是批次 3 代码就绪。批次 2 的其余
  全部工作都不占现场窗口，不要因为这张票把整个批次 2 挂起。

## 出口

批次 2 完成 = **轨 A 出口（票 17）＋ 轨 B 出口（票 18）都达成**。两轨出口独立，轨 B 不阻塞
轨 A，但两轨都完成才算批次 2 完成（规格 8.3）。票 19 的现场演练随 W1 窗口补齐
（2026-09-13 起改为随生产切到 v2 线的现场窗口补齐；2026-09-14 晚又改为用备用车 agv02 做缩减版，
等车恢复即可做，见开头那一节的补记三）。

**✅ 2026-09-09：两轨出口都已达成，用户当日宣告批次 2 完成。**
轨 B（票 18）2026-09-08 `resolved`；轨 A（票 17）2026-09-09 `done`。
**完成带着七条限定，逐条写在本文开头那一节**——尤其是「`G3` 不是八片」与
「`FP-IS-04`／`05` 依赖一条已知豁免」这两条，它们是规格 8.3 表格原文与实际出口的差异所在。
