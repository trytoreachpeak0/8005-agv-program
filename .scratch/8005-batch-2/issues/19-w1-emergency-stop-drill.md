# 19 — W1 窗口的空载急停演练

**做什么：** 在现场做一次**空载**受控急停演练，证明 8005 发出的急停在 RIoT 侧真的让车停
下来，而且 8005 只发了一次调用。这是批次 2 唯一占用现场窗口的工作。

出口判据（规格 8.4 的 W1 行）：**RIoT 侧确实停车，且 8005 侧只发一次调用。**

**窗口不由本批次开启。** W1 的门槛写的是「批次 3 代码就绪」，窗口内的主要内容是三台车逐台
逐仓的 IO 核对（属批次 3）。急停演练**搭这个窗口做**——所以这张票的实际开工时间取决于 W1
何时开，不取决于批次 2 自己完成得多快。**批次 2 的其余全部工作都不占现场窗口**，不要因为
这张票而把整个批次 2 的完成挂起。

**W1 的顺序不得倒置：先核对，后启用门禁**（规格 3.5）。急停演练排在窗口内的哪一步，跟着
窗口的整体安排走。

**空载**是硬要求，不是谨慎——演练要在没有货、没有产品在仓的状态下做。

证据落 `evidence/field/<日期>-W1-<描述>/`，形状与 L2 相同 ＋ 现场记录与照片指针。
**新增 `evidence/field/` 而不复用 `evidence/g3/`**：G3 是门禁，现场窗口不是门禁，同目录会
让「切片通过」与「现场验收通过」混淆。

**前置：** 票 18（轨 B 出口证据——机制先在 L2 上证明，再上现场）、票 11（故障隔离与停稳
判定），以及 W1 窗口开启。

**状态：** ready-for-agent —— **卡在两处，见 `19-answer.md`（2026-09-08 开工前判定）**：
① W1 窗口未开（门槛是批次 3 代码就绪）；② 演练的唯一触发路径要求 RIoT 侧把一张在途单置为
终态 FAILED，而 8005 的 RIoT 调用白名单里**没有任何调用能做到**，必须由对端的人操作——
这件事本票原文没写。**批次 2 的完成不因本票挂起。**

**2026-09-09：批次 2 已由用户宣告完成，本票状态不变，仍是 `ready-for-agent`。**
它作为轨 B 现场栏（规格 8.3 那格的「W1 的空载急停演练」）的一件**欠账**留着，
随 W1 窗口补齐——上面两处卡点一处都没解，尤其第②处要对端的人在 RIoT 侧操作，
不是我方能单方面推进的。批次完成的限定清单见
[`../README.md`](../README.md) 开头那一节第 3 条。

**2026-09-13：W1 窗口开过了，演练仍未做；本票改挂到生产切到 v2 线的那次现场窗口（用户定）。**
W1 为了不等切 v2，做在生产现有的 v0.3.0 库上（逐车逐仓 IO 核对 PASS，证据在
`8005-agv-control-server` 分支 `feat/w1-unattended` 的 `evidence/field/20260913-W1-three-vehicle-qualification/`）。
急停代码（`VehicleFaultCoordinator`、`EmergencyStopSupervisor`）只在 v2 线，生产的 MVP 线一处都没有，
所以那次窗口里本票无从做起。状态仍是 `ready-for-agent`，卡点改为两条：
① 生产跑上 v2 线；② 上面第②处没变，RIoT 侧要有人能把一张在途单置为终态 FAILED。
它与 `8005-agv-control-server#44`（切 v2 前对齐光幕极性常量）同属切 v2 前后的待办。
复核记录见规格 8.3 执行记录下的「补记」小节。

一条演练设计上的更正：要造的是「车读不出停住」，不是「单失败了」——`RequiresEscalation` 会把
一台停稳（读数确证未移动、位置已知、证据新鲜）的车挡在急停之外。**空载不等于静止**，让空载车
在途行驶即可自然满足这一条，不需要动传感器。

- [ ] 演练在空载状态下进行，车上无货且无产品在仓
- [ ] 8005 发出急停后，RIoT 侧确实停车，停车事实由现场观察与 RIoT 侧证据双向确认
- [ ] 8005 侧**只发一次**急停调用——调用记录逐条可查，重试逻辑未产生重复发令
- [ ] 停稳判定走的是组合证据（`REQ-0247`），不是任何单项证据
- [ ] 证据落 `evidence/field/<日期>-W1-<描述>/`，形状与 L2 相同
- [ ] 现场记录含参与人、时刻与照片指针
- [ ] 证据目录只增不改；演练失败时证据保留，不被重跑覆盖
