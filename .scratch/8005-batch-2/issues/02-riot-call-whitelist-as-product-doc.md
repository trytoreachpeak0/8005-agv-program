# 02 — RIoT 调用白名单提升为产品文档，补上第二种建单形态

**做什么：** 把 RIoT 调用白名单从一张工作票据变成有归属、有维护者的产品文档，并补齐它
当前缺的第二种建单形态——充电订单的 `move + act(78,1)`。做完之后，任何人问「产品代码
可以调 RIoT 的哪些端点」，答案在一份正式文档里，而不是另一张地图的 scratch 票据里。

来源是完整产品规格第 14 节待办第 6 项（标注「批次 2 前」）与第 16 节自行定案第 5 项。
规格明确把「放哪、谁维护、与 `REQ-0294` 的引用怎么改」留给实施图决定，这张票就是决定
它们的地方。

文档要能承载后续两项引用：票 08 的架构测试要拿它当断言的清单来源；`CP-0001` 批准后
增列的五个 `imap` 只读端点要能加进去而不产生第二份真相。

**前置：** 无，可立即开工。

**状态：** resolved（2026-09-08）—— 决议见 [02-answer.md](02-answer.md)。文档落在
`8005-agv-program/docs/riot-call-allowlist.md`（26 个端点）。两处偏离：票据 37 不加指针（它被
四条 REQ 按 SHA-256 绑定，改动即失效，目标由哈希绑定本身达成）；五个 `imap` 端点直接写进清单
而不是留位置（票 07 先于本票完成，「增列归票 07」的分工前提已落空）

- [x] 白名单落在 `8005-agv-program` 的正式文档路径下（不是 `.scratch/`），有明确维护者
- [x] 清单逐条可判定：每条是一个具体 HTTP 方法 ＋ 路径，不留「当前 Facade 使用的地图查询」
      这类靠时点歧义承载扩权的开放引用
- [x] 补上充电订单的建单形态 `move + act(78,1)`，与既有的运输建单形态并列
- [x] 写明「产品代码必须走具名 Facade、不得使用 `.Raw`」，并标注它的需求载体是 `REQ-0309`
      同一句里的「只能经已批准的 RIoT Map/Station 只读调用面和具名 Facade 获取目录」
- [x] 与 `REQ-0294` 的引用关系处理干净：要么文档引基线，要么基线引文档，不出现互指
- [~] 原工作票据**不加指针**（它被四条 REQ 按 SHA-256 绑定，事实上已不可编辑）；指针改放
      `CONTEXT.md` 鉴权节，「不留第二份可编辑的真相」由哈希绑定达成
- [~] 五个 `imap` 端点**直接写进清单**而非留位置——票 07 先于本票完成，当时无处可增列，而
      `REQ-0146`（v1.1.0）正文已含这五条，留空位反而会让票 08 一上来就红
