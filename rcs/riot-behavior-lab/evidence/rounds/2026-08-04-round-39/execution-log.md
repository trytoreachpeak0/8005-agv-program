# Round 39 执行日志（2026-08-04）

## 当前状态

`COMPLETED_READ_ONLY_WITH_COVERAGE_GAP`

用户于 2026-08-04 连接实验环境后批准继续；本轮随后执行了 GET-only 探针。没有调用任何写接口，测试车和 QUEUEING 身份集合的前后快照一致。

## 已完成的离线准备

- 新增 Q-040 与 E5，只允许车辆快照、QUEUEING 列表和原因诊断三个 GET 路径。
- 固定测试目标为 `RIOT-CROSS-PROJECT-TEST` / RIoT build `2.2.0.30`；凭据只从本地环境配置读取并在证据中脱敏。
- 预置 Round 13、29、30 的历史测试车订单身份，仅用于探测 `orderKey` 口径；历史场景标签不得冒充实时原因证据。
- 脚本已通过 PowerShell AST 语法检查；静态复核只构造 `HttpMethod.Get`，不含 POST、PUT、PATCH 或 DELETE 请求。

## 执行结果

### 前后状态

- 测试车前后均为 `IDLE`、`processingOrder=false`、`enable=true`、`integrationLevel=ON_LINE`、`movementState=MT_FINISHED`、站点 2。
- 测试车前后均没有 `QUEUEING(1)` 订单；身份集合为空且保持不变。
- 该结果只支持“本轮未观测到业务状态变化”，不能证明服务端内部绝对无副作用。
- 执行后完整性复核发现空集合最初被 PowerShell 管道写为零字节；已将该证据规范化为 JSON `[]`，并修补 `Save-Json` 使用 `-InputObject`。这项记录修正不改变任何现场观测。

### 历史订单与 `orderKey`

- 对 Round 13、29、30 的 8 个历史场景分别尝试字符串 `orderId`、数值记录 id 和 `upperId`，共 24 次。
- 24 次均为 HTTP 200、业务 `code=0`，但 `result.reason` 全部为“订单不存在”，建议说明 RIoT 内存数据与页面数据不对等并要求刷新或搜索订单。
- 未知订单得到同一“订单不存在”；未知车辆得到“车辆不存在”。因此 `code=0` 仅表示诊断接口成功给出诊断结果，不表示车辆或订单存在。
- 历史样本已经不在该诊断依赖的 RIoT 内存数据中，不能还原其创建时的 QUEUEING 原因；三类候选都落入同一缺失分支，所以本轮仍无法判定有效订单要求哪一种 `orderKey`。

### 鉴权、结构与稳定性

- 无鉴权与假鉴权均返回 HTTP 401、业务 `code=401`、“暂未登录或token已经过期”。
- 有效返回顶层字段为 `code/message/msgDetail/result/tid`；`result` 只有 `reason`、`suggestList`，没有服务端观测时间、状态版本、原因码或受控枚举。
- 对同一个缺失订单重复 3 次，`reason` 与两条 `suggestList` 文本一致；这只证明“订单不存在”分支在短窗口内稳定，不能外推到真实 QUEUEING 原因。

## 已确认的诊断映射

| 诊断结果 | 项目处理 |
|---|---|
| 订单不存在 | 结果与本地未终结订单事实冲突；保持下单阻断，刷新并按订单 ID/名称对账，转人工，不自动清理或重建。 |
| 车辆不存在 | 车辆身份或目标环境错误；保持车辆级阻断，刷新设备/机器人清单并转人工。 |
| HTTP/业务 401 | 鉴权失败；保持阻断并走凭据治理，不重试业务动作。 |
| 其它未实测自由文本 | 未知；保留原文并转人工，不执行 `suggestList`。 |

以上映射没有新增任何自动恢复动作。

## 尚未覆盖

- 有效当前 QUEUEING 订单的真实 `orderKey` 口径。
- 车辆距可执行路线过远、车辆状态不可执行、车辆未启用、可恢复软件急停及其它真实原因的 `reason/suggestList`。
- 有效原因在重复调用和状态恢复前后的新鲜度行为。

要覆盖这些项目，必须在测试车上存在对应的实时 QUEUEING 订单。Round 39 的只读授权不允许为了制造样本而建单、禁用车辆或触发急停；后续需要用户在 UI/车端逐场景制造状态，或单独批准有明确善后步骤的受控写实验。
