# Round 40 执行日志（2026-08-04）

## 当前状态

`WAITING_FOR_MANUAL_TOO_FAR_SCENARIO`

用户已授权唯一测试车 **新基测试300c协作1** 上的受控建单、取消、调度上下线、软件急停与解除。脚本必须先通过静态语法、安全范围和目标身份检查，之后方可执行。

静态检查已通过：PowerShell AST 无语法错误；记录调用只包含 GET/POST；写路径仅为已授权的调度上下线、绑定测试车建单、取消、软件急停与解除，并包含异常善后。

## Attempt 1 — 身份门禁前停止

- Windows PowerShell 5.1 将无 BOM 脚本中的中文常量按旧编码解释，导致配置名称与预期名称的精确比较误报不一致。
- 门禁发生在主流程任何写调用之前；`finally` 只执行车辆与订单 GET 快照。
- 回查结果：实时车辆名称仍为“新基测试300c协作1”，`IDLE / ON_LINE / emergencyState=OK`，本车非终态订单为 0。
- Attempt 1 证据保留在 `runs/`，不覆盖。脚本已把中文名称与地图名改为 ASCII 表达的 Unicode 码点构造，Attempt 2 写入 `runs-attempt-2/`。

## Attempt 2 — 两个实时原因场景完成

- S1 调度下线：`OFF_LINE` 后绑定测试车建单保持 `QUEUEING(1)`；字符串 `orderId`、数值 id、`upperId` 均返回“车辆处于调度下线状态”，`code=0`，同一字符串 `orderId` 重复三次文本一致。
- S2 软件急停：`CAN_RECOVER` 后绑定测试车建单保持 `QUEUEING(1)`；三种身份均返回“车辆处于非空闲的状态,不可分配订单”，重复三次文本一致。独立车态同时仍为 `procState=IDLE`，说明诊断里的“非空闲”是更宽泛的不可分配分类，不能等同于公开 `procState`，也没有直接暴露急停来源。
- 两个场景均先把订单取消为 `CANCELLED(2)`，之后分别恢复 `ON_LINE` 和 `emergencyState=OK`。最终车辆 `IDLE / ON_LINE / OK`，本车非终态订单为 0，`safeFinal=true`。
- Attempt 2 证据在 `runs-attempt-2/`。

## Attempt 3 — `orderKey` 判别与取消后新鲜度

只重复 S1 调度下线场景；在实时 QUEUEING 存在时增加随机未知 key，并在取消后重查原三种身份。结果为：随机未知 key 与三种真实身份都返回同一个“车辆处于调度下线状态”；订单取消为 `CANCELLED(2)` 后、车辆仍 OFF_LINE 时，三种真实身份也继续返回该原因。接口在该分支优先返回车辆不可分配原因，无法据此判断 `orderKey` 是否有效，也不随订单取消立即变化。

Attempt 3 已安全结束：车辆 `IDLE / ON_LINE / OK`，本车非终态订单为 0，`safeFinal=true`。证据在 `runs-attempt-3/`。

随后新增 GET-only 的恢复后复查：在车辆已恢复 `ON_LINE / OK / IDLE` 后重查同三种身份和随机 key，输出写入 `runs-attempt-3-post-enable/`。

## 恢复后只读复查结果

- 车辆全程保持 `IDLE / ON_LINE / emergencyState=OK`。
- 已取消订单的字符串 `orderId` 仍被识别，返回“订单是非可调度状态”；其 `suggestList` 建议“重启 RIoT 系统初始化”。8005 不得执行该建议：重启是系统级动作，不在项目白名单，也不能由自由文本授权。
- 同一订单的数值记录 id、`upperId` 和随机 key 均返回“订单不存在”。因此项目调用的 `orderKey` 固定为字符串 `orderId`；其它两种身份不得使用。
- 结合 Attempt 3 可见诊断存在优先级短路：车辆 OFF_LINE 时，任意 key 和已取消订单都先返回车辆下线原因；车辆恢复可分配后才进入订单身份/状态判断。调用方不得把一次原因文本解释为对所有输入层级都已验证。
- 状态新鲜度是“重新计算但按优先级返回”，不是订单事件快照：取消订单后只要车辆仍 OFF_LINE，文本不变；恢复 ON_LINE 后才暴露订单已非可调度或不存在。

## 当前安全映射

| 实测 `reason` | 独立事实 | 处理 |
|---|---|---|
| 车辆处于调度下线状态 | `integrationLevel=OFF_LINE` | 仅当禁用由 8005 自身造成、原因已消除且安全可证明时，才按既有批准恢复 ON_LINE；否则阻断转人工。不得直接执行 `suggestList`。 |
| 车辆处于非空闲的状态,不可分配订单 | 本轮实际为 `procState=IDLE` + `emergencyState=CAN_RECOVER` | 文本是宽泛不可分配类，不能直接映射急停解除。只有独立证明为 8005 自身软件急停且满足既有安全门槛时才可解除；否则阻断转人工。 |
| 订单是非可调度状态 | 字符串 `orderId` 可识别但订单已取消 | 保持阻断并对账订单终态；绝不自动重启 RIoT、重建或换号。 |
| 订单不存在 | key 无效或订单不在内存 | 保持阻断，按字符串 `orderId` 对账并转人工。 |

仍未覆盖：离路线过远的实时诊断文本，以及硬件/其它不可执行状态是否与软件急停共用同一宽泛原因。

## 凭据脱敏复核

完整性检查发现全局订单列表中的 `createdBy` 字符串与本地密码字面值相同。虽然该字段语义是订单创建人而非密码回显，证据仍按更严格规则处理：所有精确匹配均替换为 `<redacted>`，记录器也改为在 JSON 解析和落盘前先替换 CallApiKey 与密码字面值。该脱敏不改变订单身份、状态、诊断原因或本轮结论。
