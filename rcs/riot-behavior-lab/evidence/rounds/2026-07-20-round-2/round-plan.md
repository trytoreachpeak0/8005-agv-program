# Round 2（2026-07-20）

## 1. 研究目标

- 关联问题：`Q-015`
- 本轮要回答：RIoT 网页「调用密钥设置」中的长期密钥，能否在不调用 `admin/login` 的情况下，以 Bearer 方式访问业务只读接口。
- 明确不回答：建单、到站、取消、设备写操作、密钥刷新/权限范围差异、过期密钥行为。

## 2. 环境元数据

- RIoT 基址：现场别名 `lab-primary`（`environment.local.json`；不记录凭据）
- RIoT 平台版本：`v2.2.0.30`（沿用 Round 1 事后补记；本轮未重新核对）
- Swagger 快照/摘要：`sha256:f77dda0c0b85dce7151b1f35ef5f5fb6f2a484ed60b157ee0c167b1b70433d0d`（沿用 Round 1）
- 物模型 `productKey` / 版本：本轮不涉及
- 地图 ID / 版本：本轮仅只读探针，不依赖特定地图
- 测试车别名 / 车型：本轮不涉及写车
- 实验目录 Git 提交：本轮开始时未锁定；以证据目录时间为准
- 本机时间与时区：`Asia/Shanghai`（UTC+8）

## 3. 范围与授权

- 只读实验：`A2`（调用密钥正例 + 无鉴权反例 + 伪造密钥反例；可选设备列表对照）
- 已批准写实验：无
- 明确跳过：A1 重跑、B～H 全部、一切写接口、`admin/login`
- 授权时间与原始表述：2026-07-20，用户提供调用密钥并要求按 lab 模式验证「是否能成功访问 RIOT，不用再每次主动获取 bearer」

一次轮次授权不自动沿用到下一轮。

## 4. 前置状态

- 测试车身份已交叉确认：本轮不依赖
- 在线、定位和自动模式：本轮不依赖
- 当前订单及队列：本轮不依赖
- 起点和候选目的站：本轮不依赖
- 现场安全确认：仅只读，无写操作

## 5. 执行顺序

1. A2 正例：`Bearer callApiKey` → `GET /api/imap/v1/mapInfo/all`
2. A2 对照：`Bearer callApiKey` → `GET /api/device/v1/devices`
3. A2 反例：无 `Authorization` → 同上地图探针
4. A2 反例：伪造 Bearer → 同上地图探针
5. 核对证据文件与执行日志；更新 open-questions / knowledge / fixtures

## 6. 本轮状态表

状态取值：`未执行` / `执行中` / `等待人工` / `通过` / `失败` / `本轮跳过`。

| 类别 | 编号 | 测试项 | 只读/写 | 状态 |
|---|---|---|---|---|
| A 鉴权连通性 | A2 | 调用密钥 → 地图列表（正例） | 只读 | 通过 |
| A 鉴权连通性 | A2 | 调用密钥 → 设备列表（对照） | 只读 | 通过 |
| A 鉴权连通性 | A2 | 无 Authorization（反例） | 只读 | 通过 |
| A 鉴权连通性 | A2 | 伪造密钥（反例） | 只读 | 通过 |

## 7. 收尾确认

- 测试车可用：本轮未触碰
- 无遗留订单：本轮未建单
- 无其他车辆受影响：仅只读
- 凭据已脱敏：是（证据中 Authorization 仅保留预览）
- 未解决异常已登记：无；Q-015 已 `SUPPORTED`
