# Round 1（2026-07-15）

本轮是 RCS Insight 项目的第一轮验证，迁移自最初在 `rcs/riot-sdk/docs/live-test/` 下完成的探索性测试。

## 本轮范围与批准记录

> **2026-07-15**：用户已一次性批准 [`test-catalog.md`](../../test-catalog.md) 里 **E1–E3 / H1–H2** 整轮范围，执行时**不逐条确认**。
> **F1（充电调度）、G1（停靠）本轮暂不测**，标记为跳过，不要自行去测。
> A/B/C/D 为只读摸底，可直接执行。

换一个 AI 会话接手时：以本段为准判断授权边界——已批准的不要重复问，明确跳过的不要擅自执行。

## 执行顺序

延续 [`test-catalog.md`](../../test-catalog.md) §2 依赖顺序图：A/B/C/D（只读）→ E1–E3（写，已批准）→ 完整 E1→E2 对照 → H1/H2（写，已批准）。F1 / G1 本轮跳过。

## 本轮状态表

状态取值：`未执行` / `执行中` / `等待人工` / `通过` / `失败` / `本轮跳过`。

| 类别 | 编号 | 测试项 | 只读/写 | 状态 |
|---|---|---|---|---|
| A 鉴权连通性 | A1 | 登录拿 token | 只读 | 通过 |
| B 设备/车辆状态 | B1 | 设备列表查到测试车+在线状态字段 | 只读 | 未执行 |
| B 设备/车辆状态 | B2 | `getVehicleInfo`：位置/电量/任务状态 | 只读 | 未执行 |
| C 地图/站点/路网 | C1 | 地图列表 `mapInfo/all` | 只读 | 未执行 |
| C 地图/站点/路网 | C2 | 有效站点 `stations/{mapId}` | 只读 | 未执行 |
| C 地图/站点/路网 | C3 | 边/路网 `edges/{mapId}` | 只读 | 未执行 |
| C 地图/站点/路网 | C4 | 地图关系 Map Relation | 只读 | 未执行 |
| D 调度只读 | D1 | 路径成本 `getRouteCostsBy` | 只读 | 未执行 |
| E 移动订单 | E1 | 创建一个移动订单 | 写 | 未执行 |
| E 移动订单 | E2 | 监控订单直到到站 | 只读（依赖 E1） | 未执行 |
| E 移动订单 | E3 | 对进行中订单做 interrupt | 写 | 未执行 |
| F 充电调度 | F1 | 指定该车充电 | 写 | **本轮跳过** |
| G 停靠 | G1 | 空闲返停靠点 | 写 | **本轮跳过** |
| H 启停设备 | H1 | disable 测试车 | 写 | 未执行 |
| H 启停设备 | H2 | enable 测试车（收尾） | 写 | 未执行 |

实测后更新本表，并在 [`execution-log.md`](./execution-log.md) 对应编号写时间戳与摘要。

出现 `等待人工` 时：按 [`../../README.md`](../../README.md) §6 握手——AI 停写操作，用户做完回一句，AI 复查接口后再继续。

## 协作说明（接手顺序）

1. 读 [`../../README.md`](../../README.md)（项目定位 + 人工干预协议）与 [`../../safety-boundaries.md`](../../safety-boundaries.md)。
2. 读 [`../../environment.local.json`](../../environment.local.json)（基址、账号、测试车 key、在线状态）。
3. 读本文件的「本轮状态表」，看做到哪一条。
4. 若状态是 **`等待人工`**：先读 [`execution-log.md`](./execution-log.md) 对应条目里的等待说明，**不要继续写操作**，等用户回复后再复查接口并往下做。
5. 否则按依赖顺序图从下一条未执行项继续；把调用与返回补进 `execution-log.md`。
6. F1 / G1 保持「本轮跳过」，除非用户明确改回「未执行」并授权。
