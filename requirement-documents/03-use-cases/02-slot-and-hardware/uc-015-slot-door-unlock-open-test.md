---
id: UC-015
type: use-case
title: "仓门开锁开启测试"
status: draft
priority: medium
created_by: "ZhengyuShao 邵正宇"
updated_by: "ZhengyuShao 邵正宇"
created: 2026-07-09
updated: 2026-07-30
primary_actor: "设备/电气维护人员 R-11"
secondary_actor: "无（本 UC 只对 IO 模块下发开锁指令并读取反馈，不涉及 MES/RIOT）"
frequency: "待定，预期为低频的人工维护类操作（如设备安装调试、定期巡检、故障复测），与 [[uc-014-enable-disable-slot|UC-014]] 同量级或更低"
related_uc: ["UC-001", "UC-004", "UC-005", "UC-010", "UC-014", "UC-016", "UC-017", "UC-018"]
related_br: []
aliases: ["UC-015"]
---

# UC-015 仓门开锁开启测试

## 描述

维护人员在车辆配置维护态下，通过车载维护界面选择目标仓位并发出一次开锁（DO）脉冲，现场观察仓门是否由弹簧正常弹开，并核验锁 DI 和 DO 回读；测试完成后手动关门并确认锁 DI 恢复锁闭。本操作不经过装卸业务核验，但必须满足整车无货、无活动业务、禁止移动和禁止新分配的维护边界。

> 服务端只负责允许车辆进入配置维护态，不直接写 IO；车载上位机是唯一 IO 控制者。仓位管理启用状态不能覆盖维护态或硬件不可操作状态。

## 触发条件

维护人员因设备安装调试、定期巡检或故障复测，需要验证某仓位的开锁/开门机械功能。

## 前置条件

**系统与接口**

1. 车载上位机与 IO 模块通信正常，可下发开锁 DO 并读取锁 DI 和 DO 回读。

**任务与数据**

2. 车辆已经在线请求并获服务端允许进入车辆配置维护态；整车无货、无活动仓位操作、业务内容或预留，全部仓门初始锁闭且全部开锁输出已复位。

**人员与权限**

3. 维护人员具备通过维护界面对仓位下发开锁指令的操作权限（具体角色待定，见 Notes）。

## 后置条件

**设备与硬件**

1. 若测试正常：目标仓位仓门在下发开锁指令后正常弹开/开启，维护人员手动关闭后，仓门恢复关闭、电子锁恢复锁闭状态。
2. 若测试异常：目标仓位被标记为测试未通过，车载仓位可操作性变为不可操作；人工启用不能覆盖该硬件结果。

**任务与数据**

3. 本次测试结果（维护人员、仓位号、测试类型：开锁开启测试、结果：正常/异常、时间戳）被记录到本地数据库，用于追溯。This test result (engineer, slot number, test type: unlock/open test, result: pass/fail, timestamp)

## 假设

1. 维护人员会正确判断仓门是否正常弹开；现场没有独立门位传感器，系统以锁 DI 为机构反馈并以人工目视作为实际弹门结果。

## 正常流程

### 15.0

1. 维护人员在维护界面中选择目标仓位
2. 维护人员点击“开锁测试”，车载上位机先记录测试准备状态，再向该仓位下发一次开锁 DO 脉冲
   2.1 系统核验该开锁指令是否被 IO 模块正常接收（见 Exception Flow E2.1）
3. 维护人员现场观察该仓位仓门是否正常弹开/开启
   3.1 维护人员判定仓门是否确实正常弹开/开启（见 Exception Flow E3.1）
4. 系统确认锁 DI 变为未锁，并确认脉冲 DO 自动复位；不得重复开锁
5. 维护人员手动关闭仓门，系统确认锁 DI 恢复锁闭
6. 系统记录维护人员、仓位号、各阶段 DO/DI、目视结果和时间戳

## 备选流程

不存在需要区分的备选流程：无论维护人员是逐个测试单个仓位，还是在同一次巡检中依次测试多个仓位，均按 Normal Flow 相同的步骤逐一执行，不构成触发方式或步骤不同的分支路径。

## 异常流程

以下每条异常均以 `E<步骤号>` 编号，与 Normal Flow 中触发该异常的具体步骤一一对应：

* E2.1 开锁指令下发失败（通信异常/IO 模块未正常接收）
* E3.1 仓门未能正常弹开/开启

### E2.1 开锁指令下发失败

1. 系统按第 2 步向目标仓位下发开锁指令
2. 系统按第 2.1 步核验，发现该指令未能被 IO 模块正常接收（如通信超时、IO 模块无响应）
3. 系统提示该仓位开锁指令下发异常，测试判定为"异常"，记录本次测试结果
4. 维护人员排查车载上位机与 IO 模块之间的通信链路，修复后重新发起测试

### E3.1 仓门未能正常弹开/开启

1. 系统已正常下发开锁指令（第 2 步核验通过）
2. 维护人员按第 3.1 步现场核验，发现该仓位仓门未能正常弹开/开启（如卡滞、无反应）
3. 维护人员判定该仓位硬件异常（电子锁/机械故障），系统将该仓位标记为"异常锁定"或"测试未通过"，暂停对该仓位的业务分配
4. 维护人员排查并修复电子锁/机械故障（必要时联系设备供应商）后，重新发起本测试，直至测试通过
5. 测试通过、整车机构安全并完成服务端对账和人员明确重新投运后，车辆才可恢复业务

## 备注

* 本 UC 是用户提出的新场景："测试仓位的门是否工作正常，测试仓门是否能弹开"，并同时用于落地 [[uc-014-enable-disable-slot|UC-014]] 中遗留的"维护人员可通过自己界面开关仓门"能力（原 UC-014 Assumption 第 3 条、Notes 待补充事项第 2 条）。
* 本 UC 与 [[uc-016-slot-light-curtain-function-test|UC-016]]、[[uc-017-slot-door-state-detection-test|UC-017]] 可在同一维护窗口连续执行；UC-017 验证的是锁反馈与闩合，不存在独立门状态 DI。

## 关联用例

* [[uc-001-load-completed-lot-into-slot|UC-001]]、[[uc-005-retrieve-mis-stored-product-from-slot|UC-005]]、[[uc-010-unload-completed-lot-at-destination-station|UC-010]]：本 UC 的开锁/开门操作独立于这些业务流程的核验逻辑之外，不复用、也不受它们的业务核验约束。
* [[uc-004-slot-door-safety-interlock|UC-004]]：该 UC 是跨场景的通用仓门-移动安全联锁，包含开门前核验 AGV 是否移动（Flow A）与门开启期间的持续移动监控（Flow B）；本 UC 属于维护场景，通常在 AGV 静止/停用状态下进行，暂不要求联动该 UC 的任一 Flow，具体是否需要联动待后续确认。
* [[uc-014-enable-disable-slot|UC-014]]：人工启用/禁用与车载硬件可操作性相互独立；测试失败不能由人工启用覆盖。
* [[uc-016-slot-light-curtain-function-test|UC-016]]：该 UC 的光幕功能测试需要以本 UC 的开门动作为前提。
* [[uc-017-slot-door-state-detection-test|UC-017]]：该 UC 的锁反馈与仓门闩合测试需要以本 UC 的开锁动作作为前提。
* [[uc-018-io-point-mapping-verification-test|UC-018]]：该 UC 验证的是 IO 点位映射配置本身的正确性，是本 UC 能够正常工作的更底层前提（若映射错误，本 UC 可能实际操作了错误的物理仓位）。

## 其他信息

