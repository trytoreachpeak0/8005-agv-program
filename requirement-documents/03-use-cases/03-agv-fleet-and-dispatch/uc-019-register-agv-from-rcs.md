---
id: UC-019
type: use-case
title: "从 RCS 接入 AGV"
status: draft
priority: high
created_by: "ZhengyuShao 邵正宇"
updated_by: "ZhengyuShao 邵正宇"
created: 2026-07-13
updated: 2026-07-13
primary_actor: "运维/调度管理员（R-13）"
secondary_actor: "RCS/RIOT"
frequency: "低频；新增或更换 AGV 时执行"
related_uc: ["UC-013", "UC-020", "UC-021", "UC-022", "UC-023"]
related_br: ["BR-002"]
aliases: ["UC-019"]
---

# UC-019 从 RCS 接入 AGV

## 描述

AGV 运维/调度管理员从 RCS/RIOT 已存在的车辆列表中选择一台尚未接入本系统的 AGV，建立本地车辆档案并补充任务分配所需配置。本操作不在 RCS/RIOT 中创建车辆，也不直接连接 AGV。

新接入车辆默认处于“已禁用（Disabled）”状态；配置和现场检查完成后，由管理员通过 [[uc-013-enable-disable-agv|UC-013]] 手动启用。

## 触发条件

管理员需要将一台已在 RCS/RIOT 注册的 AGV 纳入本系统管理和任务分配。

## 前置条件

1. RCS/RIOT 接口在线，且本系统有权限查询车辆列表和基础状态。
2. 目标 AGV 已在 RCS/RIOT 中存在。
3. 管理员具有 AGV 接入权限。

## 后置条件

1. 本地新增唯一 AGV 档案，并保存 RCS/RIOT 车辆 ID 映射。
2. 保存本地名称、车型、仓位配置、服务区域、最低接单电量、调度权重和维护备注。
3. 新 AGV 状态为“已禁用”，不会参与 [[uc-023-allocate-transport-tasks-to-agv|UC-023]]。
4. 接入操作及配置内容被记录到审计日志。

## 假设

1. RCS/RIOT 是可接入车辆及其外部车辆 ID 的来源。
2. 本系统只维护本地业务档案，不负责在 RCS/RIOT 中创建、删除或修改 AGV 本体配置。
3. 仓位配置描述本系统可使用的业务仓位，不替代 IO 点位映射配置。

## 正常流程

### 19.0

1. 管理员打开“新增 AGV”界面。
2. 系统查询 RCS/RIOT 车辆列表，并标识已接入和未接入车辆。
3. 管理员选择一台未接入车辆。
4. 系统读取并展示 RCS/RIOT 车辆 ID、在线状态及可获得的基础信息。
5. 管理员填写或确认本地名称、车型、仓位数量/规格/载重、服务区域、最低接单电量、调度权重和维护备注。
6. 系统校验车辆 ID 唯一性及必填配置完整性。
7. 管理员确认保存。
8. 系统创建本地 AGV 档案，将其置为“已禁用”，并记录审计日志。
9. 系统提示管理员完成检查后通过 [[uc-013-enable-disable-agv|UC-013]] 启用车辆。

## 备选流程

### A2.1 刷新 RCS/RIOT 车辆列表

1. 管理员发现目标车辆未显示，手动刷新列表。
2. 系统重新查询 RCS/RIOT；若目标车辆仍不存在，管理员联系 RCS/RIOT 管理人员先完成外部车辆注册。

## 异常流程

查询失败

1. 系统无法取得完整车辆列表。
2. 系统不允许凭空手工创建 RCS 车辆 ID，提示接口异常并记录日志。

### E6.1 车辆已接入

1. 系统发现目标 RCS/RIOT 车辆 ID 已存在于未归档或已归档的本地档案。
2. 系统拒绝重复创建，并引导管理员查看现有档案；已归档车辆的恢复方式 TBD。

### E6.2 配置不完整或不合法

1. 必填配置缺失，或仓位、载重、电量阈值、权重等值不合法。
2. 系统拒绝保存并逐项提示修正。

## 备注

- 车辆 ID 是跨系统映射的唯一标识，不允许重复。
- “接入”与“启用”分开，避免配置不完整的车辆立即参与自动分配。
- 已归档车辆是否允许恢复，以及恢复后是否沿用原档案，TBD。

## 关联用例

- [[uc-020-maintain-agv-dispatch-profile|UC-020]]：维护本 UC 创建的本地业务配置。
- [[uc-013-enable-disable-agv|UC-013]]：完成检查后启用新接入车辆。
- [[uc-021-archive-agv|UC-021]]：不再使用时归档本地档案。
查看接入结果及可分配状态。
- [[uc-023-allocate-transport-tasks-to-agv|UC-023]]：仅已启用且满足条件的车辆参与分配。

## 其他信息
